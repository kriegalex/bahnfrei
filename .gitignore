# build artifacts
/bahnfrei
/bahnfrei.exe
/dist/

# local runtime data (SQLite lives beside the binary in venue mode)
/data/
*.db
*.db-wal
*.db-shm

# test & coverage output
coverage.out
coverage.html
*.test

# TS islands toolchain (added in TASK-005)
node_modules/

# Playwright E2E artifacts (TASK-009)
/e2e/.bin/
/e2e/test-results/
/e2e/playwright-report/
/e2e/blob-report/

# editors / OS noise
.DS_Store
.idea/
.vscode/

# claude code session artifacts
.claude/scheduled_tasks.lock
.claude/worktrees/
# agent working state (milestones, session notes) — never committed, see CLAUDE.md
.claude/runbook/
