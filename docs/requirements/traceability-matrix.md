# Requirements Traceability Matrix

**Document status:** Phase A baseline. Chain: `STR → SYS → UC → test → verification method`.
The **test** column is populated in Phase B (test IDs/paths); at baseline it is `Phase B`.
**Verification methods:** T = automated Test, D = Demonstration (scripted manual),
I = Inspection (artifact/document review), A = Analysis (benchmark/load/measurement).

## 1. Stakeholder → System requirements (forward coverage)

Every STR maps to ≥1 SYS. (Priorities per StRS §3.)

| STR | System requirements |
|-----|---------------------|
| STR-001 | SYS-001, SYS-002, SYS-003 |
| STR-002 | SYS-004, SYS-071 |
| STR-003 | SYS-006 |
| STR-004 | SYS-001, SYS-120 |
| STR-005 | SYS-010, SYS-011, SYS-012, SYS-015 |
| STR-006 | SYS-013 |
| STR-007 | SYS-005, SYS-014, SYS-015 |
| STR-008 | SYS-016, SYS-028, SYS-046 |
| STR-009 | SYS-017 |
| STR-010 | SYS-018 |
| STR-011 | SYS-025 |
| STR-012 | SYS-026, SYS-027, SYS-028, SYS-030, SYS-121 |
| STR-013 | SYS-029, SYS-030 |
| STR-014 | SYS-040, SYS-041, SYS-045, SYS-050, SYS-142 |
| STR-015 | SYS-042, SYS-043 |
| STR-016 | SYS-031, SYS-044, SYS-053, SYS-121, SYS-142 |
| STR-017 | SYS-048 *(Later)* |
| STR-018 | SYS-060, SYS-061, SYS-062, SYS-063 *(L)*, SYS-064 *(L)*, SYS-078 *(L)* |
| STR-019 | SYS-046, SYS-047 |
| STR-020 | SYS-080 *(L — DEC-013)*, SYS-082 *(L)*, SYS-085, SYS-087, SYS-093, SYS-130, SYS-149 *(P)* |
| STR-021 | SYS-083, SYS-085, SYS-086 |
| STR-022 | SYS-070, SYS-071, SYS-074, SYS-113, SYS-122, SYS-153 *(P)* |
| STR-023 | SYS-072, SYS-111 |
| STR-024 | SYS-041, SYS-049, SYS-050, SYS-051, SYS-142 |
| STR-025 | SYS-073, SYS-077, SYS-078 *(L)* |
| STR-026 | SYS-073 |
| STR-027 | SYS-075 *(Later)* |
| STR-028 | SYS-070 |
| STR-029 | SYS-003, SYS-005, SYS-010, SYS-052 |
| STR-030 | SYS-052, SYS-010 (para extension: Later, DEC-007) |
| STR-031 | SYS-091, SYS-092, SYS-093, SYS-100, SYS-101, SYS-102, SYS-104, SYS-105, SYS-150 *(P)* |
| STR-032 | SYS-100, SYS-102, SYS-103 |
| STR-033 | SYS-074, SYS-110, SYS-111 |
| STR-034 | SYS-112, SYS-113, SYS-115, SYS-153 *(P)* |
| STR-035 | SYS-114, SYS-115, SYS-117, SYS-120, SYS-131, SYS-147 *(P)*, SYS-150 *(P)*, SYS-151 *(P)*, SYS-152 *(P)* |
| STR-036 | SYS-092, SYS-132, SYS-133, SYS-146, CON-02 |
| STR-037 | SYS-062, SYS-073, SYS-078 *(L)*, SYS-105, SYS-144 |
| STR-038 | SYS-092, SYS-104, SYS-116, SYS-140, SYS-141, SYS-142, SYS-143, SYS-144, SYS-145, SYS-146 |
| STR-039 | SYS-084, SYS-131, SYS-132 |
| STR-040 | SYS-090, SYS-091, SYS-086 |
| STR-041 | SYS-081, SYS-084, SYS-085, SYS-086, SYS-087, SYS-130, SYS-148 *(P)*, SYS-149 *(P)* |
| STR-042 | SYS-076, SYS-077, SYS-078 *(L)* |
| STR-043 | SYS-053, SYS-077 |
| STR-044 | SYS-115 |
| STR-045 | SYS-116, SYS-117, SYS-152 *(P)* |
| STR-046 *(P)* | SYS-147 *(P)*, SYS-148 *(P)*, SYS-149 *(P)*, SYS-151 *(P)* |

**Coverage check:** 46/46 STR covered. Zero orphan stakeholder requirements.
*(2026-07-05 delta: STR-042/043 added from founder answers; STR-030 re-scoped per DEC-007.
2026-07-13 delta: STR-044/045 added from founder UX request. 2026-08-06 delta: STR-046 and
SYS-147–153 added from the volunteer usability walkthrough — marked *(P)* = proposed,
pending founder ratification; see `../delivery/usability-audit-volunteer-2026-08.md`.)*

## 2. System requirements → Use-cases → verification

Every SYS maps to ≥1 STR (see §1) and is verified via a UC criterion, a CI gate, or an
inspection/analysis procedure.

| SYS | Verified by (UC / procedure) | Method | Test (Phase B) |
|-----|------------------------------|--------|----------------|
| SYS-001 | UC-001 #2 | T | TASK-006 (meet setup): `internal/app` `TestCreateMeetUC001_2`, `TestUpdateAndArchiveMeet`, `TestCreateMeetAuthorization`; `internal/web` `TestMeetCreationUC001_2`, `TestMeetEditConflictAndArchive`; `internal/store` `TestMeetCreateAndGet`, `TestSessionsPerDay`; `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` |
| SYS-002 | UC-001 #3 | T | TASK-006 (meet setup): `internal/app` `TestAddEventsUC001_3`, `TestAddEventValidation`; `internal/web` `TestEventProgrammeUC001_3`; `internal/store` `TestEventProgramme`; `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` |
| SYS-003 | UC-002 #5 | T | TASK-004 (domain core): `internal/domain` `TestDisciplineCatalog_SYS003Coverage` (all 5 SyRS §2 families + core WA codes present), `TestDiscipline_CategoryCorrectTechnicalVariant`, `TestDiscipline_HurdleVariants` (per-category technical variants), `TestDisciplineCatalog_UBSKidsCupDisciplines`; organizer-defined custom disciplines proven generic by `TestParseDisciplineCatalog_CustomCatalogNoCodeChange`; rule-data verification pass 2026-07-15 (OQ-019): youth hurdle/implement variants per the primary Swiss Athletics WO 2026 §8.1.1–§8.1.3, `TestCatalogYouthVariantsWO2026` |
| SYS-004 | UC-001 #4 | T | TASK-006 (meet setup): `internal/app` `TestTimetablePublishAndAmendUC001_4`; `internal/web` `TestTimetablePublishAmendPublicUC001_4`; `internal/store` `TestTimetablePublishRetainsVersions`; `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` |
| SYS-005 | UC-002 #1–#4 | T | TASK-004 (domain core): `internal/domain` `TestResolveDefaultCategory_SwissAthletics_UC002_1`, `TestResolveDefaultCategory_CalendarYearTransition`, `TestEvaluateEntry_StartUpAndDisciplineBar`, `TestParseCategoryScheme_CustomSchemeNoCodeChange` (built-in Swiss Athletics + UBS Kids Cup schemes, data-interpreter resolver, custom-scheme load); rule-data verification pass 2026-07-15 (OQ-017/OQ-018): WO 2026 §1.1/§1.2/§1.5 primary verification, `TestSwissSchemeYouthSprintBarsWO15e`, `TestSwissSchemeMastersM30WO11` |
| SYS-006 | UC-001 #5 | T | TASK-006 (meet setup): `internal/app` `TestSanctioningSummaryUC001_5`; `internal/web` `TestSanctioningSummaryUC001_5Web`; `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` |
| SYS-010 | UC-004 #1, UC-028 #1 | T | TASK-017 (entry import & eligibility): `internal/store` `TestFindAthleteByExternalIDSYS013`, `TestFindAthleteByNaturalKeySYS013`, `TestSetAthleteExternalIDSYS013` (licence number stored as `ExternalIDs`, ADR-005 §6); `internal/app` `TestCSVImportAlabusProfileSYS013UC004_4` (athlete person data + licence number mapped from an import row). UC-028 #1 mixed-category fields presentation lands with TASK-022 — see SYS-052. |
| SYS-011 | UC-003 #1–#3 | T | TASK-016 (online entries): `internal/store` `TestEntryStoreCreateAndGetSYS011`, `TestEntryStoreDuplicateRejectedSYS011UC003_1`, `TestEntryStoreListByMeetAndSubmitter`; `internal/app` `TestOnlineEntryIndividualSYS011UC003_1`, `TestOnlineEntryClubBulkSYS011UC003_2`, `TestOnlineEntryBulkAtomicSYS011`, `TestOnlineEntryDeadlinePassedRejectedSYS011UC003_3`, `TestSubmitEntryRequiresRoleSYS011`; `internal/web` `TestOnlineEntryIndividualFlowSYS011UC003_1Web`, `TestOnlineEntryClubBulkFlowSYS011UC003_2Web`, `TestOnlineEntryDeadlinePassedRejectedWebSYS011UC003_3`, `TestEntriesBibsFeesRequireRoleSYS090`. TASK-039 (DEC-023/OQ-033 optional licence-number field on these same forms): `internal/web` `TestOnlineEntryIndividualLicenceFlowDEC023TASK039Web`, `TestOnlineEntryIndividualLicenceFieldErrorDEC023TASK039Web`, `TestOnlineEntryBulkLicenceFlowDEC023TASK039Web` |
| SYS-012 | UC-003 #4 | T | TASK-016 (online entries): `internal/domain` `TestRelayTeam_Validate`; `internal/store` `TestRelayTeamCreateAndUpdateCompositionSYS012`; `internal/app` `TestOnlineEntryRelaySYS012UC003_4`; `internal/web` `TestOnlineEntryRelayFlowSYS012UC003_4Web` |
| SYS-013 | UC-004 #1–#4 | T | TASK-017 (entry import & eligibility): `internal/domain` `TestBuiltinImportMappingProfiles_SystemNativeAndAlabusSYS013`, `TestParseImportMappingProfile_RequiredFieldMissing`, `TestParseImportMappingProfile_DuplicateHeaderRejected`, `TestParseImportMappingProfile_UnknownFieldRejected`, `TestImportMappingProfile_CustomProfileNoCodeChange`; `internal/store` `TestFindAthleteByExternalIDSYS013`, `TestFindAthleteByNaturalKeySYS013`, `TestSetAthleteExternalIDSYS013`; `internal/app` `TestCSVImportAcceptsValidRowsSYS013UC004_1`, `TestCSVImportIdempotentSYS013UC004_2`, `TestCSVImportMalformedRowsRejectedSYS013UC004_3`, `TestCSVImportAlabusProfileSYS013UC004_4`, `TestCSVImportPreviewDoesNotPersistSYS013UC004`, `TestCSVImportRequiresOfficeRoleSYS090`, `TestCSVImportUnknownProfileRejected`, `TestCSVImportMissingRequiredColumnRejected`; `internal/web` `TestCSVImportFlowSYS013UC004Web`, `TestCSVImportMalformedRowSYS013UC004_3Web`, `TestEntriesImportEligibilityRequireOfficeRoleSYS090Web` |
| SYS-014 | UC-005 #1–#5 | T | TASK-017 (entry import & eligibility): `internal/domain` `TestEvaluateEligibility_EligibleWhenNoViolationsSYS014UC005`, `TestEvaluateEligibility_LicenceMissingBlockedSYS014UC005_1`, `TestEvaluateEligibility_LicenceMissingWarningAtCMeeting`, `TestEvaluateEligibility_YouthMaxDistanceBlockedSYS014UC005_2`, `TestEvaluateEligibility_YouthOneRacePerDayBlockedSYS014UC005_3`, `TestEvaluateEligibility_YouthOneRacePerDayAllowsFirstRace`, `TestEvaluateEligibility_BarredDisciplineBlockedSYS014UC005`, `TestEvaluateEligibility_CategoryMismatchBlocked`, `TestEvaluateEligibility_UnresolvedCategoryBlocked`, `TestEvaluateEligibility_UnknownCategoryBlocked`, `TestRequiresLicenceSYS014`, `TestTrackDistanceMetersSYS014`; `internal/store` `TestEntryEligibilityUpsertAndGetSYS014`, `TestEntryEligibilityOverrideClearsEffectiveOutcomeSYS014UC005_4`, `TestEntryEligibilityOverrideConflictSYS083`, `TestListEntryEligibilityByMeetSYS014`, `TestGetEntryEligibilityNotFound`; `internal/app` `TestOnlineEntryEligibilityLicenceMissingBlockedSYS014UC005_1`, `TestOnlineEntryEligibilityLicenceMissingWarningAtCMeetingSYS014UC005`, `TestOnlineEntryEligibilityYouthMaxDistanceBlockedSYS014UC005_2`, `TestOnlineEntryEligibilityOneRacePerDayBlockedSYS014UC005_3`, `TestOverrideEligibilitySYS014UC005_4`, `TestOverrideEligibilityRequiresOfficeRoleSYS090`, `TestEligibilityExceptionsSYS014`, `TestEntryEligibilityRequiresRoleSYS090`; `internal/web` `TestEligibilityOverrideFlowSYS014UC005_4Web`. TASK-039 (DEC-023/OQ-033 optional online-entry licence-number field, closing the HasLicence operator round-trip for UC-003): `internal/domain` `TestValidLicenceNo`; `internal/app` `TestOnlineEntryLicenceSetsHasLicenceSYS014DEC023TASK039`, `TestOnlineEntryEmptyLicenceUnchangedBehaviourDEC023TASK039`, `TestOnlineEntryMalformedLicenceRejectedDEC023TASK039`, `TestClubBulkEntryLicenceSYS014DEC023TASK039`, `TestOnlineEntryLicenceJoinKeyParityWithNaturalKeyMatchDEC023TASK039`, `TestOnlineEntryLicenceJoinKeyParityWithCSVImportDEC023TASK039` (the last two prove join-key parity with the CSV import path's `resolveImportAthlete`) |
| SYS-015 | UC-003 #5 | T | TASK-016 (online entries): `internal/domain` `TestEvaluateEntryStandardSYS015UC003_5`, `TestEvaluateEntryStandard_FieldDirection`, `TestEvaluateEntryStandard_NoStandardConfigured`, `TestEvaluateEntryStandard_SeedRequiredWhenStandardConfigured`, `TestEvaluateEntryStandard_InvalidMarks`; `internal/store` `TestEntryStoreCountActiveExcludesScratchedSYS015`, `TestEventEntryLimitRoundTripSYS015`; `internal/app` `TestOnlineEntryFailsStandardSYS015UC003_5`, `TestOnlineEntrySeedRequiredSYS015`, `TestOnlineEntryLimitReachedSYS015`; `internal/web` `TestEntryExceptionReportSYS015UC003_5Web` |
| SYS-016 | UC-008 #5 (scratch), UC-015 #3 (audit) | T | TASK-018 (scratch after deadline, gated + regeneration-safe): `internal/app` `TestGenerateHeatsManualOverrideSurvivesRegenerationSYS028UC008_5` (scratch then heat regeneration), `TestCheckInRequiresOfficeCapabilitySYS025` (operator-authorization gate on entry-status changes); `internal/store` `TestEntryStoreCountActiveExcludesScratchedSYS015` (scratched entry leaves active counts). TASK-019 (post-deadline modification audit): `internal/app` `TestCaptureAfterAnnouncementRequiresCorrectionSYS046SYS047UC015_2` (UC-015 #3 assertions: audit row carries actor, before/after and reason), `TestCorrectionWithoutReasonRejectedSYS046UC015_3` |
| SYS-017 | UC-006 #3 | T | TASK-016 (online entries): `internal/store` `TestMeetFeeScheduleRoundTripSYS017`; `internal/app` `TestFeeSummarySYS017UC006_3`, `TestFeeSummaryRequiresOrganizerRoleSYS017`; `internal/web` `TestFeeSummaryFlowSYS017UC006_3Web` |
| SYS-018 | UC-006 #1–#2 | T | TASK-016 (online entries): `internal/store` `TestEnsureParticipantIdempotentSYS018`, `TestUpdateParticipantBibSYS018UC006_1_2`; `internal/app` `TestBibAssignmentSYS018UC006_1`, `TestBibAssignmentDuplicateRejectedSYS018UC006_2`, `TestBibAssignRequiresOrganizerRoleSYS018`; `internal/web` `TestBibAssignmentFlowSYS018UC006_1_2Web`, `TestBibsPDFDownloadSYS018UC006_1Web` |
| SYS-025 | UC-007 #1–#3 | T | TASK-018 (check-in, seeding & progression): `internal/app` `TestCheckInCloseMarksUnconfirmedDNSSYS025UC007_1`, `TestReinstateEntrySYS025UC007_2`, `TestConfirmCheckInRejectsAlreadyConfirmedSYS025`, `TestReinstateEntryRejectsNonDNSSYS025`, `TestCheckInRequiresOfficeCapabilitySYS025`; `internal/web` `TestCheckInFlowHTTPSYS025UC007` |
| SYS-026 | UC-008 #1–#2 | T | TASK-018 (check-in, seeding & progression): `internal/domain` `TestBuiltinSeedingRulesTR20SYS026`, `TestSerpentineSeedingSYS026UC008_1`, `TestClubSeparationSYS026UC008_2`, `TestChooseHeatCountOddFieldSize`, `TestChooseHeatCountRejectsNonPositiveInputs`, `TestRankCandidatesKeepsUnseededAthletesAtEnd`, `TestSeedHeatsRejectsNonPositiveHeatCount`; `internal/store` `TestSaveAndListUnitAssignmentsSYS026`; `internal/app` `TestGenerateHeatsSYS026UC008_1`, `TestGenerateHeatsRejectsEmptyPool`, `TestGenerateHeatsOddFieldSize`, `TestGenerateHeatsRequiresOfficeCapability`, `TestUnitCaptureShowsLaneContextSYS026SYS027`; `internal/web` `TestSeedingGenerateAndOverrideHTTPSYS026UC008`, `TestPublicStartListShowsHeatsAndLanesSYS026SYS027` |
| SYS-027 | UC-008 #3–#4 | T | TASK-018 (check-in, seeding & progression) — WA TR20.4 fixture (D2.3): `internal/domain` `TestLaneDrawGroupedSYS027UC008_3`, `TestLaneDrawDeterministicWithSeed`, `TestUnusedInnerLanesSYS027UC008_4`, `TestLaneDrawByLotSYS027`, `TestDrawLanesGroupedRejectsUndersizedField`, `TestDrawLanesByLotRejectsInsufficientLanes`; `internal/store` `TestUnitAssignmentDuplicateLaneRejectedSYS027`; `internal/app` `TestGenerateHeatsLaneDrawSYS027UC008_3`, `TestUnitCaptureShowsLaneContextSYS026SYS027`; `internal/web` `TestPublicStartListShowsHeatsAndLanesSYS026SYS027`; rule-data verification pass 2026-07-15 (OQ-034): per-event-class TR 20.4.3–20.4.5 lane-group tables from the primary WA CR&TR 2026, `internal/domain` `TestLaneGroupClassesTR204` |
| SYS-028 | UC-008 #5 | T | TASK-018 (check-in, seeding & progression): `internal/store` `TestSaveUnitAssignmentUpsertBumpsVersionSYS028`, `TestUpdateUnitAssignmentManualAndReleaseSYS028`, `TestEnsureRoundUnitCountGrowsAndKeepsExtras`; `internal/app` `TestGenerateHeatsManualOverrideSurvivesRegenerationSYS028UC008_5`; `internal/web` `TestSeedingGenerateAndOverrideHTTPSYS026UC008` |
| SYS-029 | UC-009 #1–#3 | T | TASK-018 (check-in, seeding & progression): `internal/domain` `TestTrackAdvancementSYS029UC009_1`, `TestTrackAdvancementTimeTieSYS029UC009_2`, `TestTrackAdvancementManualCodesSYS029UC009_3`, `TestTrackAdvancementDNFExcludedFromTimeQualification`; `internal/store` `TestSetUnitAssignmentQualificationSYS029`; `internal/app` `TestAdvanceRoundSYS029UC009_1`, `TestAdvanceRoundTimeTieSYS029UC009_2`, `TestManualAdvanceRefereeCodeSYS029UC009_3`, `TestAdvanceRoundRejectsIncompleteRound`, `TestManualAdvanceRejectsIllegalCode`, `TestAdvanceRoundRequiresOfficeCapability`; `internal/web` `TestAdvanceRoundHTTPSYS029UC009` |
| SYS-030 | UC-009 #4 | T | TASK-018 (check-in, seeding & progression): `internal/domain` `TestFieldAdvancementSYS030UC009_4`, `TestFieldAdvancementNoStandardConfigured`, `TestFieldAdvancementCapacityZero`; `internal/app` `TestAdvanceRoundFieldSYS030UC009_4` |
| SYS-031 | UC-013 #1 | T | TASK-021 (vertical jumps & combined events): a combined-events competition is an ordered discipline sequence built on the existing Event/Round/Unit/Session foundation (TASK-018) — each discipline is its own `Event`, in template-declared order, on the `Session` the office schedules; "parallel groups" is the existing per-unit heat/flight mechanism (no new entities needed). Built-in templates `data/templates/wa-decathlon.json`/`wa-heptathlon.json` (`domain.TemplateWADecathlon`/`TemplateWAHeptathlon`) declare the ten/seven-discipline WA-standard order — `internal/app` `TestCreateCombinedEventsMeetFromTemplateSYS031SYS044UC013` (template + date + venue yield the complete discipline programme, mirroring UC-033 #1's mechanism for UKC). Day/group scheduling beyond one session is exercised through the same generic `Session`/`Unit` machinery TASK-018 already tests; a dedicated multi-day combined-events fixture is not repeated here (OQ-047). |
| SYS-040 | UC-010 #1 | T | TASK-008 (field & track capture) proves 0.01 s FAT-resolution capture for manually entered electronic times — `internal/domain` `TestValidateFATTime`; `internal/app` `TestUC010_2_HandTimeRoundUpAndProvenance`. TASK-019 (full track capture & corrections) adds per-race wind (a single reading applied uniformly to every mark in the unit, never per athlete, D5.1/SYS-050): `internal/store` `TestUnitWindRoundTripSYS040`; `internal/app` `TestSetUnitWindAppliesUniformlySYS040UC010_4` (UC-010 #4, applies retroactively and to later captures), `TestSetUnitWindRejectsNonWindDisciplineSYS040` (denial: wind on a non-wind-relevant discipline), `TestSetUnitWindRejectsFieldFamilySYS040` (settled-result wind is track-only — field events keep per-attempt wind, TASK-008, OQ-036); `internal/web` `TestCaptureWindAppliesUniformlySYS040UC010_4Web` (form re-renders the stored reading, invalid input rejected). Heat/lane-aware capture (using TASK-018 seeding) already lands on the capture grid (`TestUnitCaptureShowsLaneContextSYS026SYS027`) and `SaveTrackResult` now stamps the athlete's drawn lane onto the settled result. Finishing order is the standings rank (`trackStandings`, unchanged). Reaction times are deferred to the FinishLynx timing exchange (TASK-020, OQ-038) — no manual reaction-time field is added, matching how a hand-operated capture form is never the reaction-time source of record. |
| SYS-041 | UC-010 #2, #5 | T | TASK-008 (field & track capture): `internal/domain` `TestRoundUpHandTime` (D5.1 round-up fixtures); `internal/app` `TestUC010_2_HandTimeRoundUpAndProvenance` (manual vs electronic scoring columns, provenance kept); `internal/web` `TestUC010_TrackCaptureFlow` (hand marker "h" on every rendered output) — road-event whole-second rounding: TASK-019 *(still open — no road discipline in the catalog yet, see `domain.RoundUpHandTime`'s doc comment)* |
| SYS-042 | UC-011 #1–#4 | T | TASK-008 (field & track capture): `internal/domain` `TestAttemptValidate` (0.01 m marks, X/–/r, wind only where relevant), `TestRankFieldSeries_NextBestTieBreak`/`_IdenticalSeriesShareRank` (UC-011 #2), `_RetireeStillRanks` (UC-011 #3), `TestFieldContinuation_CutTop8`/`_BoundaryTieAllAdvance`/`_RetireeAndNMExcluded` (UC-011 #1); `internal/app` `TestUC011_FieldCaptureGridAndTieBreak` (UC-011 #4 recompute-on-save), `TestUC011_1_DefaultSeriesCutAndContinuation`, `TestUC011_3_RetireeStillRanks`, `TestCaptureValidationAndAuthorization`, `TestOnResultsChangedHook`; `internal/store` `TestSaveAttemptInsertAndRecapture`, `TestSaveAttemptConflicts`, `TestListUnitAttemptsOrdering`; `internal/web` `TestUC011_CaptureGridFlow` (grid + live standings over SSE) |
| SYS-043 | UC-012 #1–#4 | T | TASK-021 (vertical jumps & combined events): `internal/domain` `TestVerticalTrialValidateSYS043UC012` (D5.2/TR26.3 capture vocabulary), `TestVerticalEliminationSYS043UC012_1` (UC-012 #1 worked example verbatim: 1.60/1.65/1.70/1.75 with O/XO/XXO/XXX), `TestVerticalEliminationSYS043UC012_2` (a pass neither counts toward nor resets the three-consecutive-failures streak, which carries across heights), `TestVerticalRetirementSYS043UC012`, `TestVerticalSeriesStatusSYS043`, `TestVerticalCountbackSYS043UC012_3` (fewer attempts at the height last cleared, then fewer total failures; first-place tie flagged; a jump-off is scored as an appended height, no separate mechanism), `TestVerticalCountbackFixturesSYS043UC012_4` (5-athlete rule-consistency fixture — WA TR26.8 countback has no externally published numeric table the way scoring tables do, so this is the "reference fixture suite" for the algorithm itself), `TestVerticalUnrankedAthletesSYS043`, `TestGenerateHeightProgressionSYS043` (default 3 cm HJ / 10 cm PV bar increment, WA rule, sourced); `internal/store` `TestVerticalUnitConfigRoundTripSYS043` (office-configurable progression, extend-only), `TestSaveVerticalTrialInsertAndRecaptureSYS043`, `TestSaveVerticalTrialConflictsSYS043`, `TestListUnitVerticalTrialsOrderingSYS043`; `internal/app` `TestVerticalCaptureRequiresHeightsSYS043UC012`, `TestSetVerticalHeightsSYS043` (office-only, extend-only), `TestVerticalCaptureUC012_1`, `TestVerticalCaptureCountbackAndTieSYS043UC012_3`, `TestVerticalCaptureVersionConflictSYS086UC021`, `TestVerticalCaptureRespectsAnnouncementGuardUC015` (SYS-047 guard applies to the new capture path unmodified), `TestCaptureUnitsIncludesVerticalSYS043`; `internal/web` `TestVerticalCaptureRequiresHeightsThenGridWebSYS043UC012`, `TestVerticalHeightsConfigureIsOfficeOnlyWebSYS043`. A vertical capture sheet PDF (height columns, UC-018 #1) ships alongside (`handleVerticalCaptureSheetPDF`). |
| SYS-044 | UC-013 #1–#3 | T | TASK-021 (vertical jumps & combined events): the 2001 IAAF/WA combined-events A/B/C formula coefficients ship as data (`internal/domain/data/scoring/wa-combined-events-2001.json`, `domain.CombinedScoringTable`/`ParseCombinedScoringTable`/`Points` — track/jump/throw formula branches, floor rounding, manual-timing adjustment) — `internal/domain` `TestCombinedScoringReferenceFixtureDecathlonSYS044UC013_1` (Kevin Mayer's men's decathlon WR, Talence 2018-09-16, all ten events replayed, summed total matches the published 9126 exactly), `TestCombinedScoringReferenceFixtureHeptathlonSYS044UC013_1` (Jackie Joyner-Kersee's women's heptathlon WR, Seoul 1988-09-24, summed total matches the published 7291 exactly), `TestCombinedScoringManualTimingAdjustmentSYS044`, `TestCombinedScoringZeroFloorSYS044`, `TestCombinedScoringFloorRoundingSYS044`, `TestCombinedScoringUnknownColumnSYS044`, `TestParseCombinedScoringTableValidationSYS044`, `TestBuiltinCombinedScoringTablesSYS044`, `TestAverageWindSYS044UC013_4` (D5.3/D5.5 wind-legality figure; flagging a record candidate against it is TASK-022 scope). Standings reuse the existing generic `app.ResultsService.Standings`/`domain.RankCombined` (TASK-007) unchanged — a combined-events meet is scored through a per-meet side table (`meet_combined_scoring`, migration 0014) rather than the ordinary lookup `ScoringTable`, so no discipline-count assumption ever needed changing: `internal/app` `TestCreateCombinedEventsMeetFromTemplateSYS031SYS044UC013`, `TestCombinedEventsScoringAndStandingsSYS044UC013_1_2` (per-discipline points + cumulative standings after each discipline, UC-013 #1/#2), `TestCombinedEventsDNFAndWithdrawalSYS044UC013_3` (a DNF scores an explicit gap and the athlete continues; status visible), `TestCombinedEventsVerticalDisciplineScoresThroughWAFormulaSYS043SYS044UC012UC013` (the vertical+combined intersection: an HJ captured through the TASK-021 vertical service on a combined-events meet scores via this formula table, not the UKC lookup table). Rule-data verification pass 2026-07-15 (OQ-045): equal totals tie per WA TR 39 (`tieBreak` policy on the scoring table; UKC meets keep the Reglement §3 majority rule) — `internal/domain` `TestRankCombinedTiesStandTR39`/`TestCombinedScoringTableTieBreakPolicy`, `internal/app` `TestCombinedEventsEqualTotalsTieStandsTR39`. |
| SYS-045 | UC-010 #3 | T | TASK-008 (field & track capture): `internal/domain` `TestValidateCaptureStatus` (CR 25 capture vocabulary; DQ requires rule reference), `TestRenderStatus` ("DQ (TR16.8)" convention); `internal/app` `TestUC010_3_StatusVocabulary`; `internal/store` `TestResultStatusDetailRoundTrip`; `internal/web` `TestUC010_TrackCaptureFlow` — qualification codes (Q/q/…) render with round progression, TASK-018 |
| SYS-046 | UC-015 #2–#3 | T | TASK-003 (storage layer): `internal/store` `TestAuditImmutableByTrigger`, `TestAuditAppendAndTrail`. TASK-019 (full track capture & corrections) lands the correction-propagation flow: office corrections to an announced unit are gated by `app.ResultsService.CorrectResult`, mandatory reason enforced (`ErrCorrectionReasonRequired`) — `internal/app` `TestCaptureAfterAnnouncementRequiresCorrectionSYS046SYS047UC015_2` (audit actor/before/after/reason asserted directly against `store.ListAudit`), `TestCorrectionWithoutReasonRejectedSYS046UC015_3` (denial), `TestCorrectFieldResultSYS046UC015_2` (both track and horizontal-field units), `TestCorrectResultUnknownAthleteOrUnitSYS046`, `TestCorrectResultNewAthleteAfterAnnouncementSYS046UC015_2`; `internal/web` `TestCaptureAnnounceAndCorrectionFlowSYS046SYS047UC015Web` (reason required end to end over HTTP, corrected mark re-renders), `TestCaptureAnnounceAndCorrectRequireOfficeRoleSYS090UC015Web` (office-only, UC-015's actor). The audit trail itself remains the immutable `audit_log` (SYS-046, unchanged, TASK-003) — corrections write new rows, never mutate prior ones. TASK-040 (field-event correction UI, DEC-024/OQ-036) extends the correction path — previously track+horizontal-field only — to vertical-field units, and adds the office-facing correction form to the horizontal and vertical grids (previously track-only): `internal/app` `TestCorrectVerticalResultSYS046UC015_2` (vertical settled-level correction, re-announce, blocked further trial capture, reason enforced), `TestFieldStandingsReflectCorrectionSYS046SYS047UC015_2` (regression: the capture page's own live unit standings — distinct from the meet-wide `CurrentStandings` — used to rank/display purely from the raw, never-rewritten attempt series per OQ-036, so a correction's mark/status change never reached placings; fixed in `fieldStandings`), `TestCorrectResultStatusOnlyOnScoredMeetSYS046UC015_2` (regression: `CorrectResult` scored/record-evaluated an empty mark unconditionally, so a status-only DNS/DNF/DQ/NM correction always failed on any scored meet, for every family — unreachable by any prior test since the UI never exposed a status field before); `internal/web` `TestCaptureFieldCorrectionFlowSYS046SYS047UC015Web`, `TestVerticalCaptureCorrectionFlowSYS046SYS047UC015Web` (both mirror the track web flow: notice/columns only post-announcement, blocked plain capture names the correction flow, reason-missing field error preserves the row, a reasoned correction lands and re-announces); e2e `keyboard-operator-SYS114.spec.ts` "the field-event correction row is keyboard-only" (Tab/Enter-only capture then correction, the same live standings the app-level regression test covers). TASK-041 (bulk "mark remaining as DNS", DEC-025/OQ-070) adds a second audited bulk write shape alongside TASK-018's `checkin.close_dns` (checkin.go): one `capture.bulk_dns` audit row per entry marked, actor/timestamp/after-status, written in the same transaction as its insert-only result write — see the SYS-114 row for the full test list. |
| SYS-047 | UC-015 #1, #4 | T | TASK-019 (full track capture & corrections): `internal/domain` `TestComputeProtestStateSYS047UC015_1` (30-minute countdown from announcement, D8.3), `TestComputeProtestStateSYS047UC015_4` (window elapses to official automatically, no operator action); `internal/store` `TestAnnounceUnitSequenceAndLatestSYS047` (sequential, monotonic announcement history per unit); `internal/app` `TestAnnounceOpensProtestWindowSYS047UC015_1` (office-only announce action opens the window; office-role floor), `TestCorrectionAfterWindowExpiryRequiresEscalationSYS047UC015_2` (a correction past the window demands an explicit escalation reference — the TASK-019 scope note beyond the executable AC text), `TestUnitWindAndProtestStateUnknownUnitSYS040SYS047`; `internal/web` `TestCaptureAnnounceAndCorrectionFlowSYS046SYS047UC015Web` (provisional banner with countdown minutes, then official after backdating in the app-level test). Public-surface (SYS-070/071) provisional/official labeling per result row is deferred — the public results page presents division-level combined standings without per-unit granularity yet (OQ-036 addendum); the office capture view is the authoritative surface for the protest-clock state in this slice. TASK-040 gives the vertical-jump capture page (`verticaljump.templ`) the same protest banner/announce action track and horizontal already had — the announce route itself was already family-agnostic and reachable, only its page-level display was missing — `internal/web` `TestVerticalCaptureCorrectionFlowSYS046SYS047UC015Web` (provisional banner + countdown after announcing a vertical unit). |
| SYS-048 *(L)* | UC-029 | T | Phase B (Later) |
| SYS-049 | UC-016 #1, #5 | T | TASK-022 (records & mixed-category fields): the record-reference/PB-SB evaluator is a pure function so both the capture-save flagging path and the "why not" inspection agree — `internal/domain` `TestUC016_1_MeetingRecordFlagsLegalFATBetter` (MR flags on an equal/better legal FAT mark against a loaded meeting-record list), `TestUC016_5_PersonalBestFlagsWhenBettered`, `TestSeasonBestFlagsAnalogousToPersonalBest`, `TestNoHistoryMeansNoPBSBCandidate`, `TestBestMarks`/`TestBestMarksNoHistory` (the PB/SB history reduction, wind-assisted marks excluded, season = calendar year), `TestEvaluateRecordFieldHigherIsBetter` (field vs track comparison direction), `TestParseRecordListValidation`/`TestBuiltinRecordLists` (the loadable-list data interpreter, ADR-005 §4). End to end through capture: `internal/app` `TestUC016_1_MeetingRecordFlaggedLegalFAT` (flag on the settled result, the operator capture standings, and `Standings()` which feeds public results/exports), `TestUC016_5_PersonalBestFlaggedFromHistory` (cross-race PB from in-system history, and no flag when the mark does not better it), `TestSetMeetRecordListsAuthorizationAndValidation` (organizer-only meet configuration, `ResultsService.SetMeetRecordLists`/`RecordListOptions`/`MeetRecordListIDs`); `internal/store` `TestMeetRecordListsRoundTripSYS049` (the `meet_record_lists` association table, migration 0019), `TestListAthleteResultsByDisciplineSYS049` (the cross-meet PB/SB history query, status-only rows excluded); `internal/web` `TestUC016_RecordFlagRendersAndChecklistLinksThroughWeb` (flag renders on the capture standings page and links through). Exchange-format (`internal/exchange`) surfacing of flags is out of this task's scope (TASK-025, reconciled at merge) — see OQ-052. |
| SYS-050 | UC-010 #4, UC-016 #2–#3, UC-013 #4 | T | UC-013 #4 portion (TASK-021, vertical jumps & combined events): the wind-legality *computation* — `domain.AverageWind` (`internal/domain` `TestAverageWindSYS044UC013_4`) — averages the wind-relevant-discipline readings per D5.3/D5.5. TASK-022 lands the *flagging* itself (UC-016 #2–#3): `internal/domain` `TestUC016_2_WindAssistedNeverFlags` (wind +2.4 sets no flag), `TestUC016_3_HandTimedRequiresFATForShortRaces` (a hand-timed mark better than a record requiring FAT, races ≤800m per D6.2, sets no flag), `TestRecordRequiresFAT_OnlyShortTrackRaces`, `TestWindAssisted`, `TestEvaluateRecordDoesNotBetterReference`; `internal/app` `TestUC016_2_WindAssistedNoFlagButPlacingStands` (no flag, but the wind-assisted mark still ranks — placing stands), `TestUC016_3_HandTimedNoFlagReasonInspectable` (the reason is inspectable, `domain.RecordCandidateOutcome.Reason`). Combined-events wind-average record flagging (averaging `AverageWind` into the evaluator for a combined-events meet's own records) is not wired in this task — see OQ-053. |
| SYS-051 | UC-016 #4 | T | TASK-022: `internal/domain` `TestUC016_4_ChecklistCoversRekordprotokollFieldsWithGaps` (`domain.BuildRecordChecklist` assembles timing class, wind, competitor count from in-system data; zero-test and photo-finish image reference as explicit gaps), `TestChecklistWindGapWhenNotYetRead`, `TestChecklistNoWindItemWhenNotWindRelevant`; `internal/app` `TestUC016_4_RecordChecklistGeneratedWithGaps` (`ResultsService.RecordChecklist` end to end against a flagged capture result); `internal/web` `TestUC016_RecordFlagRendersAndChecklistLinksThroughWeb` (the checklist page renders the in-system fields and the two gaps, reachable from the flagged standings row). Full record-submission-dossier generation is Later, out of scope per SYS-051's own text. |
| SYS-052 | UC-028 #1–#2 (para: UC-028 #3, Later) | T | TASK-007 proves the per-division split of a *meet-wide, points-based* combined-events field for UKC standings (`internal/app` `TestUC033_2_ScoringAndStandings`) — reused unchanged. TASK-022 adds the complementary *per-unit, raw-mark* split UC-028 needs (a single heat/race re-ranked per category, not a whole meet's points total): `internal/app` `TestUC028_1_MixedCategoryUnitCombinedAndSplit` (a 100m unit combining U16 M/U18 M entrants for lack of entries produces both the combined race result and per-category re-ranked standings, `ResultsService.UnitCapture`'s `CategorySplits`, reusing the same `scheme.ResolveDefaultCategory` resolution and `trackStandings`/`fieldStandings` ranking the combined view already uses — no ranking logic duplicated), `TestUC028_2_UKCPerBirthYearDivisionSplitInOneHeat` (the same mechanism applied to a UKC 60m heat spanning M12/M13, UC-028 #2's own example). Para sport-class split presentation (UC-028 #3) stays Later per DEC-007. |
| SYS-053 | UC-033 #1–#5 (amended #3) | T | TASK-007 (UKC template & scoring): `internal/domain` `TestUKCScoringOfficialFixtures` (fixtures from the published Wertungstabelle incl. next-lower-points rounding), `TestScoringTableIsData` + `TestUC033_5_ScoringTableSwapAtServiceLevel` (UC-033 #5 data swap), `TestBuiltinUKCTemplate`, `TestRankCombinedReglementRules`/`TestRankCombinedHighestSingleTieBreak`/`TestRankCombinedTrueTie`/`TestRankCombinedMissingDiscipline` (Reglement Rangierung; UC-033 #3 provisional half); `internal/app` `TestUC033_1_CreateUKCMeetFromTemplate`, `TestUC033_2_ScoringAndStandings`, `TestUC033_3_MissingDiscipline` (provisional), `TestResultsAuthorizationAndValidation`; `internal/store` `TestSaveResultUpsertAndListing`, `TestParticipantsAndBibUniqueness`; `internal/web` `TestUKCTemplateRosterStandingsFlow`. TASK-036 (final-vs-provisional standings, DEC-016/OQ-020): `internal/domain` `TestUC033FinalStandingsUnrankedIncomplete`, `TestUC033FinalStandingsAttemptedNoValidResultStillRanks`, `TestFinalRankCombinedOutOfCompetition`, `TestAttemptedNoValidResult`; `internal/app` `TestUC033FinalStandingsUnrankedIncomplete`, `TestSetOutOfCompetition`, `TestOMXRoundTripPreservesFinalStandingsSYS073UC033`; `internal/store` `TestUpdateParticipantOutOfCompetition`; `internal/web` `TestUC033FinalStandingsWebSurfacesSweep` |
| SYS-060 | UC-014 #1–#2 | T | TASK-020 (timing exchange & timing agent, ADR-006): `internal/exchange` — fixtures transcribed from vendor documentation (see file-level source comments in `finishlynx.go`) — `TestEncodePPLRoundTripSYS060UC014_1`, `TestEncodeSCHRoundTripSYS060UC014_1`, `TestEncodeDecodeEVTRoundTripSYS060UC014_1` (incl. a lane-swap re-export, UC-014 #2); `internal/app` `TestExportTimingFilesFieldFidelitySYS060UC014_1` (start list built from real seeded `unit_entries`/lane draws — bib, name, club, lane all round-trip through the codec), `TestExportTimingFilesLaneSwapReExportSYS060UC014_2`; `internal/store` `TestAssignTimingUnitNumbersIsIdempotentSYS060` (event/round/heat numbers assigned once, never renumbered — Database Files' own sample data keeps a stable event number across re-exports); `internal/web` `TestTimingExportImportResolveEndToEndWeb` (office UI download links, office-role floor); `cmd/bahnfrei` `TestPollAndWriteExportsWritesOnlyOnChange` (the watched-folder timing agent rewrites only the file(s) that actually changed on the hub) |
| SYS-061 | UC-014 #3–#5 | T | TASK-020: `internal/exchange` `TestParseLIFRealWorldSampleSYS061UC014_3` (a real third-party `.lif` sample — see source citation — decoding places incl. ties, an unassigned lane, and a DNS status row), `TestClassifyPlaceEdgeCasesSYS061UC014_3` (DNS/DNF/DQ mapped, FS/SC/ADV correctly left unmapped, OQ-048), `TestEncodeParseLIFRoundTripSYS061UC014_1`, `TestParseLIFRejectsEmptyFile`; `internal/app` (denial/conflict paths first-class per the task's testing convention) `TestImportLIFAppliesCleanlySYS061UC014_3` (clean apply, provisional pending announcement), `TestImportLIFUnknownBibSurfacesConflictSYS061UC014_4` (+ resolution via reassignment), `TestImportLIFExistingManualResultSurfacesConflictSYS061UC014_5` (a `.lif` time never silently overwrites an existing manual result; "keep" leaves it untouched) and `TestImportLIFReplaceOverwritesExistingManualResult` (the explicit "replace" counterpart), `TestImportLIFUnresolvedUnitSurfacesConflict`, `TestImportLIFUnmappedStatusSurfacesConflictOQ048`, `TestImportLIFAnnouncedUnitRequiresCorrectionSYS047` (the TASK-019 announce/correction guard: a queued conflict on an announced unit routes through the audited `CorrectResult` flow, mandatory reason enforced), `TestImportTimingFileRequiresOfficeCapability`; `internal/store` `TestTimingImportBatchAndConflictLifecycleSYS061UC014_4` (a resolved conflict cannot be resolved twice — no silent second application), `TestSaveResultWithSourceTracksProvenanceSYS041` (manual vs. import provenance, ADR-006 consequence #4); `internal/web` `TestTimingExportImportResolveEndToEndWeb` (upload → conflict shown on the office page → resolve, over real HTTP); `cmd/bahnfrei` `TestScanAndUploadLIFFilesUploadsOnceThenOnChange`/`TestScanAndUploadLIFFilesRejectsBadToken` (the timing agent uploads a `.lif` once per content change, a rejected upload is retried next cycle rather than dropped) |
| SYS-062 | UC-014 #6 | T | TASK-020: `internal/exchange` `TestGenericCSVRoundTripLosslessSYS062UC014_6` (the documented schema round-trips every field incl. status/wind/edge rows losslessly), `TestParseCSVRejectsWrongSchemaSYS062UC014_6` (denial: a drifted header is rejected, never misread); `internal/app` `TestGenericCSVImportExportRoundTripSYS062UC014_6`, `TestImportGenericCSVManualTimingAndUnmappedStatus` (the same conflict/apply pipeline `.lif` uses); `internal/web` `TestTimingExportImportResolveEndToEndWeb`, `TestAgentAPIExportAndImportWeb` (the timing agent can also submit the generic CSV fallback) |
| SYS-063 *(L)* | UC-032 | T | Phase B (Later) |
| SYS-064 *(L)* | UC-031 | T | Phase B (Later) |
| SYS-070 | UC-017 #2–#3 | T | TASK-010 (public live results, PoC scope): `internal/web` `TestPublicPagesAnonymousAccessSYS070UC017_2` (overview/timetable/start lists/results fetch with no session cookie, 200, no login wall, viewport meta present), `TestPublicPagesArchivedMeetUC017_3` (same stable `/m/{id}/...` URLs still 200 with results after archiving) |
| SYS-071 | UC-017 #1 | T/A | T: TASK-010: `internal/web` `TestPublicResultsLiveUpdateSYS071UC017_1` (`ResultsService.SaveResult` publishes on the meet's SSE topic synchronously with the save — asserted via a non-blocking channel receive immediately after `SaveResult` returns — and a fresh fetch of the public results live-refresh fragment shows the new mark). A (load-bearing p95 latency under concurrent load): TASK-027/TASK-035 `internal/web` `TestSYS122ViewerScalingProfile`/`TestSYS122TwoThousandConcurrentViewers` (`perf` tag) — SSE delivery p95 measured 11–39 ms at 100–500 concurrent viewers and ~12 ms with 100 subscribers under full 2,000-viewer pressure, all inside the 10 s budget (the page-render half of that load test also now passes — TASK-035, ADR-004 §9, resolved OQ-066). Results: `docs/delivery/perf-and-recovery-task-027.md` |
| SYS-072 | UC-018 #1–#3 | T | TASK-011 (printables, PoC scope): `internal/pdf` (generic, domain-agnostic renderer over `codeberg.org/go-pdf/fpdf`) `TestBuildCaptureSheetGridSYS072`, `TestBuildTrackLaneSheetSYS072`, `TestBuildResultListMultiDivisionSYS072`, `TestBuildEmptySectionRendersHeaderOnly`, `TestBuildAccentedTextRoundTrips` (all parse the generated bytes back to text via `github.com/ledongthuc/pdf`, asserting real document structure, not just non-empty output); `internal/web` `TestCaptureSheetPDFFieldGridSYS072UC018_1` (horizontal-attempt grid: meet/discipline header, generation timestamp, trial columns, captured mark plus a still-blank trial — UC-018 #1), `TestCaptureSheetPDFTrackLaneSYS072UC018_1` (track lane sheet: bib/name populated, lane/time left blank for manual capture), `TestResultListPDFSYS072UC018_2` (UKC result list: division heading, rank/bib/name/total, reuses TASK-007's standings computation unchanged — UC-018 #2), `TestCaptureSheetPDFRequiresFieldOfficialRoleSYS072`/`TestResultListPDFRequiresOfficeRoleSYS072` (anonymous 403, SYS-090) *(PoC scope: only track and horizontal-field capture sheets plus the UKC division result list, matching `ResultsService.CaptureUnits`' current unit coverage; vertical-jump height columns and start lists land with TASK-021/M2. Track lane sheets render blank lane cells for hand-writing — lane draws are not modeled until TASK-018/M2. Document headings use the same `discipline.<code>`/domain-glossary catalog keys as the standings page (UC-018 #3), so DE/FR coverage matches SYS-074's PoC scope — see docs/requirements/open-questions-and-assumptions.md)*. TASK-022 adds the SYS-049 record/best flag codes to this same result-list PDF's mark cells (`internal/web` `resultListDocument`, exercised indirectly through `TestUC016_1_MeetingRecordFlaggedLegalFAT`'s `Standings()` assertion — no dedicated PDF-flag test; the PDF cell text is the same `m.RecordFlags` the standings page and public results render). |
| SYS-073 | UC-027 #1–#3 | T | TASK-025 (omx/v1 full-meet export/import, ADR-005): `internal/exchange` `TestEncodeDecodeOMXRoundTripSYS073UC027_1` (JSON export validates against the published `schema/omx-v1.schema.json`, UC-027 #1), `TestOMXV1SchemaIsPublishedAndSelfValidating` (SYS-144: the schema this system checks exports against is the exact artifact it publishes), `TestEncodeOMXResultsCSVSYS073` (the flat CSV derivative — "CSV and a self-describing structured format"), `TestValidateOMXRejectsEachDanglingReferenceKind` (every cross-reference kind — club/athlete/relay-team/event/round/unit/entry/assignment/result — fails loudly rather than silently reconstructing a partial result), `TestDecodeOMXRejectsWrongSchemaVersion`/`TestDecodeOMXToleratesAdditiveUnknownFields`/`TestDecodeOMXRejectsMalformedJSON` (SYS-144 versioning: a v1 reader refuses a v2 document, tolerates additive v1.x fields, rejects garbage); `internal/app` `TestExportOMXDocumentAndCSVSYS073UC027_1` (office-gated `ResultsService.ExportOMXDocument`/`ExportOMXResultsCSV` end to end against a captured result), `TestExportOMXDocumentAvailableImmediatelyAtCloseSYS073UC027_3` (a meet closed at time T exports immediately — no background job, synchronous read), `TestOMXRelayTeamRoundTrip` (relay-team roster round-trips with remapped ids), `TestUC027_2_OMXRoundTripProperty` (UC-027 #2: fixed-seed randomized full-meet generator — athletes/clubs/events/rounds/units/entries/assignments/results across the CR 25 status vocabulary with wind/points/placing/record-flags/in-session corrections/unit announcements — export → fresh system → re-import; official results (by discipline+category+athlete natural key, not literal id) reconstruct equivalently; seed and iteration count overridable via `OMX_PROPERTY_SEED`/`OMX_PROPERTY_ITERATIONS` for a failure's exact reproduction). *Scope note (OQ-057): Results cover individual entries only — relay-team race results have no capture path anywhere in this system yet (mirrors OQ-049); the schema's `recordFlags` field carries the SYS-049 evaluator's computed flags — merge reconciliation with TASK-022 closed by `internal/app` `TestOMXRoundTripCarriesComputedRecordFlagsSYS049SYS073` (a real computed `MR` flag survives export → schema validation → fresh-system re-import verbatim, never recomputed on import — OQ-058); correction/audit history stays in the SYS-046 audit log, not the omx/v1 contract (OQ-056).* |
| SYS-074 | UC-017 #2 + UC-025 #1 | T | TASK-010: `internal/web` `TestPublicResultsLocalizedDisciplineLabelsSYS074` (public results page renders discipline names from the DE/FR `discipline.<code>` catalog keys, not the catalog's canonical English name) *(PoC scope: discipline localization keys are shipped for the disciplines exercised by the built-in template and test fixtures, not the full catalog — see docs/requirements/open-questions-and-assumptions.md; category codes, e.g. "U16 W"/"M7", are treated as locale-invariant identifiers throughout the app, consistent with the existing office standings/roster pages)* |
| SYS-075 *(L)* | UC-030 | T | Phase B (Later) |
| SYS-076 | UC-017 #5 | T | TASK-010: `internal/store` `TestMeetResultsPositioningSYS076` (default federation_official on create, organizer override to primary with source name/URL round-tripping through `UpdateMeet`, invalid positioning rejected by both `CreateMeet` and `UpdateMeet`); `internal/web` `TestPublicResultsSYS076Label` (federation_official carries the "unofficial results" label with the source name/link in DE and FR; primary carries no label; organizer-configurable via the meet edit form, `internal/web/meets.templ`) |
| SYS-077 | UC-035 #1–#3 | T | TASK-012 (series upload export, MVP scope only — Visana Sprint/Mille Gruyère remain *Later*): `internal/domain` `TestBuiltinUKCSeriesUploadTemplate` (shipped template shape: sheet name, identity columns cover athlete-identity fields, missing-mark placeholder), `TestParseSeriesUploadTemplateRejectsBadData`, `TestSeriesUploadTemplateIsData` (UC-035 #3 data-swap at the parser level); `internal/app` `TestSeriesUploadExportSYS077UC035_1` (exported workbook's sheet/header/column structure and per-participant marks+points+division data against the built-in template fixture), `TestSeriesUploadExportSYS077UC035_2` (missing discipline and DNS status render explicitly — never a dropped row — per the template's placeholder/status convention), `TestSeriesUploadExportSYS077UC035_3` (a revised template document changes the exported sheet/columns with no code change), `TestSeriesUploadExportNotConfigured`; `internal/web` `TestSeriesUploadDownloadSYS077UC035_1` (office-gated download route serves a real XLSX with the standings-page link). *Provenance caveat (OQ-023): the shipped template's exact column layout is an assumption — the official "Vorlage Resultatimport" file itself could not be retrieved offline; see docs/requirements/open-questions-and-assumptions.md.* |
| SYS-078 *(Later)* | UC-036 #1–#2 | T/D | Phase B |
| SYS-080 *(Later — DEC-013)* | UC-019 #1, #3 | T | Phase B (venue-node gate) |
| SYS-081 | UC-020 #1–#2 | T | TASK-003 (storage layer): `internal/store` `TestKill9Durability` (kill-9 mid-burst, confirmed-write survival, consistency-gated reopen), `TestOpenDurabilityPragmas` (WAL + synchronous=FULL); TASK-014 (app-level drill, UC-020 #2's "burst of result saves" specifically): `internal/app` `TestCaptureCrashRecoverySYS081UC020_2` (kill -9 mid-burst of real `ResultsService.SaveResult` capture writes against a UKC meet with registered participants; every confirmed (athlete, mark) pair — the last one per athlete, since capture legitimately corrects — present after reopen; result.save audit rows ≥ confirmed writes observed) |
| SYS-082 *(Later — DEC-013)* | UC-019 #2 | T | Phase B (venue-node gate) |
| SYS-083 | UC-021 #1–#3 | T | TASK-003 (primitive): `internal/store` `TestOptimisticUpdateConflictSurfaced` (conflict surfaced, stale write rejected); TASK-008 (capture path, UC-021 #2): `internal/store` `TestSaveAttemptConflicts`, `internal/app` `TestCaptureConflictSurfaced` (both versions surfaced, never silent last-write-wins). TASK-025 (the full ≥10-operator proof, driven through `ResultsService` — the real service layer over the single-writer SQLite pool every production session shares, `store.Open`'s `SetMaxOpenConns(1)`, ADR-004 §2 — never raw SQL): `internal/store` `TestSaveResultOptimisticConflictSurfaced` (the settled-`Result`-entity analogue of `TestSaveAttemptConflicts`: two sessions that read the same version race, one wins, the other's stale expectation surfaces `ErrVersionConflict` carrying the row actually stored — closes the gap `SaveResult`'s blind upsert deliberately leaves open for ordinary re-capture), `TestSaveResultOptimisticConcurrentFirstSaveConflicts` (two sessions racing to capture a result NEITHER has seen yet also conflict, via the `(unit,athlete)` UNIQUE index, never silently coalescing); `internal/app` `TestUC021_1_TenConcurrentSessionsDifferentUnits` (UC-021 #1: 10 goroutine-simulated sessions writing `SaveTrackResult` to 10 distinct units of one meet concurrently — all 10 persist, none lost or misattributed to the wrong unit/athlete), `TestUC021_2_ConcurrentEditsSameResultConflictSurfaced` (UC-021 #2 at the service layer: two sessions that both loaded the same result's version race a correction via the new `TrackResultInput.ExpectedVersion`/`*ResultConflictError` path — exactly one succeeds, the other gets both versions surfaced, `errors.Is(err, ErrConflict)`), `TestUC021_3_InfieldCaptureAndOfficeCorrectionConverge` (UC-021 #3: infield capture on one still-open heat and an office `CorrectResult` on another, already-announced heat of the SAME event race concurrently — both persist, and each result's final stored state matches its own last `result.correct` audit entry, standings never drifting from the audit-trail order) |
| SYS-084 | UC-020 #3 | T | TASK-014 (one-action backup, SQLite `VACUUM INTO` — never a raw copy of the live WAL database): `internal/store` `TestBackupProducesConsistentSnapshotSYS084` (self-contained artifact, embedded manifest with meet metadata, independently recomputed checksum matches), `TestBackupChecksumDetectsDivergenceSYS084`, `TestBackupArtifactOpensAsFreshStoreSYS084` (artifact IS a valid `bahnfrei.db` — restore is placing it at a fresh install's data dir); `internal/app` `TestBackupServiceAuthorizationSYS084` (instance-admin gate), `TestBackupServiceProducesAuditedArtifactSYS084` (SYS-046 audit); `internal/web` `TestAdminPageOffersBackupDownloadLinkSYS084`, `TestBackupDownloadRequiresInstanceAdminSYS084`, `TestBackupDownloadServesConsistentArtifactSYS084` (office-UI one-click download); `cmd/bahnfrei` `TestCLIBackupRestoreRoundTrip` (`bahnfrei backup`/`restore` subcommands; restore refuses a non-fresh data dir), `TestBackupRestoreFreshInstallSYS084UC020_3` (full HTTP restore-to-fresh-install E2E: backup taken from a running instance, restored via the CLI onto a brand-new data directory, record counts and an independently recomputed checksum equal, the same admin account still authenticates, standings and public results pages render the restored data — ≤15 min budget asserted) |
| SYS-085 | UC-034 #1–#3, #6 | T | TASK-009 (offline capture queue): `internal/app` `TestUC034_2_IdempotentReplay` (exactly-once, ordered replay under repeated batches), `TestUC034_2_OpIDReuseAcrossUnitsFails` (cross-unit op-id reuse fails loudly, never a false duplicate); `internal/store` `TestCaptureOpDedupeLedger`; `internal/web` `TestUC034_2_IdempotentReplayOverJSON`; Playwright `e2e/tests/uc-034.spec.ts` "UC-034 #1" (capture continues offline with indicator, network cut mid-event), "#2" (auto in-order sync; flaky-reconnect no-duplicates incl. dropped-ack replay), "#3" (offline reload/browser-restart survival via service worker + IndexedDB queue), "#6" (transport-agnostic by construction: same-origin relative URLs, no transport-specific paths); TASK-015 chaos run `e2e/tests/chaos-m1.spec.ts` "M1 connectivity chaos" (two field officials on different units under repeated randomized flapping, zero lost/duplicated captures under batched flaky-ack retries — found and fixed a client-side flush() reentrancy race, see islands/src/capture-offline.ts) |
| SYS-086 | UC-034 #4–#5 | T | TASK-009 (checkout + reconciliation): `internal/domain` `TestDecideSync_Routing`; `internal/store` `TestCheckoutLifecycle`, `TestReconciliationItemsRoundTrip`; `internal/app` `TestUC034_4_StartListChangeRoutesToReconciliation`, `TestUC034_5_StaleCheckoutNeverSilentlyApplied` (override bumps generation, audited per SYS-046), `TestUC034_5_WrongTokenNeverApplies` (replays accepted only from the current holder), `TestUC034_CheckoutByAnotherRequiresOverride`, `TestUC034_ReconciliationResolveApplyAndDiscard` (discard explicit + audited — anti-Web.TEC-C2.1); `internal/web` `TestUC034_5_StaleCheckoutReconcilesOverJSON`, `TestUC034_ReconciliationApplyOverJSON`; Playwright "UC-034 #4" (office start-list revision → reconciliation, nothing silently discarded), "#5" (office override → capture resumes on second device; stale device's ops land in reconciliation); TASK-015 chaos run `e2e/tests/chaos-m1.spec.ts` "M1 connectivity chaos" (start-list revision lands mid-storm while two devices flap; reconciliation queue holds exactly the expected conflicted items, one per op, none merged/discarded even under a flaky-ack batch retry) |
| SYS-087 | UC-034 #7 | T | TASK-009 (blip tolerance): `internal/web` `TestLayoutWiresOfficeBanner` (degraded-state banner on authenticated operator surfaces); Playwright "UC-034 #7" (≤5-min office blip: banner shown, typed input survives, auto-resume on reconnect, no re-login — session TTL asserted > blip window); TASK-015 chaos run `e2e/tests/chaos-m1.spec.ts` "M1 connectivity chaos" (office blips through roster start-list work while concurrently revising a checked-out unit's start list mid-storm) |
| SYS-090 | UC-022 #1, #3 | T | TASK-005 (RBAC primitive): `internal/app` `TestRoleAtLeast`, `TestAuthorize` (least-privilege capability checks), `TestParseRole`/`TestParseRoleInvalid`; `internal/web` `TestAdminRouteRequiresInstanceAdminRole` (anonymous 403, instance-admin 200 end to end). TASK-013 (per-meet grants & event scoping, PoC scope): `internal/app` `TestPermissionMatrixSweepSYS090UC022_3` (UC-022 #3: role × capability sweep against the documented matrix), `TestFieldOfficialEventScopeDeniedSYS090UC022_1` (UC-022 #1 denial: unassigned unit refused on capture/checkout/read, each denial audited), `TestFieldOfficialEventScopeAllowedSYS090UC022_1` (allow path), `TestOfficeAndAboveNotScopedByEventSYS090` (scoping applies to field officials only), `TestFieldOfficialAssignmentRequiresFieldOfficialRoleSYS090` (grant/revoke lifecycle, CapAssignUnits floor, non-field-official grant refused), `TestAccountChangeRoleAndDisableRefuseLastAdminSYS090` (last-enabled-admin lockout guard); `internal/web` `TestFieldOfficialEventScopeSYS090UC022_2` (end-to-end over HTTP: provision → assign → allowed unit 200/303, unassigned unit 403 on read and write), `TestAccountAdminAndAuditRouteGatingSYS090UC022` (anonymous and under-privileged 403 on /admin, /audit, officials matrix). TASK-042 ("my assignments" dashboard, DEC-025 — closes OQ-089): the logged-in home page (`internal/web/pages.templ` `assignmentsDashboardPage`) renders a role-specific panel for every session below the meet-organizer floor, each re-authorizing and scoping strictly to the actor's own account (`internal/app` `ResultsService.AssignedMeets`/`SubmitterMeets`, `MeetService.OfficeMeets`) — `internal/app` `TestAssignedMeetsSYS090DEC025TASK042` (a field official's panel shows exactly its scoped meet/unit, never an unassigned unit of that meet nor an unrelated meet — the UC-022 #1 denial pattern applied to a read surface), `TestAssignedMeetsNonFieldOfficialSYS090DEC025TASK042`/`TestAssignedMeetsRequiresCaptureCapabilitySYS090DEC025TASK042`, `TestOfficeMeetsSYS090DEC025TASK042`/`TestOfficeMeetsRequiresOfficeCapabilitySYS090DEC025TASK042` (competition office holds no per-meet scoping — every meet in the instance comes back, see OQ-110), `TestSubmitterMeetsSYS090DEC025TASK042`/`TestSubmitterMeetsRequiresSubmitCapabilitySYS090DEC025TASK042` (an entry submitter never sees another submitter's entries at the same meet); `internal/web` `TestAssignmentsDashboardOfficePanelSYS090DEC025`, `TestAssignmentsDashboardFieldPanelScopingSYS090UC022DEC025` (authz-critical: no leaked unit or meet over real HTTP), `TestAssignmentsDashboardSubmitterPanelSYS090DEC025`, `TestAssignmentsDashboardEmptyStatesSYS090DEC025` (honest "nothing yet" copy per role). TASK-043 (OQ-111, closes the office 403 on its own meet-detail hub that TASK-042's dashboard newly made reachable): `GET /meets/{id}` (`handleMeetDetail`, `internal/web/routes.go`) is reopened from organizer-only to the competition-office floor, and `meetDetailPage`/`meetDetailView` (`internal/web/meets.templ`/`meets.go`) filter the rendered action list on `p.CanOrganize` so organizer-only actions (edit, archive, publish, sanctioning, bib/fee/exception management, programme/timetable mutation) stay hidden for an office session while their POST/GET routes remain organizer-gated, unweakened — `internal/web` `TestMeetDetailHubOfficeAccessTASK043` (office session gets 200 with organizer-only actions absent from the rendered HTML, a link-walk asserting every href the office hub renders resolves non-403, field-official/entry-submitter sessions unchanged at 403, organizer view unchanged with every action present and resolving), `TestRosterBackToMeetOfficeSessionTASK043` (the TASK-042/OQ-111 repro: an office session follows the roster page's `roster.back_to_meet` link and lands on the hub instead of a 403). |
| SYS-091 | UC-022 #2, #4 | T/I | TASK-005: `internal/app` `TestHashAndVerifyPassword`, `TestNeedsRehash`, `TestLoginUpgradesWeakHash` (adaptive argon2id hash + upgrade-on-verify), `TestSessionExpiry`, `TestSessionSweep` (configurable session TTL), `TestCreateAccountRequiresCapability` (privileged action recorded to audit trail, SYS-046); `internal/web` `TestLoginFlowSuccessAndFailure`, `TestLoginRejectsWithoutCSRFToken`, `TestAdminRouteRequiresInstanceAdminRole` (authN required for non-public capability). TASK-013 (account lifecycle & privileged-action surfacing, PoC scope): `internal/app` `TestAccountLifecycleCreateDisableEnableRoleChangeSYS090UC022_2` (UC-022 #2: create/disable/enable/role-change each audited with actor+action+target+timestamp; disable revokes live sessions immediately and blocks login), `TestPrivilegedAuditLogSurfacesPrivilegedActionsOnlySYS091UC022_2` (office/organizer-visible audit view over the SYS-046 append-only log, CapViewAudit floor, routine capture events excluded); `internal/web` `TestFieldOfficialEventScopeSYS090UC022_2` (denied scoped attempt itself appears in /audit with the actor's name, UC-022 #1/#2). TASK-043 (OQ-111): the meet-detail hub's authenticated-session floor (`requireRole`) moves from meet-organizer to competition-office — still authenticated, still session-checked (SYS-091's "required for all non-public capabilities" unchanged); the hub is read-only for an office session (no privileged mutation added, so no new SYS-046 audit row), see the SYS-090 row for the tests. |
| SYS-092 | CI dependency/static scans + ASVS L2 audit checklist per release | A/I/T | TASK-026 (ASVS L2 pass): review `docs/delivery/reviews/asvs-l2-task-026.md` (v4.0.3 L2, overall **PASS**, STRIDE threat model, dependency posture — `govulncheck` clean 2026-07-14). Hardening fixes each test-pinned in `internal/web`: `TestLoginBruteForceThrottledSYS092Web`/`TestLoginSuccessResetsThrottleSYS092Web`/`TestLoginRateLimiterUnit` (FIX-1 auth anti-automation, ASVS V2.2.1), `TestOversizedImportUploadRejectedSYS092Web`/`TestOversizedTimingUploadRejectedSYS092Web` (FIX-2 request-body cap, V12.1.1/V13.1.3), `TestHSTSHeaderOnlyInACMEModeSYS092Web` (FIX-3 HSTS ACME-only, V9.2/V14.4.5), `TestAuthenticatedResponsesAreNoStoreSYS092Web` (FIX-4 no-store on authenticated responses, V8.2.1). CI gates: `govulncheck` (`vuln-scan`) + `go-licenses` (`licence-scan`) + `golangci-lint`. Open: F-1/OQ-078 (add gosec SAST), F-2/OQ-079 (locale-switch Referer redirect), F-3/OQ-063 (generic-CSV 404). |
| SYS-093 | UC-019 #4 + TLS config inspection | T/I | TASK-005: `internal/web` `TestGenerateSelfSignedCovers`, `TestLoadOrGenerateSelfSignedPersistsAndReuses`, `TestLoadOrGenerateSelfSignedRegeneratesWhenNearExpiry`, `TestLocalTLSConfigDefaultsToLocalhost` (venue-local self-signed cert, no internet-dependent CA), `TestBuildACMEConfigWiresIssuer`, `TestAcmeTLSConfigRequiresDomain` (hub/ACME config wiring; real ACME issuance via `ManageSync` is a deliberate offline-suite seam, not unit-tested), `TestServerServeAndShutdown` (TLS listener lifecycle end to end). TASK-026: `TestHSTSHeaderOnlyInACMEModeSYS092Web` (HSTS asserted present in ACME mode, withheld in venue-local self-signed mode so it never forbids the operator's cert-warning click-through). |
| SYS-100 | UC-023 #1 | T | TASK-023 (public data minimization, central choke point): `internal/domain` `TestPublicDisplayNameForSYS103UC023_2`, `TestPublicDisplayClubForSYS103UC023_2` (the minimization functions `PublicDisplayNameFor`/`PublicDisplayClubFor` every public renderer calls); `internal/web` `TestPublicPageMinimizationSYS100UC023_1` (automated PII scanner over `/m/{id}` overview/start lists/results/results-live — the fixture seeds online entries through check-in and generates heats, so the TASK-018 heat/lane sections are swept too: birth YEAR present, no full ISO date beyond the meet's own start/end dates, no licence-shaped token) *(neither the roster form nor the TASK-016 online-entry forms collect a full birth date, and licence numbers arriving via TASK-017 import live only in `ExternalIDs`, which no public view model carries — a compile-time guarantee, not just a render-time filter; the scanner is the regression guard for any future richer person-data field)*. TASK-029 (privacy-review finding #4, link-following crawler): `internal/web` `TestPublicPathCrawlerNeverLeaksWithdrawnOrErasedNameSYS100SYS101UC024` (walks the public meet surface by following `<a href>` links from the overview page, plus explicitly probes the never-plain-linked `results/live` fragment — a drift-resistant alternative to a hand-maintained page list; asserts neither a withdrawn nor an erased athlete's name appears on any reached page) |
| SYS-101 | UC-024 #1–#2 | T | TASK-023 (data-subject rights): `internal/domain` `TestAnonymizePersonalDataSYS101UC024_2` (erasure clears identity, keeps BirthYear/Sex/Nationality/ClubIDs for sporting-record integrity); `internal/store` `TestAnonymizeAthleteStoreSYS101UC024_2`, `TestListParticipationsAndResultsByAthleteSYS101UC024_1` (own-data-only export scoping, deny: no other athlete's rows); `internal/app` `TestExportAthleteDataSYS101UC024_1`, `TestEraseAthleteSYS101UC024_2` (allow/deny incl. field-official refusal, repeat-erasure refusal, audit row never embeds the pre-erasure name); `internal/web` `TestPrivacyExportOverHTTPSYS101UC024_1`, `TestPrivacyEraseOverHTTPSYS101UC024_2`, `TestPrivacyRoutesRequireOfficeOrAdminRoleSYS101SYS103UC023UC024`. TASK-029 (privacy-review findings #1/#4): `internal/store` `TestAthletePersonalDataEntityIDsSYS101UC024_2` (athlete-scoped, cross-meet audit-entity lookup; another athlete's rows in the same meet/unit are never included); `internal/app` `TestEraseAthleteRedactsAuditPIIInOlderAuditRowsSYS101UC024_2` (erasure also redacts the OLDER `participant.register`/`entry.submit` audit rows a prior action already wrote the name into, via `store.RedactAuditPII` — reused, not duplicated; the audit trail's actor/action/entity_type/entity_id/seq shape survives, UC-022 #2's invariant), `TestEraseAthleteUnmatchableOnReimportSYS101UC024_2` (pinning test: re-importing a CSV row with the same name/birth year/sex AND the erased athlete's former licence number creates a brand-new athlete, never resurfaces or re-links the erased record — verified by construction, both matching paths `resolveImportAthlete` tries are cleared by `AnonymizePersonalData`); `internal/web` `TestPublicPathCrawlerNeverLeaksWithdrawnOrErasedNameSYS100SYS101UC024` (see SYS-100) |
| SYS-102 | UC-024 #3 | T | TASK-023 (retention purge, clock-injected): `internal/store` `TestFindAthletesOutsideRetentionSYS102UC024_3`, `TestPurgeAthleteRetentionDataSYS102UC024_3`, `TestRedactAuditPIISYS102UC024_3` (the audit_log append-only trigger's one blessed, transactional exception — deny: a plain UPDATE after redaction is still refused, proving the trigger is restored), `TestFindExpiredMeetIDsAndMeetPersonalDataEntityIDsSYS102UC024_3`; `internal/app` `TestPurgeExpiredSYS102UC024_3` (`PrivacyService.WithClock`; deny: office role refused, allow: instance-admin; verification re-query finds nothing left), `TestPurgeExpiredAtStartupAuditsAsSystemSYS102`; `internal/web` `TestRetentionPurgeOverHTTPSYS102UC024_3`. Default retention: `app.DefaultRetentionDays` = 90 (UC-024 #3's own acceptance example; documented in code, see OQ-042). TASK-029 (privacy-review finding #2): `internal/store` `TestFindExpiredMeetIDsAndMeetPersonalDataEntityIDsSYS102UC024_3` (extended: an expired meet's entry row IDs are now part of the reported entity-ID set too); `internal/app` `TestPurgeExpiredRedactsEntryAuditRowsSYS102UC024_3` (entity_type "entry" — online-entry `entry.submit` audit rows, previously omitted from the sweep entirely — is now redacted like `participant`/`result`/`athlete`) |
| SYS-103 | UC-023 #2–#3 | T | TASK-023 (consent enforcement): `internal/domain` `TestPublicDisplayNameForSYS103UC023_2`, `TestPublicDisplayClubForSYS103UC023_2`, `TestIsMinorSYS103`; `internal/store` `TestAthleteConsentPersistsSYS103UC023_2` (allow: update under correct version; deny: stale version/unknown id); `internal/app` `TestSetConsentSYS103UC023_2_3` (deny: field official; allow: office withdraw/restore reflected on the next `Standings()` read — UC-023 #3; audit row never embeds the name); `internal/web` `TestPrivacyConsentAtEntryAndToggleOverHTTPSYS103UC023` (entry-flow consent checkbox, UC-023 #2 suppression, UC-023 #3 mid-meet toggle), `TestPublicResultsConsentSuppressionSYS103UC023_2` (allow+deny side by side in one division), `TestPublicHeatSheetConsentSuppressionSYS103UC023_2` (TASK-018 heat/lane sections: withdrawn athlete suppressed on the public start-list page while the office seeding page keeps the real name); `internal/app` `TestEntryFlowsCollectConsentSYS103UC023` (TASK-016 individual/bulk/relay entry flows collect the consent choice at submission, per person/per leg — never per batch), `TestImportPreservesConsentSYS103UC023` (TASK-017 CSV import: a matched existing athlete's withdrawal survives re-import untouched; an import-created athlete defaults to not-withdrawn with no fabricated consent interaction); `internal/web` `TestResultListPDFMinimizationSYS103UC023_2` (printed result list, a "publication-intended export" per SYS-100, minimized the same as public web pages — unlike the office-only standings page or the federation series-upload export, which UC-023 #2 explicitly keeps un-minimized) |
| SYS-104 | Document inspection per release | I | TASK-028: `docs/ops/privacy.md` — data-processing overview (what is stored where, lawful-basis notes), a template privacy notice for meets, controller guidance, and the pseudonymization-vs-anonymization framing for athlete erasure required by `docs/delivery/reviews/privacy-review-task-023.md` finding #3 |
| SYS-105 | Egress-blocked suite (UC-019 #3) + code/config inspection | T/I | TASK-027: `internal/web` `TestPublicSurfaceRendersWithEgressBlockedSYS105` (runs in the default `go test ./...` gate — fast and deterministic): the process-global `http.DefaultTransport` is replaced with a dial interceptor that fails the test on ANY non-loopback dial while the TASK-029 link-following crawler walks every public page plus the never-plain-linked `results/live` fragment; every `/static/` asset the shell references (htmx, base.css, public-live.js — all `go:embed`ded per ADR-003) fetches 200 under the block; a static scan of every crawled page's rendered HTML asserts zero absolute off-origin `href`/`src`/`action` resource references (the gap a Go-client-only check would miss — what a real browser would have fetched). Chosen over a Playwright route-block because it also proves the SERVER process performs no egress while rendering (e.g. a future server-side telemetry call), not merely that the DOM omits external URLs. I: repo-wide grep (2026-07-13): no cdn/googleapis/jsdelivr/unpkg/htmx.org reference in any template or static asset. *(Scope: proves the no-external-fetch half of SYS-105 for public surfaces; the "no telemetry containing personal data" half remains satisfied vacuously — no telemetry code exists — and re-verified by the same dial guard.)* |
| SYS-110 | UC-025 #1–#3 | T | TASK-005 (i18n mechanism): `internal/web/i18n` `TestLoadShipsCompleteDEAndFR` (DE/FR ship complete, zero missing-key fallback), `TestLoadDiscoversPseudoLocaleWithNoCodeChange` + `TestPseudoLocaleInSyncWithReference` (pseudo-locale build, translation-file-only extensibility, gated by plain `go test ./...` already in CI), `TestMissingKeyFallsBackThenBrackets`; `internal/web` `TestLocaleSwitchPersistsAcrossRequests`, `TestLocaleSwitchIgnoresUnknownLocale` (per-session switch). TASK-024 (i18n completion, UC-025 #3 date/number/mark-notation rendering + full DE/FR sweep): `internal/web` `TestFormatDateAndMarkNotationSYS110UC025_3` (day.month.year display convention via `PageData.FormatDate`/`FormatDateTime`, distinct from the ISO `<input type="date">` wire format; mark notation via `domain.FormatCentiMark` stays period-decimal in both locales — the documented convention, see `docs/requirements/open-questions-and-assumptions.md` OQ-024); `TestMeetCreationUC001_2`, `TestEventProgrammeUC001_3`, `TestSanctioningSummaryUC001_5Web` (updated to assert the new date format on the operator meet/programme/sanctioning pages); `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` (same, end to end). Full DE/FR catalog sweep (`internal/web/i18n/locales/{de,fr}.json`, 258 keys, includes the `nav.aria_label`/`footer.locale_apply` additions below) leaves zero hardcoded operator/public strings found in the `internal/web` `.templ` sources. |
| SYS-111 | UC-025 #1 + UC-018 #3 | T | TASK-024 (domain-vocabulary glossary completion): `internal/web/i18n` `TestLoadShipsCompleteDEAndFR` now covers all 33 `discipline.<code>` keys in `internal/domain/data/disciplines/catalog.json` (previously 6 — see `docs/requirements/open-questions-and-assumptions.md` OQ-022, resolved) in both `de.json`/`fr.json`; the public timetable page (`GET /m/{id}/timetable`) was switched from the catalog's canonical English name to the same `localizedDisciplineName` helper `TestPublicResultsLocalizedDisciplineLabelsSYS074` (TASK-010) already covers for the results page — a real localization gap on a public surface, not just missing keys. Official-document headings: `internal/web/printables.go`'s capture-sheet/result-list PDFs already reuse `localizedDisciplineName` (TASK-011), so the wider catalog benefits them too, unchanged code. Operator-only Programme/Units tables and the capture/reconciliation surfaces intentionally keep the canonical catalog name as a stable, locale-invariant identifier (same treatment as category codes) — unchanged by this task. |
| SYS-112 | UC-026 #1–#2 | T/I | TASK-024: `e2e/tests/a11y-public-SYS113.spec.ts` (axe-core `@axe-core/playwright`, WCAG 2.2 AA rule tags `wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`/`wcag22aa`) — one scan per public page type (`UC-026 #1: meet overview...`, `...public timetable...`, `...public start lists...`, `...public results...(populated with a captured result)...`) against a real seeded meet with a published timetable and a captured result, wired into `cd e2e && npx playwright test` (CI's existing `e2e` job). Fixes landed to reach zero violations: keyboard access to `.table-scroll` overflow regions (`tabindex="0"`, `internal/web/{public,meets,accounts,auditlog,officials}.templ`), localized `nav aria-label` (was the hardcoded English word `"primary"`), explicit `<th scope="col">` on every table header cell. UC-026 #2 (screen-reader walk-through, per release) is process documentation, not a one-shot automated check: `docs/requirements/accessibility-manual-audit-checklist.md` (I). |
| SYS-113 | UC-017 #2, UC-026 #1 | T | TASK-010: `internal/web` `TestPublicPagesAnonymousAccessSYS070UC017_2` (viewport meta tag present on every public page). TASK-024: `e2e/tests/a11y-public-SYS113.spec.ts` `SYS-113: public pages are usable at 360px width...` (real 360×740 viewport against all four public page types: `document.documentElement.scrollWidth` never exceeds `clientWidth` — no page-level horizontal scroll, wide tables scroll within their own `.table-scroll` box per the existing CSS contract — plus the same WCAG 2.2 AA scan at that viewport size, since AA already requires sufficient contrast/no-hover-only-interaction and this pins it at the width SYS-113 names). |
| SYS-114 | UC-007 #1–#2, UC-010 #1–#3 | T | TASK-030: `e2e/tests/keyboard-operator-SYS114.spec.ts` "SYS-114 UC-007: check-in is keyboard-only, incl. bulk DNS-close and reinstate" (real Tab/Shift-Tab traversal via `helpers/keyboard.ts`'s `tabUntilFocused` — never `.focus()` or `.click()` — from page load through nav/skip-link to a row's confirm button; Enter confirms one entry; the bulk "close check-in" action DNSes the other two in a single keystroke, UC-007 #1; Space reinstates a DNS entry via keyboard, UC-007 #2); "SYS-114 UC-010: track result entry is keyboard-only (times, statuses, corrections)" (keyboard-typed manual time with D5.1 round-up rendered; DNF/DQ statuses set purely via ArrowDown/ArrowUp on the native `<select>`, never `.selectOption()` — the UC-010 #3 pairing; a DQ's required rule reference typed into `status_detail`; a reasoned correction after keyboard-announcing the unit, exercising `capture.templ`'s `/correct` form switch). `helpers/keyboard.ts`'s `assertTabAdvancesFocus` pins the no-trap requirement (SC 2.1.2) directly. Fix landed: `internal/web/seeding.templ`'s check-in table (`checkInPage`) was missing the `tabindex="0"` the SYS-112 fix already gives every other operator/public scrollable table — added for consistency, so the check-in roster's own overflow region is keyboard-focusable/scrollable like the rest of the design system. TASK-041 (DEC-025, closes OQ-070): the second real bulk operation — "mark remaining as DNS" on a still-open track unit (`app.ResultsService.BulkMarkRemainingDNS`/`BulkDNSCandidateCount`), scoped to UC-010 track capture (`capture.templ`'s `trackForm`) since horizontal/vertical field units have no equivalent per-athlete-status meaning. `internal/app` `TestBulkMarkRemainingDNSOnlyUnresultedMarkedSYS114SYS046` (only unresulted entries marked, an already-captured result untouched, a second call is a no-op, per-entry `capture.bulk_dns` audit rows — SYS-046), `TestBulkMarkRemainingDNSRejectsAnnouncedUnitSYS114` (announced unit routes to the correction flow, not the bulk action), `TestBulkMarkRemainingDNSRejectsNonTrackUnitSYS114` (field units rejected — `ErrBulkDNSTrackOnly`), `TestBulkMarkRemainingDNSRequiresOfficeCapabilitySYS114` (field-official denial); `internal/web` `TestCaptureBulkDNSConfirmFlowSYS114SYS046Web` (TASK-034-style GET confirm sub-page previews the exact candidate count and posts to `/bulk-dns`; visiting/cancelling applies nothing; confirming marks the unresulted entrant DNS and leaves the captured one untouched; the action link disappears once nothing is left to mark); e2e `keyboard-operator-SYS114.spec.ts` "keyboard bulk 'mark remaining as DNS' through the confirm page" (Tab-reaches the action link, Enter navigates to the confirm sub-page, Tab-reaches and Enters its own submit button — no `.click()` anywhere, `assertTabAdvancesFocus` re-asserted on the confirm page itself). TASK-043 (OQ-111): the meet-detail hub's capability filter (`p.CanOrganize`) only wraps existing `<a>`/`<form>` elements in Go `if` blocks — no new widget shape — so the plain-anchor/native-button keyboard reachability this suite already covers for the hub's organizer view (and the office-permitted surfaces it links to: check-in, seeding, timing, entries, privacy, all covered by their own suites) carries over unchanged for the office-filtered render; no new e2e case needed, verified instead by `TestMeetDetailHubOfficeAccessTASK043`'s link-walk (`internal/web/meets_test.go`). |
| SYS-115 | UC-037 #1–#5 | T/I | TASK-031 (contextual input help): machine-readable help-content registry `internal/web/help.go` (`helpRegistry`, 20 entries over 8 screen states; reviewable per SYS-115) + reusable component `internal/web/help.templ` (+ `/static/help.js` behavior island — CSP forbids inline scripts) documented in `design-system.md` §2/§5. Tests: `internal/web/help_test.go` `TestHelpRegistryCoverageSYS115UC037_1` (UC-037 #1 — mechanical, bidirectional registry↔rendered-screen walk: every registered input shows its icon adjacent — nearest-preceding-control pairing — and every rendered icon is registered), `TestHelpTextsLocalizedDEandFRSYS110UC037_2` (DE/FR catalog completeness for all registry keys + rendered-locale assertion), `TestHelpIconATAssociationUC037_4` (focusable trigger, accessible name, `aria-describedby` → `role="tooltip"` popup), `TestHardConstraintHintsVisibleSYS117UC037_5` (representative always-visible `.hint` fixtures, DE+FR); `e2e/tests/contextual-help-UC037.spec.ts` (UC-037 #2 hover/focus/click same localized text asserted in DE and FR; #3 SC 1.4.13 hoverable/persistent/Escape-without-focus-move; #4 axe WCAG 2.2 AA scan of the help-open state — zero violations); trigger focus ring covered by the existing gallery keyboard walk (`design-gallery.spec.ts`). I: registry review per release alongside the SYS-117 audit. |
| SYS-116 | UC-038 #1–#2 | T/I | TASK-032 (design tokens + component inventory): `docs/architecture/design-system.md` (token catalog §1, component inventory §2); `scripts/check-style-tokens.sh` (CI style-conformance gate, UC-038 #1 — mechanical, documented allowlist); `internal/web` `TestDesignGalleryRendersEveryInventoriedComponentSYS116UC038_2`, `TestDesignGalleryNotLinkedFromShellNavSYS116UC038_2` (dev/fixture gallery page renders one instance of every inventoried component/state, UC-038 #2); `e2e/tests/design-gallery.spec.ts` (real-browser keyboard walk asserts a visible `:focus-visible` outline on every interactive gallery element; a second scenario asserts the three offline-status colors are distinct and the invalid-input/field-error pairing renders and is `aria-describedby`-associated) |
| SYS-117 | UC-038 #3–#4, UC-037 #5 | T/I | TASK-032: `docs/requirements/usability-audit-checklist.md` (NN/g heuristics + SYS-117 form conventions, run process); `docs/delivery/usability-audit-2026-07.md` (first dated run against the real server — 2 Critical findings recorded as OQ-074/OQ-075, both **closed in TASK-034**; 1 Major as OQ-076 substantially closed in TASK-031, 1 Minor as OQ-077 still open). **UC-038 #4 (TASK-034, closed 2026-07-14):** the reusable `FieldErrors` mechanism (`internal/web/fielderrors.go`/`.templ`: a per-field error map flowing from handler validation into the `.field-error`/`aria-invalid` convention design-system.md §2 documents) is wired into the three representative forms UC-038 #4 names — meet setup (`internal/web/meets.go` `parseMeetForm`/`meets.templ`), online entry (`entries.go` `handleEntryIndividualSubmit`/`entries.templ`), result correction (`capture.go` `handleCaptureCorrect`/`capture.templ`, row-scoped via `rowFieldKey`) — each retaining permanently visible labels/hints and preserving submitted input on error. Tests: `internal/web` `TestMeetCreateFieldErrorsOQ075UC038_4` (the audit's own end-before-start repro), `TestOnlineEntryIndividualFieldErrorsOQ075UC038_4`, the strengthened no-reason case in `TestCaptureAnnounceAndCorrectionFlowSYS046SYS047UC015Web`; `e2e/tests/inline-field-errors-UC038.spec.ts` (real-browser: a submitted validation error renders inline at the field with input preserved). TASK-031 (UC-037 #5): the field-level `.hint` convention is now adopted for hard format/unit/bound constraints across the meet, programme, entries, roster, fees, seeding, wind and vertical-heights forms (each hint `aria-describedby`-associated with its input); verified by `internal/web/help_test.go` `TestHardConstraintHintsVisibleSYS117UC037_5` (representative fixtures, DE+FR — full sweep stays with the per-release audit; residual adoption gap tracked in OQ-076) |
| SYS-120 | Benchmark on reference dataset (1,500 athletes / 4,000 entries / 250 units) | A | TASK-027 (`perf` build tag; run via `scripts/run-perf-tests.sh`): `internal/app` `TestSYS120ReferenceScaleOperatorBudgets` — seeds the full reference corpus (`internal/apptest` `SeedLargeMeet`/`SYS120Scale`) and machine-checks p95 for meet-detail list load, participant-roster load (unfiltered-list load), a real filtered entry search, and result save (`SaveTrackResult`, the real capture path) against the 2 s budget (×4 CI-headroom ceiling; literal spec budget logged). Measured 2026-07-13 (16-core dev box, exceeds the 4-core reference — see the report): all p95 < 15 ms, budget met with >100× headroom. **TASK-038 (DEC-021/OQ-067, 2026-08-05):** a server-side `q` query-param filter (case-insensitive name/bib/club substring match, `app.MatchesParticipantSearch`) ships on the roster and bib-assignment pages, retiring the "no dedicated search endpoint" proxy note. New sub-test `TestSYS120ReferenceScaleOperatorBudgets/ParticipantSearch` benchmarks the real filter pipeline (`Participants`+`ClubNamesFor`+filter) narrowed to one of the reference corpus's 24 synthetic clubs (~62/1,500 athletes) — measured p95 **44 ms**, ~46× inside budget — alongside (not replacing) `ParticipantRosterSearch`'s unfiltered-list-load budget. Handler-level coverage: `internal/web` `TestRosterSearchFilterDEC021SYS120SYS114`/`TestBibsSearchFilterDEC021SYS120SYS114` (match/no-match/case-insensitivity/unfiltered-default/authz-unchanged); predicate unit tests: `internal/app` `TestMatchesParticipantSearchDEC021`. Results: `docs/delivery/perf-and-recovery-task-027.md` |
| SYS-121 | Benchmark (seeding 200 entries ≤10 s; recompute ≤5 s) | A | TASK-027 (`perf` tag): `internal/app` `TestSYS121SeedingGenerationBudget` (a literal 200-entry 100m event through the real entry/check-in path, `GenerateHeats` with lanes — measured 46 ms vs 10 s budget) and `TestSYS120ReferenceScaleOperatorBudgets/StandingsRecomputeAfterCorrection` (a `CorrectResult` on the reference-scale corpus, then `Standings` recompute — measured p95 77 ms vs 5 s budget). Results: `docs/delivery/perf-and-recovery-task-027.md` |
| SYS-122 | Load test (UC-017 #4) | A | TASK-027/TASK-035 (`perf` tag): `internal/web` `TestSYS122TwoThousandConcurrentViewers` — 2,000 goroutine viewers (100 also holding open SSE connections) against the public results page backed by the SYS-120 reference-scale corpus, asserting the literal ≤3 s p95 / ≤0.1% error budgets. **PASSES**: TASK-027 first measured this as failing by design (`store.Open`'s single connection serialized every read, and `Standings` recomputed the whole meet uncached per request — see OQ-066 for that evidence); TASK-035 (DEC-015, ADR-004 §9) added a pooled WAL read-connection set (`store.Store.ReadDB()`) plus a per-meet render cache for the public results page/fragment, invalidated by the results-changed bus event. Re-measured: p95 **343–377 ms** (two consecutive runs, ~8–9× inside budget), 0 errors, peak heap 179–184 MiB (flat across 100→2,000 viewers, vs. the old ~64 MiB/viewer linear growth). The SYS-071 live-update half also improved: SSE delivery p95 ~12 ms vs the 10 s budget (100/100 subscribers, 0 errors). Results and the companion `TestSYS122ViewerScalingProfile` (100/250/500 viewers) numbers: `docs/delivery/perf-and-recovery-task-027.md` |
| SYS-130 | UC-020 #1 (restart ≤2 min) | T | TASK-003: `internal/store` `TestKill9Durability` asserts reopen ≤2 min; TASK-014 extends the same budget assertion to the realistic capture path: `internal/app` `TestCaptureCrashRecoverySYS081UC020_2`. TASK-027 (`perf` tag) closes the remaining gap — both prior drills stop at "the database reopens"; SYS-130 says the SYSTEM must be operational: `internal/web` `TestServeRecoverySYS130` runs the real `web.Server` as a killable OS process, drives a burst of result saves over real HTTP (setup → login → UKC meet → capture form posts), SIGKILLs it mid-burst, restarts a fresh server on the SAME database, and asserts `/healthz` answers within the 2-minute budget (measured: ~23 ms), login works, and every HTTP-acknowledged save is present with its exact confirmed mark — zero acknowledged-data loss at the HTTP layer. Results: `docs/delivery/perf-and-recovery-task-027.md` |
| SYS-131 | UC-001 #1 (timed quickstart run) | D | TASK-006: `cmd/bahnfrei` `TestQuickstartFreshInstallE2E` (automated fresh-install flow: empty data dir → setup → admin account → meet, no config file; README quickstart + Dockerfile). The human-paced ≤30-min demonstration remains for the M1 club demo (DEC-011). TASK-028: `docs/ops/quickstart.md` (the extended download-to-ready walkthrough, reusing `TestQuickstartFreshInstallE2E` as its own verification proxy — see the page's "How this is verified" section) plus `docs/ops/operator-runbook.md` for what follows meet setup. TASK-037 (DEC-017/019): `docs/ops/quickstart.md` §1 Option B updated to the resolved GHCR pull path plus a `cosign verify` step ahead of `docker run`, keeping the download-to-ready walkthrough accurate to how the container image now actually publishes |
| SYS-132 | Support-matrix CI (build/run on documented platforms & browsers) | T/I | TASK-028: I: `docs/ops/support-matrix.md` (documented hub/venue-local OS & hardware, browser engines — latest two major versions of Chromium/Gecko/WebKit). T: `scripts/build-release.sh` actually cross-compiles and (for the host-matching target) smoke-tests all five shipped platforms (`linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, `windows/amd64`) rather than only type-checking them — verified in this task by running the script end to end and checking the resulting binaries and `sha256sum -c checksums.txt`. The related SYS-122 2,000-concurrent-viewer target is verified and recorded as **met** separately (TASK-027/TASK-035, `internal/web` `TestSYS122TwoThousandConcurrentViewers`; measured capacity documented in `docs/ops/support-matrix.md` §3) — SYS-132 itself (platform/browser support) is independent of that row |
| SYS-133 | Dependency inventory inspection (no paid service required) | I | Phase B |
| SYS-140 | Coverage gates in CI (≥90% branch domain logic; ≥83% line overall — floor ratcheted from 80% at TASK-033) | A | TASK-002: `scripts/check-coverage.sh` in `ci.yml` coverage job. *Method note:* Go's cover tooling measures **statement** coverage; the ≥90%/≥80% gates are enforced on statement coverage as the agreed proxy for branch coverage. Generated code (`*_templ.go`, checked-in templ output) is excluded from the measurement (TASK-006): its authored source is the `.templ` file, whose display logic is exercised by `internal/web` rendering tests; the excluded statements are machine-emitted per-write io-error plumbing |
| SYS-141 | CI pipeline configuration inspection + gate behaviour test | I/T | TASK-002: `.github/workflows/ci.yml` — 3-OS build+test matrix, golangci-lint v2 (incl. depguard architecture rules), guarded `tsc` job, coverage gates, `go-licenses` allowlist (ADR-001 §4), `govulncheck`; SPDX+DCO in `governance.yml` (TASK-001) |
| SYS-142 | Reference fixture suites for every rule engine (UC-005 #5, UC-010 #5, UC-012 #4, UC-013 #1) | T | Per-engine fixture suites, all `internal/domain` unless noted — scoring tables: `TestUKCScoringOfficialFixtures` (TASK-007, official UBS Kids Cup table values), `TestCombinedScoringReferenceFixtureDecathlonSYS044UC013_1` + `TestCombinedScoringReferenceFixtureHeptathlonSYS044UC013_1` (TASK-021, WA combined-events tables); hand-time rounding: `TestRoundUpHandTime` + `internal/app` `TestUC010_2_HandTimeRoundUpAndProvenance` (TASK-008/019); wind legality: `TestWindAssisted`, `TestUC016_2_WindAssistedNeverFlags`, `TestAverageWindSYS044UC013_4` (TASK-021/022); countback: `TestVerticalCountbackFixturesSYS043UC012_4` (TASK-021); progression: `internal/app` `TestAdvanceRoundSYS029UC009_1` + `TestAdvanceRoundTimeTieSYS029UC009_2` + seeding/lane-draw fixtures `TestGenerateHeatsSYS026UC008_1`/`TestGenerateHeatsLaneDrawSYS027UC008_3` (TASK-018, D-reference rules); category assignment: `TestResolveDefaultCategory_SwissAthletics_UC002_1` (TASK-004); eligibility (UC-005 #5): `TestEvaluateEligibility_*` suite (TASK-017, see SYS-014 row) |
| SYS-143 | Release checklist + defect-tracker inspection per release | I | TASK-028: `docs/ops/defect-policy.md` — severity definitions (Critical/High/Medium/Low), the release gate ("no known unfixed Critical/High defect"), and the regression-test requirement for every fixed defect |
| SYS-144 | Schema/API docs versioned in repo; CI schema-diff check | I/T | TASK-025: I: `docs/schemas/omx-v1.md` (the omx/v1 versioning + deprecation policy) plus the published schema's own embedded `description` (`internal/exchange/schema/omx-v1.schema.json`). T: `internal/exchange` `TestOMXV1SchemaIsPublishedAndSelfValidating` (the schema `go:embed`s the exact file `ValidateOMXSchema` checks against — doc and runtime contract cannot drift), `TestDecodeOMXRejectsWrongSchemaVersion` (a `v1` reader refuses a non-`v1` document rather than guessing), `TestDecodeOMXToleratesAdditiveUnknownFields` (additive v1.x fields never break an older reader — the policy's "optional fields, no version bump" half). *Residual gap:* an automated CI schema-diff gate (flagging a schema-file edit as breaking vs. additive) is not implemented — noted in `docs/schemas/omx-v1.md` §"Versioning and deprecation policy" point 5, Phase B follow-up. |
| SYS-145 | ADR inventory inspection at each gate | I | Phase B |
| SYS-146 | Repository inspection before first release; release-workflow inspection/CI | I | TASK-001: `LICENSE` (AGPL-3.0-only), `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `GOVERNANCE.md`, CI SPDX+DCO gates (`.github/workflows/governance.yml`). TASK-028: the documented release process, `docs/ops/release-process.md` (SemVer versioning, `CHANGELOG.md`, artifact build via `scripts/build-release.sh`, checksum/signing stance — OQ-087, container publication — OQ-086, support window — OQ-088) plus the per-OS binaries and container image it produces, verified by actually building all five targets and the Dockerfile in this task. TASK-037 (DEC-017/018/019, closes OQ-086/087/088): `.github/workflows/release.yml` — tag-triggered (`vX.Y.Z`) publish workflow, rebuilding artifacts via `scripts/build-release.sh --skip-image`, pushing the container image to GHCR tagged `<version>`+`latest`, cosign-keyless-signing the image (by digest) and `checksums.txt`, and attaching every artifact to the GitHub release; verified by running `scripts/build-release.sh 0.0.0-dev --skip-image` locally in this task (real triggered-tag-push behavior can only be proven by an actual tag push, a human/CI action outside this task's scope) plus manual workflow review (`actionlint` unavailable in this environment). `docs/ops/release-process.md` §4/§5/§6 updated to the resolved GHCR/cosign/support-window state with verification commands |

| SYS-147 *(P)* | UC-039 #1–#3 | T | Planned — TASK-045 (M4). Proposed 2026-08-06 from the volunteer walkthrough (F2: capture table 589/980 px wide at 360 px viewport; 24 px touch targets; no `inputmode`); pending founder ratification |
| SYS-148 *(P)* | UC-039 #4–#5 | T | Planned — TASK-045 (M4). Walkthrough F3: `data-pending` has no visual rendering; result/points cells only update on reload |
| SYS-149 *(P)* | UC-040 #1–#5 | T | Planned — TASK-044 (M4). Walkthrough F1 (Critical): 4xx sync rejections retried forever as "Verbindung unterbrochen", queue head-of-line wedge, contradictory status line |
| SYS-150 *(P)* | UC-043 #1–#3 | T | Planned — TASK-049 (M4). Walkthrough F4: no participant identity correction path exists (only bib / out-of-competition mutations) |
| SYS-151 *(P)* | UC-041 #1–#3 | T/I | Planned — TASK-046 (M4). Walkthrough F5; ratification would also answer OQ-113 |
| SYS-152 *(P)* | UC-041 #4–#5 | T/I | Planned — TASK-047 (M4). Walkthrough F7/F8 |
| SYS-153 *(P)* | UC-042 #1–#3 | T | Planned — TASK-048 (M4). Walkthrough F9 |

**Coverage check:** all baselined SYS verified; every UC traces to ≥1 SYS (see UC index).
Zero orphans in either direction. SYS-147–153 are *(P)* proposed (2026-08-06 volunteer
walkthrough), planned against TASK-044…049 — not yet verified, pending founder ratification.

## 3. Baseline QA status (Phase A definition-of-done self-check)

| Check | Status |
|-------|--------|
| Every requirement uniquely identified, atomic, testable | ✅ STR-001…045, SYS-001…146 (blocks), UC-001…038 |
| Zero orphan requirements (both directions) | ✅ §1–§2 above |
| Vague founder language converted to measurable targets | ✅ "well tested/bug free/state of the art" → SYS-140…146, SYS-092, SYS-120…122; no adjectives used as requirements |
| External facts cited; named systems verified by exact spelling | ✅ Seltec (not "setlec") confirmed; Swiss Athletics stack verified; SVM (not "CSI"); see `../research/*.md` |
| Assumptions explicit, open questions consolidated | ✅ `open-questions-and-assumptions.md` (OQ-001…012, A-001…012, TBD-001…009) |
| Scope boundaries stated (MVP / Later / out-of-scope) | ✅ StRS §4 |
| Implementation-free (no stack/scaffolding choices) | ✅ CON-05; verification mechanisms named only where rule- or format-defined (e.g., FinishLynx file family is an external interface, not a stack choice) |
| IDs stable, deprecation-only policy stated | ✅ headers of each document |

Known baseline limitations (tracked, non-blocking): TBD-001…009 in
`open-questions-and-assumptions.md`; Later-priority SYS/UC intentionally not decomposed further
until MVP is ratified.
