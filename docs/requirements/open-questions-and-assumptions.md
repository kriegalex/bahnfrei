# Open Questions & Assumptions

**Document status:** DRAFT — updated continuously through Phase A; consolidated at the A6 QA pass.
Open questions are addressed to the **founder** unless another owner is named. Where an answer
is pending, work proceeds on the stated assumption (marked **A-###**); assumptions are
requirements-relevant and traced where used.

## 1. Open questions for the founder (OQ-###)

| ID | Question | Why it matters | Working assumption until answered |
|----|----------|----------------|-----------------------------------|
| OQ-001 | **Scope of "athletics tournaments":** track & field stadium meets only, or also road running, cross-country, trail, and indoor meets? | Determines domain model breadth (courses, transponder timing, mass fields) | A-001 |
| OQ-002 | **Primary initial user:** which meet scale is the MVP target — club/regional meets, or national-championship grade from day one? | Drives MVP cut, performance targets, and feature depth (e.g., call room, jury workflows) | A-002 |
| OQ-003 | **Relationship to Swiss Athletics:** is the goal (a) interoperate with their existing ecosystem, (b) be adoptable by them, or (c) merely learn from it? Is there any existing contact/mandate? | Changes integration requirements from "import/export compatible" to "certified integration" | A-003 |
| OQ-004 | **Open-source licence:** preference (e.g., AGPL-3.0 vs Apache-2.0/MIT) and stance on commercial hosting by third parties? | One-way door; affects contributions, commercial ecosystem, timing-vendor integrations | A-004 |
| OQ-005 | **Governance & funding:** solo-founder project, foundation/association (CH Verein), or company-backed? Any budget for infrastructure/hosting? | Affects sustainability requirements and hosting-model requirements | A-005 |
| OQ-006 | **Hosting model:** self-hosted per organizer, central SaaS instance, or both? Who operates the reference instance? | Drives architecture-relevant NFRs (multi-tenancy, offline sync) — must be answered before Phase B ADRs | A-006 |
| OQ-007 | **Para athletics:** in scope for MVP, later, or out of scope? | Classification/scoring adds real complexity; affects data model early | A-007 |
| OQ-008 | **Languages:** confirm DE/FR/IT/EN all required at launch, or DE/FR first? | i18n effort and translation workflow | A-008 |
| OQ-009 | **Name/brand** for the project? | Repo, docs, domain registration | A-009 |
| OQ-010 | **Existing data/relationships:** does the founder have access to real meet data, a pilot club/meet, or timing hardware for testing? | Validation strategy for Phase B acceptance tests | A-010 |
| OQ-011 | **Alabus entry-data format:** can the founder (or a Swiss Athletics / club contact) obtain a sample Alabus entries export and its format spec? The Alabus marketing site blocks automated access; the TAF3↔Alabus flow is documented only from the Seltec side ([research](../research/competitive-analysis.md#c2-swiss-athletics-as-a-system-of-systems)) | "Interoperate with Swiss Athletics" concretely means importing what Alabus exports; without a sample, that requirement stays format-TBD | A-011 |
| OQ-012 | **Data-standard alignment:** should the project align its exchange schema with the W3C Open Athletics Community Group draft Competition Data Model (and possibly contribute upstream)? No federation mandates it today | Shapes the export/API surface; reversible but strategically visible | A-012 |

## 2. Working assumptions (A-###)

> **Status 2026-07-05:** founder answers received (§4). Confirmed: A-002, A-003, A-005,
> A-010, A-011, A-012. Superseded/updated by decisions: A-001 → DEC-001 (road/XC now **out of
> scope**, indoor stadium stays in), A-004 → DEC-004 (AGPL-3.0 lead candidate), A-006 →
> DEC-006 (homelab-first; desktop-app option to be evaluated in Phase B0), A-007 → DEC-007
> (para **out of MVP**, not merely reduced), A-008 → DEC-008 (DE/FR complete incl. operator
> surfaces; IT/EN later), A-009 → DEC-009 (shortlist in §6).

| ID | Assumption | Basis |
|----|------------|-------|
| A-001 | MVP scope is **outdoor & indoor stadium track & field** (incl. combined events and relays); road/XC/trail are **later**, not out of scope. | Seed says "athletics tournaments"; named parallels (Seltec TAF, Swiss Athletics meets) are stadium-centric |
| A-002 | MVP targets **club → regional/cantonal meets** (≤ ~1000 athletes, 1–3 days), engineered so championship-grade operation is a growth path, not a rewrite. | Realistic OSS adoption path; incumbents are entrenched at national level |
| A-003 | Interoperability stance: **(a) coexist/interoperate** — import licence/entry data, export federation-conformant results; no assumption of official mandate. | No stated mandate in seed |
| A-004 | Licence TBD-owner: founder. Requirements are written licence-neutral but assume **OSI-approved copyleft or permissive** licence, compatible with community contribution. | Seed: "open source" |
| A-005 | Community project with **lightweight governance documented in-repo** (maintainers file, contribution guide); no paid staff assumed. | Seed silent on funding |
| A-006 | **Both** hosting modes are eventually needed; MVP assumes **single-organizer deployment** (one meet at a time) with offline-capable venue operation; multi-tenant SaaS is later. | Venue reality + OSS adoption path; flagged as Phase B ADR gate item |
| A-007 | Para athletics: **data-model-ready in MVP** (sport class on athlete/result), full workflows (RAZA scoring, classification management) later. | Integrated Swiss meets exist; full support is costly |
| A-008 | All four languages **DE/FR/IT/EN** are required for public-facing surfaces at launch; operator/admin surfaces may launch DE/EN first. | CLAUDE.md locale mandate; Swiss federation practice |
| A-009 | Working name **"OpenMeet"** used in docs only; no branding decisions implied. | Placeholder |
| A-010 | No privileged data access assumed; verification uses public formats, synthetic data, and published vendor file-format specs. | Conservative default |
| A-011 | Until an Alabus sample is available, Swiss entry import is specified against a **generic documented CSV mapping** plus the publicly documented TAF3 import behaviour (categories/disciplines only; rounds & schedule created in-system). | [Seltec wiki on the Swiss entries flow](../research/competitive-analysis.md#c2-swiss-athletics-as-a-system-of-systems) |
| A-012 | The system's exchange schema is **self-defined, versioned, and openly documented**, with the W3C Open Athletics draft used as a design reference (not a compliance target). | It is a community-group draft, not adopted by any federation studied |

## 3. TBD register

Items marked TBD elsewhere in the baseline are consolidated here at the A6 pass.

| ID | Item | Owner | Blocking? |
|----|------|-------|-----------|
| TBD-001 | Historical IAAF/World Athletics XML result format — no primary spec located; needed only if federation submission requires it | Research (Phase B, on demand) | No |
| TBD-002 | Seltec user-reported limitations beyond vendor docs (forum review out of scope in first pass) | Research (optional) | No |
| TBD-003 | Para-athletics support level in incumbent systems — unverified either way; affects differentiation claim strength, not requirements | Research (optional) | No |
| TBD-004 | Exact Alabus feature set / export format (see OQ-011) | Founder / federation contact | Blocks final Swiss-entry-import spec detail, not the baseline |
| TBD-005 | SVM (Schweizer Vereinsmeisterschaft) point table & nationality quotas — only secondarily sourced; primary SVM Reglement PDF fetch failed | Research (before any SYS with exact point values) | Blocks exact team-scoring values only; scheme is specified as configurable |
| TBD-006 | "RAZA" para scoring — could not be verified to exist under that name; no requirement is built on it | Research (only if para scoring becomes MVP) | No |
| TBD-007 | World Athletics results-submission format/API — appears federation-mediated, no public API found | Founder / Swiss Athletics contact | No — result export is specified as file-based |
| TBD-008 | Track tie-break rule text (CR&TR Rule 26) and field-event flight-size thresholds — the full WA CR&TR 2026-edition PDF extracts cleanly; the TR 26 placings text was read during the OQ-044 pass, so extraction is on-demand when ranking logic needs it | Research (Phase B, on demand) | No |
| TBD-009 | Swiss licence fee amounts (CHF 60/120 secondary only) and Gebührenreglement details | Research (only if fee handling in scope) | No |

## 4. Founder answers (received 2026-07-05) and resulting decisions

Founder answers verbatim (lightly formatted), each converted into a decision **DEC-###** that
updates the corresponding assumption. Requirement deltas were applied to the baseline on
2026-07-05 (see §5).

| OQ | Founder answer (verbatim) | Decision |
|----|---------------------------|----------|
| OQ-001 | "track & field stadium meets only" | **DEC-001:** Scope = stadium track & field only. Road/XC/trail move from *Later* to **out of scope** until revisited. |
| OQ-002 | "club/regional meets, the goal is to help small clubs with no IT budget. big events certainly have more budget and more tools" | **DEC-002:** MVP optimizes for **small volunteer-run clubs with zero IT budget**; championship-grade is a growth path, not a target. Youth-series meets (UBS Kids Cup style) are a canonical MVP scenario. |
| OQ-003 | "the goal is to learn what kind of constraints (input / output) systems from swiss athletics and Seltec TAF3 would put on our system. Currently, some timings are done via electronic systems (track running / sprinting), but other are taken by humans on the field (ball throw, long jump, etc...) and entered manually into a system (unknown for now). the list of participants with their number is managed in Seltec (I saw the printed sheets)" | **DEC-003:** Interop stance = **learn & coexist**: treat Seltec/Swiss Athletics as fixed environment constraints (import/export at their boundaries), not integration partners. Verified pipeline in [research C7](../research/competitive-analysis.md#c7-field-observations--live-results-pipeline-ubs-kids-cup-le-mouret-4-july-2026). |
| OQ-004 | "I want a license that allows adoption and improvement by clubs, but not for a private company to easily make money off of this project" | **DEC-004:** Licence direction = **strong copyleft; AGPL-3.0 is the leading candidate** (closes the SaaS loophole). Note: no OSI licence can prohibit commercial use outright; AGPL makes proprietary capture unattractive. Final choice = ADR-001 at the Phase B gate. |
| OQ-005 | "solo for now, TBD if adopted by clubs with small club sponsoring it" | **DEC-005:** Solo-founder governance; keep in-repo governance lightweight; revisit if clubs adopt. |
| OQ-006 | "homelab hosted for now. later, if a club wants to use it, it may need to live in the cloud, but with modest budgets. maybe we should investigate if a desktop offline app is even possible and matches the constraints" | **DEC-006:** Deployment target #1 = **founder homelab / single self-hosted instance**; cloud later at modest cost. **Phase B0 must evaluate a desktop/offline-first app vs. self-hosted web app as an explicit ADR with trade-offs** (ADR-002 candidate). |
| OQ-007 | "out of scope for now, local meetings are not para-athletic" | **DEC-007:** Para athletics = **out of scope for MVP** (kept data-model-friendly: adding a class field later must not require redesign). Mixed **age-category** fields remain MVP (youth series need them). |
| OQ-008 | "DE/FR, but allow for easy internationalization" | **DEC-008:** Launch languages = **DE + FR complete** (public *and* operator surfaces); i18n architecture must make adding IT/EN a translation task, not an engineering task. |
| OQ-009 | "I let you brainstorm something and check against existing offerings. Core idea: athletics, switzerland, small clubs hosting events with volunteers" | **DEC-009 (RESOLVED 2026-07-05):** Name = **Bahnfrei** (founder pick from shortlist §6). Name/logo rights held personally by the founder per ADR-001 §5. |
| — *(gate record)* | Founder, 2026-07-05: "I ratify all six ADRs as proposed, keep the name Bahnfrei" | **DEC-014: Phase B gate 2 PASSED.** ADR-001 (AGPL-3.0-only + DCO), ADR-002 v2 (hub-first), ADR-003 (Go/SQLite/HTMX), ADR-004 v2 (storage + capture queue), ADR-005 (domain model + omx/v1 + external IDs), ADR-006 (FinishLynx-first + timing agent) — all **Accepted**. Phase B implementation is unblocked; backlog in `docs/delivery/work-breakdown.md`. |
| OQ-010 | "no access for now, could be asked, but probably not or with a lot of convincing (GDPR, anonymization, ...)" | **DEC-010:** Verification uses **synthetic data only**; the public LA.portal result pages (already-published data) may be used as structural reference. |
| OQ-011 | "absolutely no idea. if I have a PoC to show to the local clubs, I may obtain something" | **DEC-011:** **PoC-first strategy**: build against the documented generic CSV mapping (A-011); an Alabus sample becomes an ask once a PoC exists to show clubs. |
| OQ-012 | "is this gonna become mainstream? also in switzerland? I guess that if we must choose something from scratch, we can align with W3C Open Athletics Community Group" | **DEC-012:** Honest answer: **no evidence it will become mainstream** — it is a community-group draft no federation has adopted (C3). Use it as the design reference where we define schemas from scratch (per A-012); do not promise conformance. |
| OQ-016 *(founder direction, 2026-07-05, ADR-002 ratification discussion)* | "we will be expecting some kind of online connectivity to a central hub. the system should be however tolerant to LTE and WIFI issues and not stop working at the first network issue in the meet timing room or out in the field […] should not cost a fortune to deploy, as the target is small clubs […] my goal is not to have a SaaS page with subscriptions […] I build the open source foundation and they use the stack and host the stack (made as easy as possible by us) on their own" | **DEC-013: hub-first connectivity posture.** Expected = connectivity to a central **self-hosted** hub; MVP = interruption tolerance (SYS-085–087, UC-034); full no-internet venue operation deferred behind an evidence gate (SYS-080 → *Later*, UC-019); **field-wide venue Wi-Fi = non-goal** (research C8: club-tier incumbents all use officials' phones + LTE via cloud relay); deployment cost sized for small clubs; **no subscription-SaaS business** — clubs self-host. Re-scopes ADR-002 (v2) and ADR-004 (v2); adds Swiss-chain interop SYS-077/078 (C2.1). |

## 5. Requirement deltas applied from the answers (2026-07-05)

| Change | Where |
|--------|-------|
| Para: STR-030 re-scoped to mixed **age-category** fields (MVP); para sport classes → out of MVP scope (Later) | StRS §3.5/§4, SYS-052, UC-028 |
| Languages: DE/FR complete at launch (public + operator); IT/EN → Later, must be translation-only effort | STR-033, SYS-110, UC-025 |
| Road/XC/trail from Later → out of scope | StRS §4 |
| NEW STR-042: public results labeled unofficial; federation channel remains the official source of truth for sanctioned meets | StRS §3.4, SYS-076, UC-017 |
| NEW STR-043: Swiss youth-series formats (UBS Kids Cup first) with series-specific scoring tables | StRS §3.3, SYS-053, UC-033 |
| Licence direction (AGPL-lead) recorded as constraint note | SyRS CON-02 |
| Desktop-app-vs-web evaluation mandated for Phase B0 | DEC-006 → ADR-002 candidate |

## 6. Project name shortlist (DEC-009, for founder pick)

Criteria: works across DE/FR/IT/EN, evokes athletics + grassroots volunteering, no collision
found with existing athletics software (checked 2026-07-05 via web search; **domain/trademark
checks still TBD before adoption**).

| Candidate | Reading | Notes |
|-----------|---------|-------|
| **Bahnfrei** | DE starter's call "Bahn frei!" (track clear!) | Distinctive, sympathetic, DE-leaning; understandable in CH; no software collision found |
| **PisteLibre** | FR mirror of the same idea | FR-leaning twin; could pair as bilingual brand "Bahnfrei / PisteLibre" |
| **OpenLane** | EN, "the open lane" | Neutral EN; collision risk with US automotive marketplace "OPENLANE" in other domain — likely acceptable, verify trademark class |
| **Startklar** | DE/CH "ready to start" | Warm, volunteer-flavored; common word → weak trademark, domains likely taken |
| **Anlauf** | DE "run-up" (jump approach) + "getting started" | Nice double meaning; DE-only comprehension |

Avoided: anything with "OpenTrack" (existing commercial vendor + W3C group naming collision,
C3/C4), "Athletica" (club-name collisions), "Meet*" generics (HyTek "Meet Manager", "MeetPro").

## 7. New/updated open items from this round

| ID | Item | Owner | Blocking? |
|----|------|-------|-----------|
| OQ-013 | **Club validation of the results pipeline:** confirm with the UBS Kids Cup host club: (a) results were entered in TAF3 on-site (in the results room), (b) live push used Liveserver/LA.portal, (c) whether a club may additionally publish results elsewhere (any federation policy against it), (d) how manual field measures reached TAF3 (paper slips → typed in?) | Founder (has club contact from the meet) | Blocks STR-042 fine-tuning and the official-channel strategy, not the baseline |
| TBD-003 *(updated)* | Para support in incumbents: **partially resolved** — Seltec offers a paid "Para" licence module with para-points support ([research C7.2](../research/competitive-analysis.md)) | closed enough for now | No |
| OQ-014 *(updated 2026-07-05)* | **Official-tier entry (C2.1):** the lock is double — (1) **normative:** WO 2026 §5.6a mandates TAF3 by name for Meisterschafts- und offizielle Wettkämpfe (see OQ-015 resolution), so entering this tier requires Swiss Athletics to amend/waive an association rule it can change annually; (2) **technical:** results must reach Alabus same-day and the only upload tooling is inside TAF3. The eventual ask to Swiss Athletics (once a PoC has club traction, per DEC-011) is therefore *recognition as an additional permitted system + a documented upload path* — precedents: DLV Bestenliste auto-ingests from **two** systems (TAF3 + COSA WIN), Atletiekunie recognises two systems. | Founder → Swiss Athletics Geschäftsstelle | Blocks the **official/championship** tier only; the youth-series/school/club tier has an official Excel path (C2.1) and is not blocked |
| OQ-015 *(RESOLVED 2026-07-05)* | WO 2026 §5.6a mandates TAF3 for championship/official competitions (a revisable federation rule, not state law); §5.6b mandates LA.portal; §5.3b requires TAF3-linked licence numbers for Bestenliste entry. Resolved. | closed | No — sharpens OQ-014 from a file-format ask to a recognition ask |
| OQ-017 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | Swiss WO 2026 §1.1/§1.2 start-up ceilings verified against the primary source: youth bands admit younger athletes, Männer/Frauen admit all birth years, each Masters band its own lower bound and older — masters early start-up confirmed **not permitted**. Data corrections shipped (M30/W30 missing start-down allowance; a WO source erratum documented). Resolved. | closed | No — closed |
| OQ-018 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | The suspected "transcription artifact" was wrong: WO 2026 §1.5e literally bars U10/U12/U14/U16 athletes from 150/200/300/400 m and 300/400 mH (Swiss youth sprint 60/80/100 m instead, §8.1.1–§8.1.3). All six bars now encoded; a U16-or-younger event using 200/400 m is correctly flagged blocked by SYS-014 eligibility. Resolved. | closed | No — closed |
| OQ-019 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | Youth hurdle-height/implement variants populated from the primary WO 2026 discipline tables (§8.1.1–§8.1.3), matching the WA CR&TR senior specs. Deliberately out of scope: WO indoor tables (§9.1.2/§9.1.3, different hurdle layouts) and Masters implement/hurdle specs. Resolved. | closed | No — closed (indoor variants are an additive follow-up when an indoor meet is first scoped) |
| OQ-020 *(TASK-007, 2026-07-08; RESOLVED 2026-08-04, DEC-016/TASK-036)* | A real official UKC Rangliste (LV Langenthal, 17 May 2025) contradicted the shipped assumption: an athlete missing a whole discipline is listed unranked at the bottom ("aufg."), while one who attempted every discipline but scored no valid mark in some still ranks, that discipline flooring to 1 point rather than 0; a third case ("n.a.") evidenced a distinct out-of-competition status. Resolved (DEC-016, TASK-036): final-vs-provisional standings distinction, the 1-point floor, and an out-of-competition flag shipped. Residual gaps: OQ-090, OQ-091. | closed | No — closed |
| OQ-021 *(TASK-010, 2026-07-10; revisited TASK-018, 2026-07-12)* | **Start-list publication step:** SYS-070 lists "start lists" among the public surfaces and SYS-071 gives publications a 60 s budget, implying a deliberate publish action — but only the timetable has a versioned draft/published state today; start lists serve the live roster/heat-sheet directly. Decide whether start lists need their own published snapshot (timetable-style), an ADR-worthy publication-model decision, or whether the live view is the intended permanent shape. | Tech lead (revisit at TASK-018) | No — PoC surfaces are labeled unofficial (SYS-076); the live view is not a silent gap |
| OQ-022 *(RESOLVED 2026-07-12, TASK-024)* | SYS-074 discipline-name localization (`discipline.<code>` i18n keys) had shipped only for disciplines the built-in UKC template exercised, not the full catalog; the public timetable page also still used the canonical English name instead of the localized helper. Resolved: all 33 disciplines gained DE/FR keys; the timetable page switched to the shared localization helper. Operator-only surfaces intentionally keep the canonical catalog name (locale-invariant identifier), unchanged. | Tech lead (TASK-024) | No — closed |
| OQ-024 *(RESOLVED 2026-07-12, TASK-024)* | SYS-110's mark-notation and date-format conventions: resolved as (1) person-displayed dates/timestamps use Swiss day.month.year notation in both DE and FR (distinct from the fixed ISO wire format `<input type="date">` requires); (2) captured marks keep period-decimal notation ("7.67") in both locales, deliberately not switched to the incumbent's comma convention, since marks feed period-decimal interchange formats (LIF, TAF3-CSV) and a locale-varying representation risks silent misparsing (the SYS-086 failure class). | Tech lead (TASK-024) | No — closed; revisit only if a future locale's users report the mark format is unreadable |
| OQ-023 *(TASK-012, 2026-07-10)* | **UKC series-upload file: exact column layout unverified.** SYS-077/UC-035 require the export to conform to "the current official UKC organizer template," but its byte-for-byte layout could not be obtained offline. The Mille Gruyère/Visana Sprint organizer manual confirms a downloadable "Vorlage Resultatimport" Excel template is the non-TAF3 submission path for the Nachwuchsserien family; a real UBS Kids Cup TAF3 result-list export (LV Langenthal, 17 May 2025, https://lvl.ch/images/resultate/2025/Gesamtrangliste_UBSKidsCup_2025.pdf) corroborates the column semantics the shipped fixture mirrors, but the shipped file is a clearly-marked assumption, not a verified conformance match. Obtain the actual template and diff it against the shipped data file once accessible. | Founder / UBS Kids Cup or Nachwuchsserien series office | No — SYS-077 CON-01 requires the template to ship as a replaceable data file; correcting it is a data-file swap, not a code change |
| OQ-029 *(TASK-016, 2026-07-12)* | **Meet-status "published" transition was unwired:** UC-003 #1's "published meet with open entries" precondition had no prior path to `MeetStatus.MeetPublished` — TASK-006/007 wired only draft creation and archive. Added `MeetService.PublishMeet` (draft→published, organizer-gated, audited as `meet.publish`) as the minimal missing transition, with a button on the meet workspace when the meet is a draft. Confirm this is the intended lifecycle action long-term — e.g. should publishing also require a completeness check, mirroring the sanctioning-summary gate (UC-001 #5)? | Tech lead (meet-lifecycle owner) | No — the transition is minimal and additive; a completeness gate can be layered on later without a breaking change |
| OQ-025 *(TASK-016, 2026-07-12; decided DEC-023, 2026-08-03)* | Entry-submitter account provisioning has no self-service sign-up path — only an instance admin can create accounts (TASK-013's account model). Resolved (DEC-023): self-service submitter sign-up stays deferred; organizers provision `entry_submitter` accounts out of band. | closed | No |
| OQ-026 *(TASK-016, 2026-07-12; decided DEC-023, 2026-08-03)* | SYS-015 entry limits shipped at event granularity only (per-club/per-athlete caps need a policy decision not specified in the baseline). Resolved (DEC-023): per-club/per-athlete caps stay deferred; the per-event cap remains the shipped granularity. | closed | No |
| OQ-027 *(TASK-016, 2026-07-12; decided DEC-020, 2026-08-03)* | Relay entries carry no seed performance / SYS-015 entry-standard evaluation. Resolved (DEC-020): release 0.1 ships relays as entries/roster-only; a bundled post-0.1 relay task covers this alongside OQ-032/049/057. | closed | No |
| OQ-028 *(TASK-016, 2026-07-12)* | Payment processing confirmed out of scope per SYS-017: fee computation only (schedule × entries, CSV export), no external payment/invoicing integration. | closed | No — matches the SyRS baseline already |
| OQ-030 *(TASK-017, 2026-07-12)* | **Alabus entries-export column layout remains unverified** — same underlying gap as OQ-011, revisited for the SYS-013(b) mapping profile itself. No sample Alabus export could be obtained (marketing site blocks automated access; the TAF3↔Alabus flow is documented only from the Seltec/TAF3 side, `docs/research/competitive-analysis.md`). The shipped `alabus.json` import-mapping profile is a clearly-marked **assumption** — German-language column headers modelled on the verified TAF3-family result-list vocabulary (OQ-023) and WO 2026 §5.3b's licence-number join-key requirement, loaded through the same generic mapping interpreter as the system-native profile, so replacing it with the real layout is a data-file swap, not a code change. Obtain a real Alabus export sample (per DEC-011's PoC-first strategy) and diff it against the shipped file. | Founder / Swiss Athletics or club contact | No — same versioned-data path as OQ-011/OQ-023; a correction is a data-file swap |
| OQ-031 *(TASK-017, 2026-07-12)* | UC-005 #3's "max one race ≥600m per day" cap is approximated at meet scope, not day scope — over-flags a multi-day meet where an athlete legitimately runs one ≥600m race on each of two different days (single-day meets unaffected). Narrow the check to day-scope once events carry a day link. | Tech lead (revisit at TASK-018, mirrors OQ-021's precedent) | No — the meet-scope check is conservative (over-flags, never under-flags) and self-contained |
| OQ-032 *(TASK-017, 2026-07-12; decided DEC-020, 2026-08-03)* | Relay entries are not evaluated for SYS-014 eligibility (category/licence/youth-protection) — no single athlete to key the check against. Resolved (DEC-020): deferred, bundled into the same post-0.1 relay task as OQ-027/049/057. | closed | No |
| OQ-033 *(TASK-017, 2026-07-12; closed TASK-039, 2026-08-05, DEC-023)* | Online entry forms collected no licence number, so any online entry at a licence-required tier (A/B-Meeting) was always flagged blocked until an operator separately attached one via CSV re-import — a real UX gap for A/B-Meeting organizers. Resolved (DEC-023, TASK-039): individual and bulk online-entry forms gained an optional, permissively-validated licence field (OQ-104), resolving the athlete via the same join-key semantics as CSV import; the relay entry form is unchanged (OQ-106), scoped out by DEC-023. | closed | **Closed** — no longer blocks A/B-tier organizers relying on online entry |
| OQ-034 *(TASK-018, 2026-07-12; RESOLVED 2026-07-15, rule-data verification pass)* | Lane-draw classification verified against the primary WA CR&TR 2026 TR 20.4: the by-lot single-round rule is scoped to top-tier World Rankings competitions, so UC-008 #3's grouped 400 m club final is legitimate practice, not a violation. Real correction found: rank-group → lane-set tables differ by event class (straight races / 200-300 m / 400 m-class), not one universal table; all three classes now shipped. Resolved. | closed | No — closed |
| OQ-035 *(TASK-018, 2026-07-12)* | Two best-effort seeding algorithms are shipped without a fully worked reference example to verify against, only synthetic fixtures: (1) the club-separation swap heuristic (D2.2) resolves a same-club pass-boundary collision by exchanging with the next-ranked candidate in a different heat, proven only against a small synthetic case, not against larger fields with multiple simultaneous collisions; (2) when a heat already contains a manually-overridden entry (SYS-028), heat generation falls back to a by-lot draw for the remaining free lanes instead of re-deriving a partial TR20.4 group assignment. Confirm both are acceptable simplifications, or specify exact expected behaviour with a worked example. | Founder / tech lead (algorithm refinement) | No — both sit behind a stable interface; refining either is additive, not a schema or API change |
| OQ-036 *(TASK-019, 2026-07-12; RESOLVED 2026-08-05, DEC-024/TASK-040)* | Result corrections were scoped to the settled-result level for track only; horizontal/vertical field corrections had no operator UI (UC-015 #2/#3). Resolved (DEC-024, TASK-040): the correction UI now covers every family (track, horizontal-field, vertical-field) at the settled-result level; **attempt-level resumable corrections stay deferred per DEC-024**. Building the field/vertical UI surfaced two pre-existing bugs, fixed in the same task: live unit standings weren't re-ranking after a correction, and a status-only (DNS/DNF/DQ/NM) correction failed outright. Follow-on narrower questions: OQ-101, OQ-102. | Founder / tech lead (follow-up to TASK-021/022's field-event work) | No — closed; the remaining attempt-level resumable-correction question is deliberately deferred per DEC-024, not blocking |
| OQ-037 *(TASK-019, 2026-07-12; decided DEC-024, 2026-08-03)* | The protest-clock countdown is computed at render time, not live-ticking in the browser — an idle tab shows a stale countdown until the next fetch/SSE refresh. Resolved (DEC-024): a client-side ticking countdown stays deferred; render-time accuracy is sufficient for now. | closed | No — purely a display refinement; the underlying state is always correct when read |
| OQ-038 *(TASK-019, 2026-07-12; decided DEC-024, 2026-08-03)* | Reaction times (SYS-040) had no manual capture field — UC-010's acceptance criteria never exercise it, and a hand-operated form is not a plausible source of record for a value that comes from a start-block transducer. Resolved and closed (DEC-024): the FinishLynx `.lif` import (UC-014) is the sole reaction-time source; no manual field. | closed | No |
| OQ-039 *(TASK-019, 2026-07-12; decided DEC-024, 2026-08-03)* | The escalation reference required for a post-window correction (SYS-047) is a free-text field, not a structured jury/referee decision record (who filed the protest, who ruled). Resolved (DEC-024): a structured jury/protest-case entity stays deferred; the free-text reference is the MVP shape. | closed | No |
| OQ-044 *(TASK-021, 2026-07-13; RESOLVED 2026-07-15, rule-data verification pass)* | Cross-height elimination confirmed against the primary WA CR&TR 2026 TR 26.2 (three consecutive failures across heights eliminate, except in a tie for first): shipped behaviour was already correct, no change needed. | closed | No — closed, shipped behaviour confirmed correct |
| OQ-045 *(TASK-021, 2026-07-13; RESOLVED 2026-07-15, rule-data verification pass)* | The secondary source describing WA combined-events tie-break described the pre-2020 rule; WA CR&TR 2026 TR 39 abolishes tie-breaking entirely (equal points stand as a tie). Fixed: a data-driven `tieBreak` policy field lets WA-formula tables stand ties per TR 39 while UKC meets keep their Reglement §3 majority rule unchanged. | closed | No — closed |
| OQ-046 *(TASK-021, 2026-07-13; scheduled 0.2, DEC-040)* | Vertical jump capture is online-only — the offline capture island (TASK-009, UC-034/SYS-085-087) is wired specifically to the horizontal-attempt grid and was not extended to the height-progression addressing scheme vertical jump needs. A field official capturing high jump/pole vault offline sees an ordinary network error, not a graceful offline queue. Scheduled for TASK-063 (DEC-040). | Founder / tech lead | No — the offline island's scope is additive per discipline family |
| OQ-047 *(TASK-021, 2026-07-13)* | SYS-031's multi-day/parallel-group combined-events scheduling (UC-013 #1) is satisfied by reusing the existing Event/Round/Unit/Session foundation, with no combined-events-specific entity and no dedicated multi-day/multi-group fixture test. Confirm this reuse is sufficient, or whether combined events need a first-class "this group of events is one competition" grouping entity (e.g. so a public results page doesn't list a decathlon's ten disciplines as unrelated individual events). | Founder / tech lead | No — no new entities were introduced; a future grouping entity would be additive |
| OQ-040 *(TASK-023, 2026-07-13)* | SYS-103's "minor" age threshold is set to 18 (Swiss age of majority, ZGB Art. 14). nFADP sets no fixed digital-consent age the way GDPR Art. 8 lets member states set 13–16; confirm 18 is the right threshold for *publication*-consent specifically, not a stricter/looser sport-specific convention. | Founder / privacy counsel (feeds the Opus TASK-023 review gate) | No — a single named constant; changing it is a one-line, fully-traced change |
| OQ-041 *(TASK-023, 2026-07-13; decided DEC-026, 2026-08-03)* | SYS-103's publication-withdrawal suppression is applied uniformly to any athlete of any age, though the requirement text emphasizes minors without saying withdrawal is restricted to them. Resolved (DEC-026): confirmed as intended — suppression applies at any age. | closed | No |
| OQ-042 *(TASK-023, 2026-07-13)* | SYS-102's default retention period (90 days) is UC-024 #3's own acceptance-criteria example, not sourced from a specific legal or federation deadline; photo/extended-data consent flags are recorded at entry time but not yet enforced (no photo-publication surface exists yet). Confirm 90 days against any Swiss Athletics results-challenge/protest window once known, and revisit consent-flag enforcement when photo publication is scoped. (A related SYS-100 minimization determination for the fee-summary CSV export and CSV-import consent handling was made under the same task's audit and is settled, not open.) Remains open pending legal/federation input. | Founder / next Swiss Athletics contact | No — `--retention-days` is already an operator-configurable flag; a corrected default is a one-line change |
| OQ-043 *(TASK-023, 2026-07-13; decided DEC-026, 2026-08-03)* | UC-023 #2 anticipates a "configured legal mode" for suppressing a withdrawn athlete's public entry; the system ships one mode only — "neutralize" (keep the row, blank name/club) — chosen over full omission since omission lets a determined viewer infer identity by elimination in a small division. Resolved (DEC-026): confirmed as intended, the sole suppression mode. | closed | No |
| OQ-048 *(TASK-020, 2026-07-13)* | FinishLynx status codes FS (false start) and SC (scratched) have no CR 25 result-status equivalent (SYS-045's operator-settable vocabulary is DNS/FS/DNF/DQ/NM); an imported row carrying one is queued as an `unmapped_status` conflict for manual translation rather than guessed. A cleanly-imported DQ also carries no rule reference (FinishLynx has no such field); the office fills it in via the ordinary correction flow. Confirm the FS→DQ-class judgment call with the founder/a timing-crew contact. | Founder / tech lead | No — the conflict path is safe by construction; resolving the mapping question only changes which codes route through `unmapped_status` |
| OQ-049 *(TASK-020, 2026-07-13; relay part decided DEC-020, 2026-08-03)* | Three scope limits in the timing-exchange slice, all fail safely and are additive to relax later: (1) track-only exports (field units have no photo-finish counterpart); (2) individual entries only — relay legs are skipped from the export roster (**resolved, DEC-020: relays stay roster-only in 0.1, bundled into the post-0.1 relay task**); (3) `unresolved_unit` conflicts only support discard, not reassignment to a different unit — still open. | Founder / tech lead | No — each limit fails safely; lifting any is additive |
| OQ-050 *(TASK-020, 2026-07-13)* | The timing agent has no TLS trust-distribution mechanism for a venue's self-signed local hub certificate (SYS-093's no-internet-dependent-validation posture); the shipped stopgap is an explicit, documented `--insecure-skip-verify` flag rather than a real trust mechanism. This mirrors the same open question UC-019 #4 already names for LAN clients generally. Route the actual trust-distribution design through an ADR when a venue/offline-node deployment is prioritized (UC-019 is evidence-gated, DEC-013). | Founder / tech lead (ADR, when UC-019 is prioritized) | No — the flag is opt-in and documented; the hub role (ACME-issued certs) needs no such flag |
| OQ-051 *(TASK-020, 2026-07-13)* | `.lif`/`.evt` header field-layout carries fixture-vs-reality drift risk, per ADR-006's own named risk: the authoritative field order is transcribed from the vendor's documented "Database Files" page, but a real third-party `.lif` sample pads two extra empty header fields, inconsistent with that spec. No real FinishLynx-generated file from an actual timing PC has been obtained to confirm which is correct. The ingest pipeline degrades safely (an unparseable/mismatched header surfaces as a conflict, never silently misapplied), but a real venue sample (per OQ-013's club-contact ask) should be diffed against the fixture set once available. | Founder / OQ-013's club contact (or any Swiss club running FinishLynx) | No — the codec already degrades to an explicit conflict rather than misparsing |
| OQ-052 *(TASK-022, 2026-07-13; scheduled 0.2, DEC-040)* | Record/best flags (SYS-049) are wired into manual-capture and correction flows and, since TASK-025, into `omx/v1` export — but results written by the FinishLynx timing-import ingest pipeline never run through the record evaluator, so an imported mark earns no record/PB/SB flag until an operator re-saves or corrects it. Scheduled for TASK-064 (DEC-040). | Tech lead / founder (timing-import evaluator wiring as a follow-up) | No — the gap is an omission, never a wrong flag |
| OQ-053 *(TASK-022, 2026-07-13; scheduled 0.2, DEC-040)* | Combined-events wind-average record flagging (SYS-050, UC-016) is not wired into the record evaluator — the evaluator is only called from plain track/field capture paths, not the combined-events per-discipline save path, so a combined-events meet's marks never earn a record/PB/SB flag and no combined-events record type is evaluated. Scheduled for TASK-064 (DEC-040). | Tech lead / founder (prioritize a follow-up task if combined-events records are wanted) | No — the gap is an omission, not an incorrect flag; combined-events standings/scoring are unaffected |
| OQ-054 *(TASK-022, 2026-07-13; scheduled 0.2, DEC-040)* | No organizer-facing UI exists to select/upload a meet's record lists — the service-layer mechanism is complete and tested, but no meet-settings page exposes it and there is no file-upload flow for a custom record-list JSON file. The one shipped built-in list is an explicitly labeled non-authoritative fixture, not usable as-is for a real meet. Scheduled for TASK-064 (DEC-040). | Founder / tech lead (prioritize a meet-settings UI + list-upload flow) | No — the gap only affects self-serve configurability, not correctness of flagging once a list is wired |
| OQ-055 *(TASK-022, 2026-07-13; scheduled 0.2, DEC-040)* | Retroactive per-race wind correction (`SetUnitWind`) does not re-evaluate record/best flags for results whose wind reading changes after they were saved — a result's flags stay stale until the next capture/correction touches that row. Scheduled for TASK-064 (DEC-040). | Tech lead / founder (decide whether `SetUnitWind` should re-run record evaluation) | Possibly — a rare retroactive-wind-correction scenario could leave a stale record flag |
| OQ-056 *(TASK-025, 2026-07-13)* | `omx/v1` (UC-027 #1) carries only the current settled state of each result, never correction/audit history — a deliberate choice since SYS-046's append-only audit log already owns "who changed what, when, why," and duplicating it into the interchange format would be a second, driftable source of truth. Confirm no federation/regulatory delivery requirement (D10.2's ≤24h submission window) actually needs correction history riding along in the same file. | Founder / tech lead | No — an additive `corrections[]` array is a non-breaking extension if one is ever needed |
| OQ-057 *(TASK-025, 2026-07-13; decided DEC-020, 2026-08-03)* | `omx/v1` results cover individual entries only — relay-team race results have no capture path anywhere in the system yet (out of scope per the task brief's UC-027/UC-021 focus), confirming OQ-049's finding system-wide. Resolved (DEC-020): relays stay roster-only in 0.1, bundled into the same post-0.1 relay task as OQ-027/032/049. | closed | No — the gap fails safely, relay results are simply absent from every export |
| OQ-058 *(TASK-025, 2026-07-13; RESOLVED 2026-07-13, merge reconciliation with TASK-022)* | `omx/v1`'s `results[].recordFlags` field shipped empty from TASK-025, pending TASK-022's concurrent record-flag computation work landing. Resolved: both tasks reconciled onto the same pre-existing column with no schema change needed; evaluator-computed flags now round-trip end to end. | closed | No — the field was additive-safe by construction |
| OQ-059 *(TASK-029, 2026-07-13)* | Relay-entry audit rows and a relay leg's identity are not part of the SYS-101 erasure-redaction sweep. Analysis suggests no audit row leaks a relay leg's identity and display names resolve live (already reflecting post-erasure pseudonyms), so the gap is unverified rather than defective. Confirm whether a relay-scoped erasure test is wanted, or whether the closed-form argument is sufficient. | Founder / tech lead | No — relay composition is unchanged; adding a relay-specific test later is additive |
| OQ-060 *(TASK-029, 2026-07-13)* | The public-path privacy crawler discovers pages by following rendered links plus a small hardcoded list for unlinked routes — a real regression improvement over the prior hand-enumerated list, but it cannot discover a future public route reachable only via unlinked/JS-constructed URLs. Confirm whether a stronger discovery mechanism (e.g. a compile-time route registry tag) is wanted before more public surfaces ship. | Founder / tech lead | No — the crawler is additive test infrastructure |
| OQ-061 *(UX requirements round, 2026-07-13; decided DEC-036, 2026-08-07 — brand ratified, scheduled TASK-056)* | Bahnfrei had no ratified visual identity — STR-045/SYS-116 mandate a design system, but no brand colors, typography, or logo were decided; the interim state was a neutral placeholder favicon (TASK-050). Resolved (DEC-036, TASK-056): teal accent, warm-cream neutrals, a self-hosted display typeface, and a real favicon/wordmark shipped as a token-level retint — no designed logo yet, per DEC-036's explicit scope. | closed | No — tokens make rebranding a data-level change |
| OQ-062 *(TASK-033, 2026-07-13; decided DEC-032, 2026-08-07; RESOLVED 2026-08-07, TASK-052)* | Timing-conflict "merge" resolution could never attach an imported mark to a status-only result, contradicting its own doc comment ("fill only the existing result's blank fields from the imported data") — the mark was silently discarded whenever the existing result carried any status. Resolved (DEC-032, TASK-052): blank fields are now genuinely filled independently (mark and status each fill from whichever side is blank). | closed | No — closed |
| OQ-063 *(TASK-033, 2026-07-13; closed TASK-034, 2026-07-14)* | `ExportGenericCSV` never verified the meet exists, unlike its siblings — an unknown meet ID returned 200 with a header-only CSV instead of 404. Closed (TASK-034): now checks the meet exists first, matching its siblings; all four timing-export routes 404 uniformly for an unknown meet. | closed | No — a one-line change plus test update |
| OQ-066 *(TASK-027, 2026-07-13; RESOLVED 2026-08-03, DEC-015/TASK-035 read-path fix)* | SYS-122's 2,000-concurrent-viewer budget (p95 render ≤3 s, errors ≤0.1%) was measured UNMEETABLE on the original read architecture: a single pooled SQLite connection plus per-request full-meet recomputation extrapolated to p95 ≈2.3 minutes and ≈125 GiB of in-flight memory at 2,000 viewers (measured up to 250 viewers, an unconfined 2,000-viewer run OOM-killed the host on 2026-07-13). Resolved (DEC-015, TASK-035): a pooled WAL read-connection set plus a per-meet render cache invalidated by the results-changed bus event. Re-measured: the literal 2,000-viewer assertion now passes at p95 343-377 ms, memory flat rather than linear in viewer count. Full numbers: `docs/delivery/perf-and-recovery-task-027.md`. | Founder / tech lead (architecture decision; likely a small ADR amendment + follow-up TASK) | No — closed |
| OQ-067 *(TASK-027, 2026-07-13; RESOLVED 2026-08-05, DEC-021/TASK-038)* | SYS-120 names "entry search" as a budgeted operator-visible interaction, but no search/filter feature existed anywhere — the operator's only way to find an entry among 4,000 was the full roster/entries list page. Resolved (DEC-021, TASK-038): a server-side query-param filter (case-insensitive substring match on name/bib/club) ships on the roster and bib-assignment pages, measured p95 44 ms — well inside the 2 s budget. | Founder / tech lead | No — a search endpoint is additive |
| OQ-070 *(TASK-030, 2026-07-13; closed TASK-041, 2026-08-05, DEC-025)* | SYS-114's "bulk operations for multi-athlete actions" had exactly one implemented instance (check-in's "close check-in", UC-007 #1); track result entry had no bulk mechanism. Confirm whether that single instance satisfies the clause, or whether a bulk result-entry action should ship. Closed (DEC-025, TASK-041): a second bulk operation shipped — "mark all remaining as DNS" on a still-open track unit, an audited office action behind a confirm sub-page. | closed | **Closed** — no longer blocks SYS-114's bulk-operation coverage |
| OQ-074 *(TASK-032, 2026-07-13; closed TASK-034, 2026-07-14)* | Usability-audit finding H3/F5: no destructive/irreversible action (athlete erasure, account disable, meet archive, retention purge) had an explicit confirmation step — all were single-click submits, and the strict CSP rules out an inline `confirm()` fix. Closed (TASK-034): all four sit behind a shared GET confirm sub-page; erasure and retention purge (irreversible and unrecoverable) additionally require typing a confirmation token before the POST is honored. | closed | **Closed** — no longer blocks the 0.1 usability-audit re-run (UC-038 #3) |
| OQ-075 *(TASK-032, 2026-07-13; closed TASK-034, 2026-07-14)* | Usability-audit finding H9/F3: validation errors rendered as a single page-level flash message, never inline at the offending field — a form did not name which field was wrong. Closed (TASK-034): a reusable field-error mechanism wires handler validation into per-field inline messages on the three UC-038 #4 representative forms (meet setup, online entry, result correction); the page-level flash remains as a summary. | closed | **Closed** — no longer blocks the 0.1 usability-audit re-run (UC-038 #3) |
| OQ-076 *(TASK-032, 2026-07-13; substantially closed TASK-031, 2026-07-13)* | Usability-audit finding H6/F2: the SYS-117 field-level hint-text convention existed as a CSS class but was not adopted per-field on existing forms (page-level blurbs only). TASK-031 adopted it wherever a hard format/unit/bound constraint exists, added the SYS-115 help-icon registry for non-obvious domain fields, and separately gave 13 dense inline/table-row fields an accessible name (F1) via `aria-label`. **Still open (narrowed):** the residual visible-label upgrade for those 13 fields, and hint copy for remaining fields without a hard constraint. | Founder / tech lead | No — the residual visible-label upgrade is additive copy/layout |
| OQ-078 *(TASK-026, 2026-07-14; RESOLVED 2026-08-03, testing-stack hardening pass)* | SYS-092's "static analysis in CI" clause was satisfied for dependencies (`govulncheck`) but had no dedicated own-code security SAST. Resolved: gosec now runs in CI and the merge gate; all baseline findings triaged (real fixes shipped where warranted, remainder carries a justified inline suppression). | closed | No — closed |
| OQ-079 *(TASK-026, 2026-07-14; closed TASK-034, 2026-07-14)* | Low-severity open-redirect in the locale-switch handler: it redirected to the raw `Referer` header, which may be an absolute off-origin URL (assessed not a practical phishing primitive, but left unfixed pending a UX-safe fix). Closed (TASK-034): the redirect target is now accepted only when root-relative or same-host, else falls back to `/`. | closed | No — a bounded guard plus test; no architectural impact |
| OQ-077 *(TASK-032, 2026-07-13; scheduled 0.2, DEC-040)* | **Minor usability-audit finding (`usability-audit-checklist.md` F6): no operation shows explicit progress/in-flight feedback beyond the browser's native page-load indicator.** Every mutating action is a synchronous POST-redirect-GET (or a direct file download for exports/backup/PDFs); there is no disabled-while-submitting button state, spinner, or "processing…" text anywhere. At current MVP scale this is low-risk (SYS-120 targets ≤2s p95 for interactive operations, and a full page navigation's native loading affordance is not "silence"), but it is worth a deliberate look once import/export operations on larger meets are measured (TASK-027 performance work) rather than assuming it stays fine. | Founder / tech lead | No — purely additive UX polish; does not change any handler's contract |
| OQ-086 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-017)* | SYS-146's container image publication target was undecided — the Dockerfile builds a verified working image, but no registry account existed to publish to. Closed (DEC-017, TASK-037): images publish to GHCR, tagged with the release version and `latest`, pushed by CI on every version-tag push. | closed | No — closed |
| OQ-087 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-019)* | Release artifacts were unsigned — a SHA-256 checksums file shipped, but no code-signing certificate, no GPG-signed checksums, and no cosign/sigstore signature on the container image, adding OS Gatekeeper/SmartScreen friction to SYS-131's ≤30-minute quickstart and weakening supply-chain posture. Closed (DEC-019, TASK-037): the container image and checksums file are signed keyless with sigstore/cosign in CI; macOS notarization and Windows Authenticode remain deliberately deferred (cost, no adopter demand). | closed | No — closed |
| OQ-088 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-018)* | Support window / backport policy across release lines was undecided — an interim default (latest release only, best-effort) documented the current stance without founder confirmation. Closed (DEC-018): confirmed as the standing 0.x policy, no multi-version backport/LTS commitment. Revisit if/when a club runs an older line in production. | closed | No — closed |
| OQ-089 *(TASK-028, 2026-07-14; decided 2026-08-03, DEC-025; closed TASK-042, 2026-08-05)* | Logged-in non-organizer accounts landed on a content-free hub home page (the TASK-005 landing-page placeholder) — only `meet_organizer`+ sessions had a workspace to point to; office/field-official/entry-submitter sessions worked from out-of-band links only. Confirm whether a personal "my assignments" dashboard is wanted. Closed (DEC-025, TASK-042): a role-specific dashboard panel shipped for every session below the organizer floor, scoped per role. | closed | **Closed** — the dashboard is shipped; OQ-110–OQ-112 record narrower follow-on questions this task's own scope call raised |
| OQ-090 *(TASK-036, 2026-08-04)* | **French-locale TAF3 unranked-standings marker text is unverified.** The LV Langenthal Gesamtrangliste evidence (DEC-016/OQ-020) is German-language TAF3 output ("aufg."/"n.a."); no French-locale UBS Kids Cup individual Dreikampf Rangliste could be located to confirm the equivalent abbreviations. The shipped `standings.unranked_missing`/`standings.unranked_out_of_competition` FR strings ("ab."/"h.c.") are a reasoned assumption — standard French sporting abbreviations for "abandon" and "hors concours" respectively, the latter corroborated by a UBS Kids Cup team-relay result list's own legend ("A = Hors concurrence", https://www.ubs-kidscup.ch/fileadmin/ubskidscup/ubs_kids_cup_team/Ranglisten/Rangliste_Yverdon.pdf) — not a literal citation from the individual-Dreikampf French output. Obtain a French-locale TAF3 UBS Kids Cup Gesamtrangliste and diff the exact abbreviations. | Founder / tech lead | No — a values-only i18n string correction (`internal/web/i18n/locales/fr.json`) if the real abbreviations differ |
| OQ-091 *(TASK-036, 2026-08-04; decided DEC-032, 2026-08-07; RESOLVED 2026-08-07, TASK-052)* | The out-of-competition participant flag (OQ-020 evidence) had a complete service-layer mechanism but no operator-facing UI toggle — reachable only via a direct service call. Resolved (DEC-032, TASK-052): a roster-row toggle shipped, version-guarded against concurrent edits, same office gate as other roster mutations. | closed | No — closed |
| OQ-094 *(TASK-035, 2026-08-03; scheduled 0.2, DEC-040)* | The public-results render cache (DEC-015/TASK-035) invalidates on the results-changed bus event (consent withdrawal is wired), but meet edits and privacy erasure/retention-purge are not — they rely on the cache's bounded 5s TTL as a safety net instead. Whether that TTL is an acceptable permanent answer, or whether the invalidation hook should extend to erasure/purge, is a deliberate design call. Scheduled for TASK-066 (DEC-040). | Founder / tech lead | No — rare, deliberate operator actions with no measured incident; the TTL bounds the exposure window |
| OQ-101 *(TASK-040, 2026-08-05)* | The OQ-036/DEC-024 field-correction fix substitutes the corrected mark as the athlete's sole ranking input, losing D5.6's full next-best-mark tie-break vector against any other, uncorrected athlete they end up exactly tied with — a rare double coincidence. Confirm whether this residual edge case needs a real fix (an explicit ordered mark sequence, not just a single corrected-mark field) or is an acceptable simplification. | Founder / tech lead | No — the common case is correct; the narrow tie-break gap is a documented simplification, not silently wrong data |
| OQ-102 *(TASK-040, 2026-08-05)* | Vertical-jump corrections can now earn a record/best flag (TASK-040 widened the family gate), but normal vertical capture never evaluates records at all (OQ-046's scope gap) — an inconsistency where a correction can surface a flag a normal capture of the identical height never would. Confirm whether corrections should skip vertical record evaluation too, or whether this stands as the first slice of vertical record support. | Founder / tech lead | No — either answer is a small, self-contained change |
| OQ-104 *(TASK-039, 2026-08-05)* | **The online-entry licence-number field (DEC-023) validates only a deliberately permissive well-formedness pattern (`domain.ValidLicenceNo`: Unicode letters/digits/space/`.`/`-`/`/`, 1–40 chars), not the real Swiss Athletics licence-number format.** This mirrors OQ-030's status — the real Alabus/Swiss Athletics licence-number shape is still unverified — so inventing a strict pattern (e.g. a fixed digit count) risked rejecting real licence numbers the CSV import path (which imposes no format at all) would happily accept. Tighten `licenceNoPattern` once a verified format is available (same data-only-swap posture as OQ-030's `alabus.json`); revisit together. | Founder / Swiss Athletics or club contact | No — a values-only regex tightening, no code-shape change |
| OQ-105 *(TASK-039, 2026-08-05; scheduled 0.2, DEC-040)* | A supplied licence number triggers natural-key athlete matching on the online-entry path, gated on the licence field being non-empty — a bare submission with no licence still always creates a new athlete, deliberately narrower than the CSV import path's unconditional dedup (a public self-service form risks silently merging two different people who happen to share name/birth-year/sex). Confirm whether unconditional dedup is wanted for a later release. Scheduled for TASK-065 (DEC-040). | Founder / tech lead | No — the licence-gated behaviour is the more conservative default; broadening it is additive |
| OQ-106 *(TASK-039, 2026-08-05)* | The relay entry form (UC-003 #4) did not gain a licence-number field — DEC-023 scoped it to "individual/bulk" only, and relay legs are typically club-roster youth athletes without an individual licence. If A/B-tier relay entries need per-leg licence capture, extending the relay leg input with the same optional field is a small, additive follow-up. | Founder / tech lead | No — additive; today's relay entries at a licence-required tier get the same behaviour they had before TASK-039 |
| OQ-110 *(TASK-042, 2026-08-05)* | The "my assignments" dashboard's competition-office panel (DEC-025) reads as "its meets" but SYS-090 gives that role no per-meet scoping at all — unlike a field official, no office account is ever "assigned" to a specific meet, so it sees every meet in the instance. This faithfully matches today's coarse authorization model but is weaker personalization than the field-official/entry-submitter panels. Confirm whether real per-meet office scoping is wanted for a later release. | Founder / tech lead | No — matches the existing, tested authorization model exactly; narrowing it later is additive |
| OQ-111 *(TASK-042, 2026-08-05; closed TASK-043, 2026-08-06)* | Building the office dashboard panel surfaced a pre-existing dead end: a competition-office session had no working link into most of its own event-scoped surfaces — the only "back to meet" link targeted the organizer-gated meet-detail page, so an office session clicking it got a 403. Closed (TASK-043): the meet-detail hub reopens to the competition-office floor, capability-filtered so organizer-only actions stay hidden and gated. | closed | **Closed** — the office-appropriate hub is shipped as a capability-filtered `/meets/{id}`, not a separate surface |
| OQ-112 *(TASK-042, 2026-08-05)* | The "my assignments" dashboard resolves its content with one store round-trip per candidate meet rather than a single join query — matches A-002's MVP scale but would not scale to an account holding assignments across dozens of meets. Revisit with a batched query if a real account ever approaches that scale. | Founder / tech lead | No — bounded by the same MVP meet-count assumption every other list surface already relies on |
| OQ-113 *(TASK-043, 2026-08-06; closed TASK-046, 2026-08-06)* | Even with the meet-detail hub office-reachable (TASK-043), it still had no link into result capture or reconciliation — result entry is the office role's highest-frequency action, yet the only way in was a typed URL or the field-official's own dashboard panel. Closed (TASK-046, SYS-151/UC-041 #1): the hub gained a direct capture-index link for both organizer and office; the office dashboard panel gained direct links into capture and reconciliation. | closed | **Closed** — both the hub's capture-index link and the office dashboard's capture/reconciliation links are shipped |

| OQ-116 *(volunteer usability walkthrough, 2026-08-06; decided DEC-030, 2026-08-07; RESOLVED, TASK-053)* | Volunteer account onboarding and credential recovery had no product path — no password reset of any kind, a locked-out volunteer required account recreation. Resolved (DEC-030, TASK-053): account recovery ships as an admin-issued one-time password reset (audited, no email infrastructure). Self-service email reset stays out. | closed | No for 0.1 originally (workaround: recreate the account); now closed |
| OQ-117 *(volunteer usability walkthrough, 2026-08-06; closed DEC-031, 2026-08-07)* | Check-in on roster-seeded (template) meets showed a confusing empty state (F7): the event has rostered athletes on every capture page, but check-in still shows "no entries" plus a destructive close action. Resolved (DEC-031): current semantics confirmed as intended — check-in tracks online entries only; roster-seeded meets show the honest empty state with capture as next step. | closed | No — capture works; the visible-but-empty check-in was confusing but not blocking |
| OQ-118 *(volunteer usability walkthrough, 2026-08-06)* | **Capture-station packaging and outdoor legibility:** there is no web-app manifest (operator phones cannot install the capture surface as a home-screen/full-screen app; the service worker exists for offline sync), no dark scheme, and outdoor/high-brightness legibility is only required for public surfaces (SYS-113) — operator surfaces have no explicit contrast/legibility target beyond WCAG AA inherited via the design system. Decide whether a PWA manifest and an explicit operator outdoor-legibility target should join SYS-147's scope (0.x) or stay Later. See finding F12. | Founder | No |
| OQ-119 *(TASK-044, 2026-08-06; scheduled 0.2, DEC-040)* | A sync 403 caused by a revoked per-unit assignment shows the same "please re-authenticate, log in" banner SYS-149 groups all authorization/authentication failures under (UC-040 #4 only specifies the session-expiry case), as a genuinely expired session, even though logging back in does not fix it — the real fix is the office restoring the assignment. Decide whether the 403 branch should special-case `unit_not_assigned` with a distinct message. Scheduled for TASK-065 (DEC-040). | Founder / tech lead | No — the current banner is truthful and unblocks the operator to seek help; imprecise about *which* help, not wrong |
| OQ-125 *(TASK-048, 2026-08-06; scheduled 0.2, DEC-040)* | SYS-153/UC-042's per-category jump nav was scoped to groupings each public page already renders (results by division, heat sheets by discipline+category) but not to the start-list page's flat roster table, which has no category column to group by — a real gap for a sizeable template meet's roster (finding F9). The row filter already covers that table; only the jump-nav grouping is missing. Confirm whether the flat roster should also get category-derived jump-nav sections. Scheduled for TASK-066 (DEC-040). | Founder / tech lead | No — the filter already lets a visitor find one name/bib/club; only "browse by category" is missing |
| OQ-128 *(TASK-049, 2026-08-06; scheduled 0.2, DEC-040)* | The new identity-correction form's bib-format check (digits-only, SYS-150 names "bib" among the correctable fields but specifies no format) is new and asymmetric — no bib-format rule existed anywhere else (registration/manual-assignment paths accept any non-empty string), so a non-numeric bib set at registration would become uneditable through the correction form. Decide: extend the digits-only check repo-wide, relax the correction form to match the permissive default, or confirm digits-only as the intended rule. Scheduled for TASK-065 (DEC-040). | Founder / tech lead | No — no fixture or observed meet uses a non-numeric bib; the inconsistency is real but latent |
| OQ-131 *(TASK-045, 2026-08-06; scheduled 0.2, DEC-040)* | The vertical-jump grid's mobile pattern (sticky bib/name column, height columns scroll within their own container) still requires an inner horizontal scroll to reach a later height, unlike the row-card layout the field-horizontal/track grids get — SYS-147/UC-039 #1's page-level no-sideways-scroll assertion is satisfied either way, but a "current height" mobile view would be a materially bigger UI change. Confirm whether the sticky-column trade-off is acceptable as the permanent pattern. Scheduled for TASK-063 (DEC-040). | Founder / tech lead | No — the page-level scrollWidth requirement is met; the inner-scroll trade-off is documented, not silently shipped |
| OQ-132 *(TASK-045, 2026-08-06)* | SYS-148's row Result/Points update-in-place (UC-039 #4) is wired only for the field-horizontal grid — the only capture family with an asynchronous/offline save path today. Track and vertical-jump stay synchronous POST-redirect-GET (no staleness defect, but no cell-level pending/confirmed feedback either). Confirm whether track/vertical should eventually gain the same offline-tolerant async save path, or whether the synchronous pattern is an acceptable permanent difference. | Founder / tech lead | No — no defect exists for track/vertical today; this is a scope decision, not a fix |
| OQ-133 *(TASK-045, 2026-08-06; decided DEC-032, 2026-08-07; RESOLVED 2026-08-07, TASK-052)* | The letter-marker quick-action buttons (TASK-045's F2 fix) rendered unconditionally at every viewport width, adding minor density to an already-dense desktop table. Resolved (DEC-032, TASK-052): hidden above the phone breakpoint; behavior at ≤480px unchanged. | closed | No — closed |
| OQ-134 *(TASK-047, 2026-08-06; scheduled 0.2, DEC-040)* | The new confirm-sub-page "inapplicable" state (SYS-152/UC-041 #4) only covers one staleness cause (candidate count dropping to zero); other stale-precondition causes (unit announced mid-flow, wrong discipline family) still fall through to an older flash-error redirect — both fail safely, just inconsistently. Confirm whether every stale-precondition cause should converge on the one `Inapplicable` shape. Scheduled for TASK-065 (DEC-040). | Founder / tech lead | No — every stale-precondition path already fails safely; this is a UI-consistency question, not a correctness gap |
| OQ-135 *(TASK-047, 2026-08-06; decided DEC-032, 2026-08-07; RESOLVED 2026-08-07, TASK-052)* | SYS-152's empty-state-must-explain-why rule was audited and fixed on two surfaces (check-in, capture standings); the seeding table, reconciliation queue, officials matrix, and timing-import log were not re-audited. Resolved (DEC-032, TASK-052): all four surfaces swept with a reason-plus-next-step empty state, DE/FR complete. Residual gap found, not fixed: OQ-140. | closed | No — closed |
| OQ-143 *(TASK-053, 2026-08-07; closed DEC-034, 2026-08-07)* | DEC-030's decision text says "organizer/admin sets a temporary password," but the implemented reset gate is instance-admin only, matching the existing accounts-administration surface exactly. Confirm whether `meet_organizer` should also reset passwords, or whether instance-admin-only is permanent. Resolved (DEC-034): instance-admin-only is the permanent shape; an instance needing another reset-capable person promotes a trusted organizer. | closed | No — closed |
| OQ-137 *(TASK-050, 2026-08-06; deferred DEC-029, 2026-08-07)* | There is no per-meet timezone field in the data model — operator timestamps now render in the server's OS-local zone rather than raw UTC (F6), which is correct for today's single-hub Swiss deployment but has no explicit per-meet zone to fall back on if the server is misconfigured or a future multi-region hub serves meets across zones. Confirm whether an explicit per-meet timezone field is worth adding now. Deferred (DEC-029), stays open. | Founder / tech lead | No — single-hub, single-country deployment makes server-zone-is-meet-zone true by construction today |

| OQ-140 *(TASK-052, 2026-08-07; decided DEC-033, 2026-08-07; RESOLVED 2026-08-07, TASK-054)* | The seeding-generate handler discarded `GenerateHeats`' error on redirect, so a rejected generate (e.g. empty confirmed/checked-in pool) rendered the same unchanged empty state, with no indication anything failed. Resolved (DEC-033, TASK-054): the handler now checks the error and re-renders with a localized, actionable flash message; the success path is unchanged. Two other seeding handlers with the same pre-existing gap are tracked as OQ-146. | closed | No — closed |
| OQ-146 *(TASK-054, 2026-08-07; decided DEC-035, 2026-08-07; RESOLVED 2026-08-07, TASK-055)* | Three more seeding handlers (override, advance round, manual advance) discarded their service-call errors the same way `handleSeedingGenerate` did before OQ-140's fix, out of that task's scope. Resolved (DEC-035, TASK-055): all three now check their service call's error and re-render with a localized, actionable flash message distinguishing each known rejection; success paths unchanged. | closed | No — closed |
| OQ-160 *(live-instance review, 2026-08-07; decided DEC-039, 2026-08-07; RESOLVED 2026-08-08, TASK-059)* | Static assets were served with no caching headers at all (verified against a production instance), causing browsers to apply heuristic caching with no way to revalidate — cutting against SYS-081's weak-venue-link tolerance posture, and observed in production as stale UI after an upgrade (an operator's browser kept rendering the previous stylesheet until a hard refresh, a real mid-meet hazard). Resolved (DEC-039, TASK-059). | closed | No — closed |
| OQ-156 *(live-instance review, 2026-08-07; decided DEC-042, 2026-08-08)* | Native `datetime-local`/date inputs render the browser's own locale pattern, not the app locale (observed: a US date placeholder inside the German UI). HTML gives the page no control over a native date/time control's display format; the value wire format is unaffected. Resolved (DEC-042): accept and document — the native controls stay, no hint text or replacement fields. | closed | No — closed |
| OQ-153 *(TASK-057, 2026-08-07; decided DEC-038, 2026-08-07 — remaining forms folded into TASK-058; RESOLVED 2026-08-07, TASK-058)* | The DEC-037 stacked-layout form convention was applied only to the worst-case forms TASK-057 targeted (login, setup, change-password, meet form, seeding forms) — entries, timing-import, reconciliation, officials, and import forms were left on the pre-DEC-037 inline layout. Confirm whether a follow-up sweep is wanted. Resolved (DEC-038, TASK-058): entries and timing/entries-import forms swept; the remaining surfaces (bulk-entry table, relay-composition sub-form, timing-agents form, reconciliation/officials pages) turned out to be genuine exceptions (dense multi-field tables or button-only pages), not remaining scope. | closed | No — closed |
| OQ-168 *(TASK-070, 2026-08-08)* | The new traceability gate (`scripts/check-trace.sh`, DEC-044) found SYS-133 and SYS-145 carry a bare, never-backfilled "Phase B" Test cell in the traceability matrix with no deferred marker — their inspection-only verification (paid-service-free self-hosting; ADR-inventory-current architecture) has no dated per-release evidence artifact behind it, unlike other inspection-method SYS rows (e.g. SYS-132, SYS-146). Confirm whether these two should gain a dated per-release inspection checklist/artifact reference, or whether a bare "Phase B" cell is an accepted permanent placeholder for evergreen structural inspections that have no per-release deliverable to point to. | Founder / tech lead | No — mechanically exempted in `scripts/check-trace.sh` pending resolution; both requirements are met in practice (no paid dependency exists, architecture docs are current), only the dated-evidence pointer is missing |

## 8. Founder answers (received 2026-08-03) and resulting decisions

Guided walkthrough of the remaining category-1 (founder/tech-lead) items from
`docs/delivery/oq-triage-2026-07-15.md`; each answer converted into a decision **DEC-###**
(numbering continues from DEC-014). Category-2a/2b items (external contacts required) remain open.

| OQ | Founder decision (2026-08-03) | Decision |
|----|-------------------------------|----------|
| OQ-066 | Read-path fix = per-meet render cache + pooled readers | **DEC-015:** Public-results read path = **per-meet cached render of the public results page/fragment, invalidated by the existing results-changed bus event, plus a pooled WAL read connection set**. Single-WRITER invariant (ADR-004) untouched. Record as an ADR-004 amendment; `TestSYS122TwoThousandConcurrentViewers` flips green when the follow-up task lands. Scheduled as **TASK-035**. |
| OQ-020 | Match the official UKC convention, including the 1-point floor | **DEC-016:** **Final** standings follow the official TAF3 convention — athletes without all series disciplines are listed **unranked at the bottom** ("aufg."/"n.a."-style), and a present-but-no-valid-attempt discipline scores the **1-point floor**; **live** standings keep rank-by-partial-total, labeled **provisional**. Requires a final-vs-provisional distinction in `Standings` and a UC-033 #3 wording amendment. Scheduled as **TASK-036**. |
| OQ-086 | GHCR | **DEC-017:** Container images publish to **GHCR** (`ghcr.io`), pushed from CI on release tags. Values-only edits to `docs/ops/release-process.md` §5 and `docs/ops/quickstart.md` §1 Option B, plus a push step in the release workflow. Scheduled as **TASK-037**. |
| OQ-088 | Latest-only, best-effort | **DEC-018:** Support window = **latest release only, best-effort** (the interim default in `docs/ops/release-process.md` §6 is confirmed as the real 0.x policy). Revisit if/when a club runs an older line in production. Scheduled as **TASK-037**. |
| OQ-087 | Cosign keyless in CI; defer OS-level signing | **DEC-019:** Sign the **container image and checksums file with sigstore/cosign keyless** in CI. macOS notarization and Windows Authenticode are **deferred** (cost/no adopters); document the Gatekeeper/SmartScreen workaround in the quickstart. Scheduled as **TASK-037**. |
| OQ-027, OQ-032, OQ-049, OQ-057 | Relays stay roster-only in 0.1 | **DEC-020:** Release 0.1 ships relays as **entries/roster-only** (teams entered, composed, bib-assigned; no result capture, timing import, per-leg eligibility, or entry standards). Post-0.1: **one bundled relay task** covering manual capture, FinishLynx roster convention, per-leg eligibility, entry standards, and the OQ-059 relay-scoped erasure test — instead of five scattered gaps. All extensions were verified additive. Logged in the work-breakdown *Later* table; TASK-scheduled when triggered. |
| OQ-067 | Ship entry search in 0.1 | **DEC-021:** A **server-side entry/roster search (query-param filter)** is a release-0.1 feature; add the reserved SYS-120 benchmark sub-test when it lands. Scheduled as **TASK-038**. |
| OQ-061 | Neutral default for 0.1 | **DEC-022:** 0.1 ships on the **neutral design-token set**; brand identity (logo, colors, wordmark) is decided later — a token-level data change, timed for when there is a PoC demo audience. |
| OQ-033 (+ OQ-025, OQ-026) | Licence field yes; sign-up and finer caps deferred | **DEC-023:** The online-entry form gains an **optional licence-number field** (closes the A/B-tier operator round-trip). Self-service submitter sign-up (OQ-025) and per-club/per-athlete caps (OQ-026) stay **deferred** — organizers provision accounts out of band; the per-event cap remains the shipped granularity. Licence field scheduled as **TASK-039**. |
| OQ-036 (+ OQ-037, OQ-038, OQ-039) | Field-correction UI yes; the rest deferred/closed | **DEC-024:** The capture page gains a **field-event correction UI** (settled-level, matching the track form); attempt-level resumable corrections stay later. Ticking protest clock (OQ-037) and structured jury/protest entity (OQ-039) **deferred**. OQ-038 **closed**: the FinishLynx `.lif` import is the sole reaction-time source; no manual field. Correction UI shipped by **TASK-040** (both horizontal and vertical field rows; see OQ-036's resolution note and OQ-101/OQ-102). |
| OQ-070, OQ-089 (+ OQ-046, OQ-077) | Bulk DNS and assignments dashboard yes; the rest deferred | **DEC-025:** In scope: a **"mark all remaining as DNS" bulk action** on a still-open unit (second real SYS-114 bulk operation), and a **"my assignments" landing dashboard** for non-organizer roles (field official / competition office / entry submitter see the meets/units they are scoped to). Deferred: offline vertical-jump capture (OQ-046 — ask the pilot club whether HJ/PV officials ever roam beyond hub Wi-Fi before building) and in-flight submit feedback (OQ-077). Scheduled as **TASK-041** (bulk DNS) and **TASK-042** (dashboard). |
| OQ-041, OQ-043 | Confirm both shipped defaults | **DEC-026:** Privacy defaults **confirmed as intended**: publication-withdrawal suppression applies at **any age** (not minors only), and the sole suppression mode is **"neutralize"** (row kept, name/club blanked) — the more protective reading in small divisions. Both closed; escalate to counsel only if challenged. OQ-040 (18 threshold) and OQ-042 (90-day retention) remain open pending legal/federation input (category 2b). |

## 9. Founder answers (received 2026-08-07) and resulting decisions

Release-0.1 tag triage: ratification of the M4-proposed requirement set, repository
visibility, and disposition of every open OQ (numbering continues from DEC-026).
The category-2 external-evidence items (OQ-013, OQ-014, OQ-030, OQ-090, OQ-104) remain
open with the founder as owner — no decision is possible without the external input.

| OQ / item | Founder decision (2026-08-07) | Decision |
|----|-------------------------------|----------|
| STR-046, SYS-147–153, UC-039–043 | Ratify as-is | **DEC-027:** The 2026-08-06 volunteer-walkthrough requirement set is **ratified as baseline** unchanged — all seven SYS plus STR-046 and UC-039–043 carry implementation and test evidence from TASK-044…050. UC-038 #3's zero-open-criticals precondition for the 0.1 tag is now unconditionally satisfied (`../delivery/usability-audit-2026-08-r01.md`). Proposed markers removed across StRS/SyRS/UC/matrix. |
| Repository visibility | Public at tag time | **DEC-028:** The repository goes **public when the v0.1.0 tag is pushed** (same maintainer action window). Rationale: AGPL OSS posture, GHCR/cosign consumption, discoverability, and zero-cost hosted CI minutes. Visibility flip is a maintainer step in `../ops/release-process.md` §3. |
| OQ-046, OQ-052–055, OQ-061, OQ-077, OQ-094, OQ-101, OQ-105, OQ-106, OQ-110, OQ-112, OQ-118, OQ-119, OQ-125, OQ-128, OQ-131, OQ-132, OQ-134, OQ-137 | Defer, none block 0.1 | **DEC-029:** All listed items are **deferred to the post-0.1 backlog** as a block — each is non-blocking (edge case, scale bounded by MVP, documented trade-off, or awaiting demand). They stay open in this register; re-triage when their triggers fire. |
| OQ-116 | Admin-issued reset, first patch release | **DEC-030:** Account recovery ships as an **admin-issued one-time password reset** (organizer/admin sets a temporary password, audited; no email infrastructure — fits the offline-venue posture). Scheduled as **TASK-053** for 0.1.x, post-tag. Self-service email reset stays out (would be new-ADR territory). Until it lands, the runbook's account-recreation path stands. |
| OQ-117 | Current semantics are intended | **DEC-031:** Check-in on roster-seeded meets **stays as-is and the OQ is closed**: check-in tracks online entries; roster-seeded template meets legitimately show the (TASK-047) honest empty state with the capture link as next step. No hiding, no roster listing. |
| OQ-062, OQ-091, OQ-133, OQ-135 | All four into one quick-wins slice | **DEC-032:** One post-tag S slice, **TASK-052**: timing-conflict merge becomes **fill-blanks-only** (imported mark preserved, matching the documented intent — OQ-062); roster-page **out-of-competition toggle** over the complete service layer (OQ-091); **letter-marker quick-buttons hidden above the phone breakpoint** (values-only CSS — OQ-133); **empty-state sweep completed** across seeding, reconciliation, officials and timing-import surfaces (OQ-135). |

## 10. Founder answers (received 2026-08-07, post-merge triage) and resulting decisions

Triage of the two OQs raised while implementing TASK-052/053 (numbering continues from
DEC-032).

| OQ / item | Founder decision (2026-08-07) | Decision |
|----|-------------------------------|----------|
| OQ-140 | Fix now, small slice | **DEC-033:** The seeding-generate handler gets the **error-feedback treatment every other mutating form already has** — a rejected `GenerateHeats` call (e.g. empty confirmed/checked-in entry pool) SHALL surface an actionable message on the seeding page instead of silently redirecting to the unchanged empty state. Scheduled as **TASK-054**. |
| OQ-146 | Fix the same way | **DEC-035:** The remaining three seeding handlers that discard service errors — `handleSeedingOverride`, `handleAdvanceRound`, `handleManualAdvance` — get **the same page-level flash-error treatment TASK-054 gave `handleSeedingGenerate`** (localized actionable message on rejection, success path unchanged). Scheduled as **TASK-055**. |
| OQ-143 | Instance-admin only; promote when needed | **DEC-034:** The password-reset gate **stays instance-admin only (`CapManageAccounts`) as the permanent shape** and the OQ is closed. Password resets are account administration; DEC-030's "organizer/admin" wording is narrowed accordingly. An instance that needs an additional reset-capable person promotes a trusted organizer to instance admin via the existing accounts-page role change — accepted with the caveat that promotion is full (account management, backup, retention purge), not reset-only. No organizer-scoped reset path is planned. |

## 11. Founder answers (received 2026-08-07, branding round) and resulting decisions

Ratification of the brand identity deferred by DEC-022 (OQ-061), decided on the evidence
base in `docs/research/visual-identity-trends.md` (numbering continues from DEC-035).

| OQ / item | Founder decision (2026-08-07) | Decision |
|----|-------------------------------|----------|
| OQ-061 | Teal accent, warm cream neutrals, self-hosted display + tabular type; scope tokens + favicon + wordmark | **DEC-036:** Bahnfrei's brand identity is ratified: (1) **accent hue teal** (target `#0f766e` band with a brighter interactive variant; exact values tuned to WCAG AA contrast) — a hue unclaimed across every surveyed market segment; (2) **warm cream neutral undertone** for surfaces; data surfaces stay light-background per the market convention and SYS-113 outdoor legibility; (3) **typography upgrade to a self-hosted open-source condensed display face with tabular numerals** for headings and result grids — vendored in-repo with its licence text, honouring ADR-002/ADR-003's no-external-fetch constraint; body copy stays the system stack; (4) **scope of this round: token values in `tokens.css`, replacement of the TASK-050 placeholder favicon with a real teal mark, and a typographic wordmark treatment for "Bahnfrei" in the header** — no designed logo yet (that remains a later, additive step). Scheduled as **TASK-056**. |
| Founder feedback (post-TASK-056 review, same evening) | Interactive components look like a 2000s site — buttons, links, menus and forms are unstyled browser defaults | **DEC-037:** The design system gains a **component-chrome and form-layout pass**: (1) real button styles — solid-accent primary, outlined neutral secondary, danger variant for destructive confirms — with hover as a color shift (new accent-hover token), not an opacity fade; (2) links colored with the accent and a deliberate underline convention; header nav styled as navigation (spacing, hover, `aria-current` page indicator) rather than prose links; (3) inputs/selects get consistent token-driven border, radius, background and padding; (4) operator/admin forms adopt stacked label-above-input field groups so label/field pairs never wrap apart — dense capture grids keep their compact row ergonomics and 44 px touch floors untouched. All under SYS-116/STR-045; WCAG AA contrast checks extend to every new pair; focus-ring and keyboard-walk invariants unchanged. Scheduled as **TASK-057**; shipped, with the remaining office forms tracked as OQ-153. |
| Founder direction (live-instance walkthrough, same evening) | Chrome alone is not enough — operator/public pages are paragraphs of middot-separated links, page actions have no hierarchy, status is bold text, the category picker is a raw listbox | **DEC-038:** A **structural pass** follows the chrome pass: (1) **information architecture** — the meet hub, the results-capture index and the public meet pages replace their middot-separated prose-link paragraphs with structured action lists/cards on `--color-surface`, separating navigation from actions; (2) **action hierarchy** — each page's primary action gets `.button-primary` treatment (e.g. creating a meet on the meets list), consequential state changes such as publishing a meet are visually weighted as such, and archive/destructive actions style accordingly; (3) the **status-chip grammar is generalized** — meet status (draft/published/archived) and round/publication states render as discrete chips in the established grey→live→green semantics, never color-alone (SYS-148); (4) the **category multi-select listbox becomes a checkbox grid** (same form field name and semantics as SYS-002's zero-selected rejection, no schema change); (5) the **DEC-037 form-layout sweep is completed** over the remaining office forms — entries (individual/bulk/relay), timing import/agents, reconciliation, officials, import — closing OQ-153. All under SYS-116/STR-045; contrast checks extend to any new pair; capture-grid geometry and keyboard/focus invariants stay untouched. Scheduled as **TASK-058**; shipped as specified, reusing the existing semantic palette (no new color tokens). |
| OQ-160 | Fix now | **DEC-039:** Static assets get a **cache-discipline pass, TASK-059**: asset URLs referenced from templates become **content-fingerprinted** (a hash of the embedded file's bytes, as a version query or filename component) and the static handler serves a correctly-fingerprinted request with `Cache-Control: public, max-age=31536000, immutable`; un-fingerprinted or stale-fingerprint requests serve the current bytes with a revalidation-friendly header (`no-cache`) so nothing breaks for old references. The service-worker script itself is always served revalidatable, and its cache name incorporates the asset fingerprint so a deploy invalidates the previous cache on activation. Shipped as specified (TASK-059). |

## 12. Founder answers (received 2026-08-08, release-0.2 scoping) and resulting decisions

Scope ratification for release 0.2 (per the founder's 2026-08-07 direction there is no
0.1.1 patch; develop accumulates into a larger 0.2). Numbering continues from DEC-039.

| OQ / item | Founder decision (2026-08-08) | Decision |
|----|-------------------------------|----------|
| Release-0.2 scope | All four proposed completeness bundles, plus a professional-UI headline track ("currently it still looks like a student project") and a Docker Compose deployment definition; team scoring waits | **DEC-040:** Release 0.2 is scoped as **professional UI + capture completeness + records self-serve**. (1) **Headline — design-professionalism track.** The founder's standing assessment is that the UI, even after the DEC-036–039 rounds, does not yet read as professional. The track is **evidence-first**: an extensive published-source research round into UI/UX best practices for data-dense operational web applications (design-system references, component/form/table/navigation/typography/spacing patterns, accessibility-compatible polish), documented with citations in `docs/research/` alongside the existing visual-identity and contextual-help rounds, and distilled into (a) product-facing design principles folded into `docs/architecture/design-system.md` and (b) standing working guidance for the development workflow (kept with the workflow tooling, per the documentation-audience rule) — **TASK-060**. A **structured design audit** of every operator and public surface against that evidence follows, producing an enumerated, prioritized polish list for founder triage — **TASK-061**; polish slices then ship as triaged. (2) **Vertical-jump completeness** — offline capture parity (OQ-046) and the mobile sticky-column grid pattern (OQ-131) — **TASK-063**. (3) **Records self-serve** — record/best flags in exchange surfaces (OQ-052), combined-events wind-average flagging (OQ-053), retroactive wind-correction staleness (OQ-055), and an organizer-facing record-list management UI (OQ-054) — **TASK-064**. (4) **Robustness sweep** — OQ-077 (audit polish), OQ-105 (licence-triggered dedup widening), OQ-119 (sync-403 messaging), OQ-128 (bib-format check), OQ-134 (stale-precondition confirm pages) — **TASK-065**. (5) **Public-results polish** — render-cache invalidation on erasure/meet edits (OQ-094) and per-category navigation (OQ-125) — **TASK-066**. **Team/club scoring (UC-029) is explicitly held for 0.3** as its headline candidate. The scheduled OQ rows above are annotated "scheduled 0.2, DEC-040"; each closes through its TASK. Audit findings from TASK-061 may extend this scope through normal founder triage. |
| Container deployment shape | Adopt Docker Compose for the container path, unless research argues otherwise | **DEC-041:** The hub-role container deployment gains a **first-class Docker Compose definition**: a committed `compose.yaml` (pinned GHCR image reference, named data volume, port mapping, `restart: unless-stopped`, documented ACME/hub variant) with the quickstart's container option rewritten around `docker compose up -d` / upgrade via `docker compose pull && docker compose up -d`. Research supports rather than argues against it: Docker's own documentation names **single-host production deployment as a supported Compose use case** ("Why use Compose?", docs.docker.com/compose/intro/features-uses — "Compose supports production deployments on single hosts") and its production guide (docs.docker.com/compose/how-tos/production) prescribes exactly this shape (restart policies, override files for production settings). Compose v2 ships as a plugin with standard Docker Engine installs, so it adds no separate install for anyone already running the container. ADR-002 is unchanged: the single self-contained binary remains the primary volunteer path; Compose only turns the hand-typed multi-flag `docker run` line into a reviewable, versioned artifact. Scheduled as **TASK-062**. |
| OQ-156 | Accept + document only | **DEC-042:** Native `datetime-local`/date inputs keep their browser-locale display format — option (a). The behaviour is documented in plain language in the operator quickstart's meet-creation step (the picker follows the browser's language setting; stored values and validation are locale-independent). No hint text, no replacement fields. OQ-156 closed. |
| Requirements-practice audit | Research how requirements, use cases and decisions are documented per best practice; check conformance; measure how much implementation narration has leaked into the spec documents | **DEC-043:** Release 0.2 gains a **requirements-practice audit** (TASK-067): (1) benchmark the StRS/SyRS/use-case/ADR/register practice against published requirements-engineering guidance (ISO/IEC/IEEE 29148 quality characteristics, use-case authoring conventions, architecture-decision-record conventions, traceability practice), with cited findings in `docs/research/`; (2) audit every committed specification document for content misplaced by audience — implementation reports and status narration inside requirement or decision entries, material that belongs in git history or working notes; (3) propose a remediation plan for founder triage before any restructuring ships. |
| Practice-audit remediation (TASK-067 findings) | Slim the polluted records — narration is derivable from git; adopt ADR supersession; machine-check traceability | **DEC-044:** The TASK-067 audit found the requirement/use-case/ADR core sound but the registers polluted with implementation narration (~60% of this file; traceability-matrix Test cells up to ~750 words). Remediation ratified: (1) **TASK-068** — rewrite this register and the traceability-matrix Test column to terse conformant form: an OQ/DEC entry is question/decision, date, owner, one-line rationale and pointers; a matrix Test cell is test identifiers/paths only; stripped implementation narration is **deleted** — git history is its authoritative record. (2) **TASK-069** — audience fixes: neutral reviewer designations in the two delivery review documents; `docs/delivery/oq-triage-2026-07-15.md` leaves the repository; the glossary gains the project's own coined terms (omx, checkout, reconciliation, walk-by sync, ULID, ASVS); `use-cases.md`'s blanket automatability claim is corrected to admit inspection-verified criteria; design-system rationale cells are trimmed. (3) **TASK-070** — a traceability check joins the merge gate: every SYS SHALL map to at least one verification pointer, and orphans in either direction fail the gate (built after TASK-068 settles the format). (4) **ADRs are immutable once accepted**: a changed decision produces a new ADR marked "Supersedes ADR-###"; existing ADRs stay as they are. Research basis: `docs/research/requirements-engineering-practices.md`. |
