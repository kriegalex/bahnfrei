# Open Questions & Assumptions

**Document status:** DRAFT — updated continuously through Phase A; consolidated at the A6 QA pass.
Open questions are addressed to the **founder** unless another owner is named. Where an answer
is pending, work proceeds on the stated assumption (marked **A-###**); assumptions are
requirements-relevant and traced where used.

## 1. Open questions for the founder (OQ-###)

| ID | Question | Why it matters | Working assumption until answered |
|----|----------|----------------|-----------------------------------|
| OQ-001 | **Scope of "athletics tournaments":** track & field stadium meets only, or also road running, cross-country, trail, and indoor meets? | Determines domain model breadth (courses, transponder timing, mass fields) | A-001 |
| OQ-002 | **Primary initial user:** which meet scale is the MVP target — club/regional meets, or national-championship grade from day one? | Drives MVP cut, performance targets, and feature depth (e.g., call room, jury workflows) | A-002 |
| OQ-003 | **Relationship to Swiss Athletics:** is the goal (a) interoperate with their existing ecosystem, (b) be adoptable by them, or (c) merely learn from it? Is there any existing contact/mandate? | Changes integration requirements from "import/export compatible" to "certified integration" | A-003 |
| OQ-004 | **Open-source licence:** preference (e.g., AGPL-3.0 vs Apache-2.0/MIT) and stance on commercial hosting by third parties? | One-way door; affects contributions, commercial ecosystem, timing-vendor integrations | A-004 |
| OQ-005 | **Governance & funding:** solo-founder project, foundation/association (CH Verein), or company-backed? Any budget for infrastructure/hosting? | Affects sustainability requirements and hosting-model requirements | A-005 |
| OQ-006 | **Hosting model:** self-hosted per organizer, central SaaS instance, or both? Who operates the reference instance? | Drives architecture-relevant NFRs (multi-tenancy, offline sync) — must be answered before Phase B ADRs | A-006 |
| OQ-007 | **Para athletics:** in scope for MVP, later, or out of scope? | Classification/scoring adds real complexity; affects data model early | A-007 |
| OQ-008 | **Languages:** confirm DE/FR/IT/EN all required at launch, or DE/FR first? | i18n effort and translation workflow | A-008 |
| OQ-009 | **Name/brand** for the project? | Repo, docs, domain registration | A-009 |
| OQ-010 | **Existing data/relationships:** does the founder have access to real meet data, a pilot club/meet, or timing hardware for testing? | Validation strategy for Phase B acceptance tests | A-010 |
| OQ-011 | **Alabus entry-data format:** can the founder (or a Swiss Athletics / club contact) obtain a sample Alabus entries export and its format spec? The Alabus marketing site blocks automated access; the TAF3↔Alabus flow is documented only from the Seltec side ([research](../research/competitive-analysis.md#c2-swiss-athletics-as-a-system-of-systems)) | "Interoperate with Swiss Athletics" concretely means importing what Alabus exports; without a sample, that requirement stays format-TBD | A-011 |
| OQ-012 | **Data-standard alignment:** should the project align its exchange schema with the W3C Open Athletics Community Group draft Competition Data Model (and possibly contribute upstream)? No federation mandates it today | Shapes the export/API surface; reversible but strategically visible | A-012 |

## 2. Working assumptions (A-###)

> **Status 2026-07-05:** founder answers received (§4). Confirmed: A-002, A-003, A-005,
> A-010, A-011, A-012. Superseded/updated by decisions: A-001 → DEC-001 (road/XC now **out of
> scope**, indoor stadium stays in), A-004 → DEC-004 (AGPL-3.0 lead candidate), A-006 →
> DEC-006 (homelab-first; desktop-app option to be evaluated in Phase B0), A-007 → DEC-007
> (para **out of MVP**, not merely reduced), A-008 → DEC-008 (DE/FR complete incl. operator
> surfaces; IT/EN later), A-009 → DEC-009 (shortlist in §6).

| ID | Assumption | Basis |
|----|------------|-------|
| A-001 | MVP scope is **outdoor & indoor stadium track & field** (incl. combined events and relays); road/XC/trail are **later**, not out of scope. | Seed says "athletics tournaments"; named parallels (Seltec TAF, Swiss Athletics meets) are stadium-centric |
| A-002 | MVP targets **club → regional/cantonal meets** (≤ ~1000 athletes, 1–3 days), engineered so championship-grade operation is a growth path, not a rewrite. | Realistic OSS adoption path; incumbents are entrenched at national level |
| A-003 | Interoperability stance: **(a) coexist/interoperate** — import licence/entry data, export federation-conformant results; no assumption of official mandate. | No stated mandate in seed |
| A-004 | Licence TBD-owner: founder. Requirements are written licence-neutral but assume **OSI-approved copyleft or permissive** licence, compatible with community contribution. | Seed: "open source" |
| A-005 | Community project with **lightweight governance documented in-repo** (maintainers file, contribution guide); no paid staff assumed. | Seed silent on funding |
| A-006 | **Both** hosting modes are eventually needed; MVP assumes **single-organizer deployment** (one meet at a time) with offline-capable venue operation; multi-tenant SaaS is later. | Venue reality + OSS adoption path; flagged as Phase B ADR gate item |
| A-007 | Para athletics: **data-model-ready in MVP** (sport class on athlete/result), full workflows (RAZA scoring, classification management) later. | Integrated Swiss meets exist; full support is costly |
| A-008 | All four languages **DE/FR/IT/EN** are required for public-facing surfaces at launch; operator/admin surfaces may launch DE/EN first. | CLAUDE.md locale mandate; Swiss federation practice |
| A-009 | Working name **"OpenMeet"** used in docs only; no branding decisions implied. | Placeholder |
| A-010 | No privileged data access assumed; verification uses public formats, synthetic data, and published vendor file-format specs. | Conservative default |
| A-011 | Until an Alabus sample is available, Swiss entry import is specified against a **generic documented CSV mapping** plus the publicly documented TAF3 import behaviour (categories/disciplines only; rounds & schedule created in-system). | [Seltec wiki on the Swiss entries flow](../research/competitive-analysis.md#c2-swiss-athletics-as-a-system-of-systems) |
| A-012 | The system's exchange schema is **self-defined, versioned, and openly documented**, with the W3C Open Athletics draft used as a design reference (not a compliance target). | It is a community-group draft, not adopted by any federation studied |

## 3. TBD register

Items marked TBD elsewhere in the baseline are consolidated here at the A6 pass.

| ID | Item | Owner | Blocking? |
|----|------|-------|-----------|
| TBD-001 | Historical IAAF/World Athletics XML result format — no primary spec located; needed only if federation submission requires it | Research (Phase B, on demand) | No |
| TBD-002 | Seltec user-reported limitations beyond vendor docs (forum review out of scope in first pass) | Research (optional) | No |
| TBD-003 | Para-athletics support level in incumbent systems — unverified either way; affects differentiation claim strength, not requirements | Research (optional) | No |
| TBD-004 | Exact Alabus feature set / export format (see OQ-011) | Founder / federation contact | Blocks final Swiss-entry-import spec detail, not the baseline |
| TBD-005 | SVM (Schweizer Vereinsmeisterschaft) point table & nationality quotas — only secondarily sourced; primary SVM Reglement PDF fetch failed | Research (before any SYS with exact point values) | Blocks exact team-scoring values only; scheme is specified as configurable |
| TBD-006 | "RAZA" para scoring — could not be verified to exist under that name; no requirement is built on it | Research (only if para scoring becomes MVP) | No |
| TBD-007 | World Athletics results-submission format/API — appears federation-mediated, no public API found | Founder / Swiss Athletics contact | No — result export is specified as file-based |
| TBD-008 | Track tie-break rule text (CR&TR Rule 26) and field-event flight-size thresholds — not extracted verbatim yet. *Update 2026-07-15:* the full WA CR&TR 2026-edition PDF now extracts cleanly with `pdftotext` (primary URL in `internal/domain/data/seeding/tr20.json`); the TR 26 placings text was read during the OQ-044 pass, so extraction is on-demand when ranking logic needs it | Research (Phase B, on demand) | No |
| TBD-009 | Swiss licence fee amounts (CHF 60/120 secondary only) and Gebührenreglement details | Research (only if fee handling in scope) | No |

## 4. Founder answers (received 2026-07-05) and resulting decisions

Founder answers verbatim (lightly formatted), each converted into a decision **DEC-###** that
updates the corresponding assumption. Requirement deltas were applied to the baseline on
2026-07-05 (see §5).

| OQ | Founder answer (verbatim) | Decision |
|----|---------------------------|----------|
| OQ-001 | "track & field stadium meets only" | **DEC-001:** Scope = stadium track & field only. Road/XC/trail move from *Later* to **out of scope** until revisited. |
| OQ-002 | "club/regional meets, the goal is to help small clubs with no IT budget. big events certainly have more budget and more tools" | **DEC-002:** MVP optimizes for **small volunteer-run clubs with zero IT budget**; championship-grade is a growth path, not a target. Youth-series meets (UBS Kids Cup style) are a canonical MVP scenario. |
| OQ-003 | "the goal is to learn what kind of constraints (input / output) systems from swiss athletics and Seltec TAF3 would put on our system. Currently, some timings are done via electronic systems (track running / sprinting), but other are taken by humans on the field (ball throw, long jump, etc...) and entered manually into a system (unknown for now). the list of participants with their number is managed in Seltec (I saw the printed sheets)" | **DEC-003:** Interop stance = **learn & coexist**: treat Seltec/Swiss Athletics as fixed environment constraints (import/export at their boundaries), not integration partners. Verified pipeline in [research C7](../research/competitive-analysis.md#c7-field-observations--live-results-pipeline-ubs-kids-cup-le-mouret-4-july-2026). |
| OQ-004 | "I want a license that allows adoption and improvement by clubs, but not for a private company to easily make money off of this project" | **DEC-004:** Licence direction = **strong copyleft; AGPL-3.0 is the leading candidate** (closes the SaaS loophole). Note: no OSI licence can prohibit commercial use outright; AGPL makes proprietary capture unattractive. Final choice = ADR-001 at the Phase B gate. |
| OQ-005 | "solo for now, TBD if adopted by clubs with small club sponsoring it" | **DEC-005:** Solo-founder governance; keep in-repo governance lightweight; revisit if clubs adopt. |
| OQ-006 | "homelab hosted for now. later, if a club wants to use it, it may need to live in the cloud, but with modest budgets. maybe we should investigate if a desktop offline app is even possible and matches the constraints" | **DEC-006:** Deployment target #1 = **founder homelab / single self-hosted instance**; cloud later at modest cost. **Phase B0 must evaluate a desktop/offline-first app vs. self-hosted web app as an explicit ADR with trade-offs** (ADR-002 candidate). |
| OQ-007 | "out of scope for now, local meetings are not para-athletic" | **DEC-007:** Para athletics = **out of scope for MVP** (kept data-model-friendly: adding a class field later must not require redesign). Mixed **age-category** fields remain MVP (youth series need them). |
| OQ-008 | "DE/FR, but allow for easy internationalization" | **DEC-008:** Launch languages = **DE + FR complete** (public *and* operator surfaces); i18n architecture must make adding IT/EN a translation task, not an engineering task. |
| OQ-009 | "I let you brainstorm something and check against existing offerings. Core idea: athletics, switzerland, small clubs hosting events with volunteers" | **DEC-009 (RESOLVED 2026-07-05):** Name = **Bahnfrei** (founder pick from shortlist §6). Name/logo rights held personally by the founder per ADR-001 §5. |
| — *(gate record)* | Founder, 2026-07-05: "I ratify all six ADRs as proposed, keep the name Bahnfrei" | **DEC-014: Phase B gate 2 PASSED.** ADR-001 (AGPL-3.0-only + DCO), ADR-002 v2 (hub-first), ADR-003 (Go/SQLite/HTMX), ADR-004 v2 (storage + capture queue), ADR-005 (domain model + omx/v1 + external IDs), ADR-006 (FinishLynx-first + timing agent) — all **Accepted**. Phase B implementation is unblocked; backlog in `docs/delivery/work-breakdown.md`. |
| OQ-010 | "no access for now, could be asked, but probably not or with a lot of convincing (GDPR, anonymization, ...)" | **DEC-010:** Verification uses **synthetic data only**; the public LA.portal result pages (already-published data) may be used as structural reference. |
| OQ-011 | "absolutely no idea. if I have a PoC to show to the local clubs, I may obtain something" | **DEC-011:** **PoC-first strategy**: build against the documented generic CSV mapping (A-011); an Alabus sample becomes an ask once a PoC exists to show clubs. |
| OQ-012 | "is this gonna become mainstream? also in switzerland? I guess that if we must choose something from scratch, we can align with W3C Open Athletics Community Group" | **DEC-012:** Honest answer: **no evidence it will become mainstream** — it is a community-group draft no federation has adopted (C3). Use it as the design reference where we define schemas from scratch (per A-012); do not promise conformance. |
| OQ-016 *(founder direction, 2026-07-05, ADR-002 ratification discussion)* | "we will be expecting some kind of online connectivity to a central hub. the system should be however tolerant to LTE and WIFI issues and not stop working at the first network issue in the meet timing room or out in the field […] should not cost a fortune to deploy, as the target is small clubs […] my goal is not to have a SaaS page with subscriptions […] I build the open source foundation and they use the stack and host the stack (made as easy as possible by us) on their own" | **DEC-013: hub-first connectivity posture.** Expected = connectivity to a central **self-hosted** hub; MVP = interruption tolerance (SYS-085–087, UC-034); full no-internet venue operation deferred behind an evidence gate (SYS-080 → *Later*, UC-019); **field-wide venue Wi-Fi = non-goal** (research C8: club-tier incumbents all use officials' phones + LTE via cloud relay); deployment cost sized for small clubs; **no subscription-SaaS business** — clubs self-host. Re-scopes ADR-002 (v2) and ADR-004 (v2); adds Swiss-chain interop SYS-077/078 (C2.1). |

## 5. Requirement deltas applied from the answers (2026-07-05)

| Change | Where |
|--------|-------|
| Para: STR-030 re-scoped to mixed **age-category** fields (MVP); para sport classes → out of MVP scope (Later) | StRS §3.5/§4, SYS-052, UC-028 |
| Languages: DE/FR complete at launch (public + operator); IT/EN → Later, must be translation-only effort | STR-033, SYS-110, UC-025 |
| Road/XC/trail from Later → out of scope | StRS §4 |
| NEW STR-042: public results labeled unofficial; federation channel remains the official source of truth for sanctioned meets | StRS §3.4, SYS-076, UC-017 |
| NEW STR-043: Swiss youth-series formats (UBS Kids Cup first) with series-specific scoring tables | StRS §3.3, SYS-053, UC-033 |
| Licence direction (AGPL-lead) recorded as constraint note | SyRS CON-02 |
| Desktop-app-vs-web evaluation mandated for Phase B0 | DEC-006 → ADR-002 candidate |

## 6. Project name shortlist (DEC-009, for founder pick)

Criteria: works across DE/FR/IT/EN, evokes athletics + grassroots volunteering, no collision
found with existing athletics software (checked 2026-07-05 via web search; **domain/trademark
checks still TBD before adoption**).

| Candidate | Reading | Notes |
|-----------|---------|-------|
| **Bahnfrei** | DE starter's call "Bahn frei!" (track clear!) | Distinctive, sympathetic, DE-leaning; understandable in CH; no software collision found |
| **PisteLibre** | FR mirror of the same idea | FR-leaning twin; could pair as bilingual brand "Bahnfrei / PisteLibre" |
| **OpenLane** | EN, "the open lane" | Neutral EN; collision risk with US automotive marketplace "OPENLANE" in other domain — likely acceptable, verify trademark class |
| **Startklar** | DE/CH "ready to start" | Warm, volunteer-flavored; common word → weak trademark, domains likely taken |
| **Anlauf** | DE "run-up" (jump approach) + "getting started" | Nice double meaning; DE-only comprehension |

Avoided: anything with "OpenTrack" (existing commercial vendor + W3C group naming collision,
C3/C4), "Athletica" (club-name collisions), "Meet*" generics (HyTek "Meet Manager", "MeetPro").

## 7. New/updated open items from this round

| ID | Item | Owner | Blocking? |
|----|------|-------|-----------|
| OQ-013 | **Club validation of the results pipeline:** confirm with the UBS Kids Cup host club: (a) results were entered in TAF3 on-site (in the results room), (b) live push used Liveserver/LA.portal, (c) whether a club may additionally publish results elsewhere (any federation policy against it), (d) how manual field measures reached TAF3 (paper slips → typed in?) | Founder (has club contact from the meet) | Blocks STR-042 fine-tuning and the official-channel strategy, not the baseline |
| TBD-003 *(updated)* | Para support in incumbents: **partially resolved** — Seltec offers a paid "Para" licence module with para-points support ([research C7.2](../research/competitive-analysis.md)) | closed enough for now | No |
| OQ-014 *(updated 2026-07-05)* | **Official-tier entry (C2.1):** the lock is double — (1) **normative:** WO 2026 §5.6a mandates TAF3 by name for Meisterschafts- und offizielle Wettkämpfe (see OQ-015 resolution), so entering this tier requires Swiss Athletics to amend/waive an association rule it can change annually; (2) **technical:** results must reach Alabus same-day and the only upload tooling is inside TAF3. The eventual ask to Swiss Athletics (once a PoC has club traction, per DEC-011) is therefore *recognition as an additional permitted system + a documented upload path* — precedents: DLV Bestenliste auto-ingests from **two** systems (TAF3 + COSA WIN), Atletiekunie recognises two systems. | Founder → Swiss Athletics Geschäftsstelle | Blocks the **official/championship** tier only; the youth-series/school/club tier has an official Excel path (C2.1) and is not blocked |
| OQ-015 *(RESOLVED 2026-07-05)* | **Does the WO mandate specific software? Yes.** WO 2026 §5.6a: championship and official competitions **must** be administered with TAF3 (provided free by Swiss Athletics); §5.6b mandates activating its LA.portal live publication; §5.3b makes licence-number linking *in TAF3* the only route into the Bestenliste. Note: a federation rule (revisable annually), not state law; World Athletics certifies equipment only, not meet software (C2.1). | closed | No — but it upgrades OQ-014 from "file format ask" to "recognition ask" |
| OQ-017 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | **Swiss WO §1.2 start-up ceilings — verified against the primary WO 2026 PDF** (URL in the scheme file's `source`). §1.1's Startberechtigung column disambiguates D4.2: each youth band admits younger athletes ("U16: 15-jährig und jünger"), Männer/Frauen admits all ("alle Jahrgänge"), each Masters band admits only its own lower bound and older ("M45: 45-jährig und älter") — so the boolean model was already faithful, and masters early start-up is confirmed **not permitted** (the previously unevidenced `false` default). Corrections shipped in `swiss-athletics.json` v`wo2026.2`: M30/W30 gained the missing `startDownAllowed` (per §1.2a masters may start down), and a WO source erratum is documented (the M80/W80 row prints "85-jährig und älter", inconsistent with every other row's pattern). Fixture: `internal/domain` `TestSwissSchemeMastersM30WO11`. | closed | No — closed |
| OQ-018 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | **D4.3 youth discipline bars — the "likely transcription artifact" theory was WRONG; the bar is the literal rule.** Primary WO 2026 §1.5e: "Athletinnen und Athleten der Kategorien U10, U12, U14 und U16 dürfen an von Swiss Athletics bewilligten Wettkämpfen in den Disziplinen 150 m, 200 m, 300 m, 400 m, 300 mH und 400 mH nicht starten" (exceptions: SM Staffel, SVM). Swiss youth up to U16 sprint 60/80/100 m instead (§8.1.1–§8.1.3). `swiss-athletics.json` v`wo2026.2` now encodes all six bars (plus §1.5c steeple bars extended to the newly catalogued 2000mSC/1500mSC); the misleading note is replaced with the verbatim rule. Fixture: `internal/domain` `TestSwissSchemeYouthSprintBarsWO15e`. Note for meet templates: a U16-or-younger event using 200/400 m would now be flagged as blocked by SYS-014 eligibility — correct per the WO. | closed | No — closed |
| OQ-019 *(TASK-004, 2026-07-06; RESOLVED 2026-07-15, rule-data verification pass)* | **Youth hurdle-height/implement variants — populated from the primary WO 2026 discipline tables** (§8.1.1 championships, §8.1.2 further disciplines, §8.1.3 U14/U12/U10), which carry hurdle count/height/first-hurdle/spacing and implement masses per category and match the WA CR&TR senior specs, closing the earlier secondary-source caveat. `catalog.json` v`2026.2` adds: 110mH U20/U18 (99.1/91.4 cm), 100mH U20W/U18W/U16M, 80mH (U16W/U14M), 60mH (U14W fixed 76.2 cm; U12/U10 meet-defined height per the §8.1.3 footnote), 300mH all categories (7 hurdles), 400mH U20/U18, steeple barrier heights (3000/2000/1500mSC), U16→U12 implement masses (shot/discus/hammer/javelin), a new `hurdleCount` variant field, and the missing Swiss youth track disciplines (80/150/600/1000/2000 m, DE/FR i18n keys shipped). **Residual, deliberately out of scope:** the WO *indoor* tables (§9.1.2/§9.1.3) define different 60 mH layouts (5 hurdles) and 50 m/50 mH events — encoding them needs an indoor/outdoor dimension on `DisciplineVariant`; masters implement/hurdle specs (WA Masters rules) remain unpopulated. Fixture: `internal/domain` `TestCatalogYouthVariantsWO2026`. | closed | No — closed (indoor variants are an additive follow-up when an indoor meet is first scoped) |
| OQ-020 *(TASK-007, 2026-07-08; evidence updated 2026-07-15, rule-data verification pass; RESOLVED 2026-08-04, DEC-016/TASK-036)* | **UKC ranking with a missing discipline — primary evidence contradicted the shipped assumption for FINAL lists.** A real official UKC Rangliste (TAF3-produced, LV Langenthal, 17 May 2025, https://lvl.ch/images/resultate/2025/Gesamtrangliste_UBSKidsCup_2025.pdf) shows every athlete missing a discipline entirely (no captured result, not even an invalid-attempt status) **listed unranked at the bottom of their division** — total column "aufg.", no rank number — while an athlete who attempted every discipline but scored no valid result in one or two of them ("ogV") still ranks normally, that discipline flooring to **1 point** rather than 0 (e.g. Geiser Lukas, W9, rank 28: 60m 210 pts + two ogV disciplines at 1 pt each = 212). One athlete with all three marks present was still unranked ("n.a." — Thome Lauriane, W12), evidencing a distinct out-of-competition status. **Shipped (DEC-016, TASK-036):** UC-033 #3 amended; `domain.FinalRankCombined`/`AttemptedNoValidResult` implement the final-vs-provisional distinction and the 1-point floor (`ScoringTable.NoValidAttemptFloor`, set on `ubs-kids-cup.json`); `ResultsService.FinalStandings`/`CurrentStandings`/`SeriesComplete` gate FINAL semantics on every discipline's units being announced; public results, PDF result lists, series-upload export and the omx snapshot all resolve through `CurrentStandings` (or round-trip what it needs) so none re-implement the completeness check; a `store.Participant.OutOfCompetition` flag (migration 0020, `ResultsService.SetOutOfCompetition`) covers the out-of-competition case. Fixture-verified against the evidence: `internal/domain` `TestUC033FinalStandingsUnrankedIncomplete`/`TestUC033FinalStandingsAttemptedNoValidResultStillRanks`/`TestFinalRankCombinedOutOfCompetition`, `internal/app` `TestUC033FinalStandingsUnrankedIncomplete`/`TestSetOutOfCompetition`/`TestOMXRoundTripPreservesFinalStandingsSYS073UC033`, `internal/web` `TestUC033FinalStandingsWebSurfacesSweep`. Residual gaps tracked narrowly: OQ-090 (French-locale marker text unverified), OQ-091 (no operator-UI toggle for the out-of-competition flag yet). | closed | No — closed |
| OQ-021 *(TASK-010, 2026-07-10; revisited TASK-018, 2026-07-12)* | **Start-list publication step:** SYS-070 lists "start lists" among the public surfaces and SYS-071 gives publications a 60 s budget, implying a deliberate publish action — but only the timetable has a versioned draft/published state today (`TimetableVersion`); the domain model's `OfficialDocument.DocumentStartList` exists but is unwired. PoC behaviour: `GET /m/{id}/startlists` serves the live roster directly (see `internal/web/public.go`). Decide whether start lists need their own published snapshot (timetable-style) once seeding lands (TASK-018) — if so, that is an ADR-worthy publication-model decision, not a silent change. **TASK-018 status:** heat seeding now exists (`internal/app/seeding.go`), and the public start-list page was extended additively (`ResultsService.PublicHeatSheets`) to show the live per-event/round heat+lane breakdown alongside the existing flat roster — still no versioned snapshot, consistent with this OQ's original assumption, so the underlying publication-model decision remains open for the Tech Lead. | Tech lead (revisit at TASK-018) | No — PoC surfaces are labeled unofficial (SYS-076); TASK-018 shipped the live view, not a snapshot |
| OQ-022 *(RESOLVED 2026-07-12, TASK-024)* | **Discipline-catalog localization coverage (SYS-074):** public pages localize discipline names via `discipline.<code>` i18n keys with fallback to the catalog's canonical English name; keys were shipped only for the disciplines the built-in UKC template and test fixtures exercised, not the full `catalog.json`. **Closed:** all 33 disciplines in `internal/domain/data/disciplines/catalog.json` now have `discipline.<code>` keys in both `de.json`/`fr.json` (standard German/French athletics vocabulary — e.g. `HJ`→"Hochsprung"/"Saut en hauteur", `Decathlon`→"Zehnkampf"/"Décathlon"); the public timetable page (`GET /m/{id}/timetable`) was also switched from the catalog's canonical name to the same `localizedDisciplineName` helper the public results page already used (a real gap, not just missing keys) — see `TestLoadShipsCompleteDEAndFR` (i18n) which now covers the full set. Operator-only surfaces (meet-detail Programme/Units tables, capture index, reconciliation) intentionally keep the canonical catalog name as a stable, locale-invariant identifier — same treatment as category codes (SYS-074 traceability note) — unchanged by this task. | Tech lead (TASK-024) | No — closed |
| OQ-024 *(RESOLVED 2026-07-12, TASK-024)* | **SYS-110 "the chosen mark-notation convention SHALL be consistent and documented" + locale-correct date formatting.** Two conventions, decided and documented here: (1) **Dates/timestamps displayed to a person** use day.month.year notation (`02.01.2006` / `02.01.2006 15:04 "UTC"`, `PageData.FormatDate`/`FormatDateTime` in `internal/web/view.go`) — the Swiss convention both DE and FR share (C7.3); this is distinct from the fixed ISO wire format `<input type="date"/"datetime-local">` requires, which never changes. Applied to meet/session dates, timetable times, programme deadlines, published-timetable/audit-log timestamps, and the sanctioning summary's generation timestamp, across both operator and public surfaces. (2) **Captured marks** (`domain.FormatCentiMark`) keep **period-decimal notation** ("7.67") in both DE and FR — deliberately **not** switched to the incumbent's comma convention (C7.3 cites Seltec rendering "7,67") — because marks feed the results-interchange formats this system reads/writes (LIF, TAF3-compatible CSV, World Athletics-style feeds), which are period-decimal; a locale-varying mark representation risks silent misparsing at those seams (the class of failure SYS-086 already names as an anti-pattern to avoid, "Web.TEC 2 loss mode" — different failure, same "silent data corruption" shape). Fixture test: `internal/web` `TestFormatDateAndMarkNotationSYS110UC025_3`. | Tech lead (TASK-024) | No — closed; revisit only if a future locale's users report the mark format is unreadable |
| OQ-023 *(TASK-012, 2026-07-10)* | **UKC series-upload file: exact column layout unverified.** SYS-077/UC-035 require the export to conform to "the current official UKC organizer template," but the byte-for-byte layout of that file could not be obtained offline. What was verified from primary sources (fetched 2026-07-10): (1) the Mille Gruyère/Visana Sprint "Anleitung und Funktionen des Tools der Nachwuchsserien" organizer manual, §3.1 "Excel", confirms a downloadable "Vorlage Resultatimport" Excel template is the non-TAF3 results-submission path for the Nachwuchsserien family (Visana Sprint, Mille Gruyère — the UBS Kids Cup's own organizer page instead advertises an "Excel-Auswertungssoftware", a related but distinct artifact); (2) a real UBS Kids Cup TAF3 result-list export (Gesamtrangliste, LV Langenthal, 17 May 2025, https://lvl.ch/images/resultate/2025/Gesamtrangliste_UBSKidsCup_2025.pdf) shows the column semantics — Rang/StNr/Name/Jg/Nat/Verein, a mark+points pair per discipline, combined total, PB flag — that the shipped `internal/domain/data/series-uploads/ubs-kids-cup.json` fixture mirrors. **Shipped behaviour is a clearly-marked assumption**: the fixture and its tests (`TestBuiltinUKCSeriesUploadTemplate`, `TestSeriesUploadExportSYS077UC035_1`) verify structural self-consistency and the UC-035 edge cases, not conformance to the real "Vorlage Resultatimport" file. Obtain the actual template (organizer login at kidscup.swiss-athletics.ch, or the series office) and diff it against the shipped data file once accessible. | Founder / UBS Kids Cup or Nachwuchsserien series office | No — SYS-077 CON-01 already requires the template to ship as a replaceable data file; correcting it is a data-file swap (proven by `TestSeriesUploadTemplateIsData`/`TestSeriesUploadExportSYS077UC035_3`), not a code change |
| OQ-029 *(TASK-016, 2026-07-12)* | **Meet-status "published" transition was unwired:** UC-003 #1's "published meet with open entries" precondition had no prior path to `MeetStatus.MeetPublished` — TASK-006/007 wired only draft creation and archive. Added `MeetService.PublishMeet` (draft→published, organizer-gated, audited as `meet.publish`) as the minimal missing transition, with a button on the meet workspace when the meet is a draft. Confirm this is the intended lifecycle action long-term — e.g. should publishing also require a completeness check, mirroring the sanctioning-summary gate (UC-001 #5)? | Tech lead (meet-lifecycle owner) | No — the transition is minimal and additive; a completeness gate can be layered on later without a breaking change |
| OQ-025 *(TASK-016, 2026-07-12)* | **Entry-submitter account provisioning has no self-service path:** UC-003's "athlete, club entry submitter" actor authenticates via an `entry_submitter` account, but per TASK-013's account model only an instance admin can create accounts — there is no public sign-up flow. Shipped behaviour: an organizer/admin issues an `entry_submitter` account per club/athlete out of band, same as any other role; the online-entries page is reachable from the public meet overview's nav (`public.nav.entries`), but an anonymous visitor who clicks it still hits the login wall. Confirm whether self-service registration is in scope for a later milestone. | Founder / tech lead | No — PoC-level behaviour consistent with existing account provisioning; a self-service flow is additive |
| OQ-026 *(TASK-016, 2026-07-12)* | **SYS-015 entry limits shipped at event granularity only:** the requirement text lists "entry limits (max entries per event/club/athlete)"; this iteration implements and tests the per-event cap (`Event.EntryLimit`, organizer-configured on the add-event form) since it is the unambiguous case — a per-club or per-athlete cap needs a policy decision (per meet? per event? per day?) not specified in the baseline. Per-club/per-athlete caps are deferred. | Founder / tech lead | No — the shipped per-event cap is the common case; finer caps are additive |
| OQ-027 *(TASK-016, 2026-07-12)* | **Relay entries carry no seed performance / entry-standard evaluation:** UC-003 #5's entry-standard fixture is specified against an individual 100 m entry; this iteration evaluates the SYS-015 standard for individual/bulk entries only — a relay team entry has no seed-performance field and never reports `FailsStandard`. Confirm whether relay entry standards (e.g. a squad's best combined time) are in scope and how they should be computed. | Founder / tech lead | No — relays are entered and bib-assigned normally; only the standard-comparison sub-feature is scoped out |
| OQ-028 *(TASK-016, 2026-07-12)* | **Payment processing confirmed out of scope:** SYS-017 explicitly states "Payment processing itself is out of MVP scope"; this iteration implements fee *computation* only (schedule × entries, per-club summary, CSV export suitable for the Swiss starting-fee levy report) with no external payment/invoicing integration — matching CLAUDE.md's guardrail that external integrations are one-way doors reserved for the Tech Lead/ADR process. | closed | No — matches the SyRS baseline already |
| OQ-030 *(TASK-017, 2026-07-12)* | **Alabus entries-export column layout remains unverified** — same underlying gap as OQ-011, revisited for the SYS-013(b) mapping profile itself. No sample Alabus export could be obtained (marketing site blocks automated access; the TAF3↔Alabus flow is documented only from the Seltec/TAF3 side, `docs/research/competitive-analysis.md`). Shipped behaviour: `internal/domain/data/import-profiles/alabus.json` is a clearly-marked **assumption** profile — German-language column headers (Vorname/Name/Jahrgang/Geschlecht/Verein/Lizenznummer/Disziplin/Kategorie/Meldeleistung) modelled on the verified TAF3-family result-list vocabulary elsewhere in this repo (OQ-023) and on WO 2026 §5.3b's licence-number join-key requirement — loaded through the same generic `domain.ParseImportMappingProfile` interpreter as the system-native profile, so replacing it with the real layout is a data-file swap, not a code change (`TestBuiltinImportMappingProfiles_SystemNativeAndAlabusSYS013`, `TestImportMappingProfile_CustomProfileNoCodeChange`). Obtain a real Alabus export sample (per DEC-011's PoC-first strategy) and diff it against the shipped file. | Founder / Swiss Athletics or club contact | No — same versioned-data path as OQ-011/OQ-023; a correction is a data-file swap |
| OQ-031 *(TASK-017, 2026-07-12)* | **UC-005 #3's "max one race ≥600m per day" cap is approximated at meet scope, not day scope.** `internal/domain.Event` carries no day/session link yet (round/unit scheduling — and the wiring that would give an event a competition day — lands with seeding, TASK-018); `internal/app.otherActiveRaceDistances` therefore checks an athlete's other active entries across the **whole meet**, not the specific day, which over-flags a multi-day meet where an athlete legitimately runs one ≥600m race on each of two different days. Single-day meets (the common case, including every fixture in this repo) are unaffected. Revisit once TASK-018 links events to a scheduled day, narrowing the check from meet-scope to day-scope. | Tech lead (revisit at TASK-018, mirrors OQ-021's precedent) | No — the meet-scope check is conservative (over-flags, never under-flags) and self-contained in one function |
| OQ-032 *(TASK-017, 2026-07-12)* | **Relay entries are not evaluated for eligibility.** SYS-014's checks (category/licence/youth-protection) are athlete-birth-year/sex-keyed; a relay-team entry has no single athlete to evaluate against (four-plus athletes, mirroring OQ-027's precedent for the SYS-015 entry-standard gap). `EntryDetail.Eligibility` stays zero-value for relay entries, and `EligibilityExceptions` never lists them. Decide whether per-leg-athlete eligibility (each relay member individually checked against the event's category/youth-protection rules) is in scope for a later milestone. | Founder / tech lead | No — relays are entered and bib-assigned normally; only the eligibility sub-feature is scoped out, same shape as OQ-027 |
| OQ-033 *(TASK-017, 2026-07-12)* | **Online entry forms collect no licence number.** `IndividualEntryInput`/`BulkEntryLine` (TASK-016, UC-003) have no licence-number field, so every online-submitted entry evaluates `HasLicence: false` — meaning any online entry at a licence-required tier (A/B-Meeting) is always flagged `licence_missing` (blocked) until an operator separately attaches a licence (e.g. by re-importing the same athlete via CSV with a licence column, which enriches the existing athlete record — `store.SetAthleteExternalID`). This is correct per SYS-014 as shipped, but is a real UX gap for A/B-Meeting organizers relying on online entry: confirm whether the online-entry form should grow an optional licence-number field (UC-003 scope) rather than requiring an import round-trip. | Founder / tech lead | No — the evaluation itself is correct; only the UX path to supply a licence for an online entry is missing |
| OQ-034 *(TASK-018, 2026-07-12; RESOLVED 2026-07-15, rule-data verification pass)* | **Lane-draw classification — verified against the primary WA CR&TR 2026 TR 20.4; D2.3's tension dissolves.** (1) TR 20.4.6's "any event where only a single round (final) is required → drawn by lot" is **scoped to competitions under World Rankings definition 1.(a)-(c)/2.(a)-(b)** (top-tier internationals), and the TR20 interpretation notes explicitly allow lower-level competitions different principles — so UC-008 #3's grouped 400 m club final is legitimate practice, not a rule violation; the grouped draw for seeded fields is retained. (2) TR 20.4.1: first-round lanes are drawn by lot "unless the applicable regulations provide otherwise" — also documented in the data file's notes; an organizer seeding without marks already gets the by-lot path. (3) **Real correction found:** the rank-group → lane-set tables differ by event class (TR 20.4.3 straight races: top4 → 3-6; TR 20.4.4 200m/300m: top3 → 5-7; TR 20.4.5 400m-class: top4 → 4-7 — the only table previously shipped), each with 8- and 9-lane variants. `tr20.json` v`wa-tr20-2026.2` now carries all three classes (`laneGroupClasses`, interpreted by `domain.SeedingRules.LaneGroupsForDiscipline`); judgment calls flagged in the file: 300mH and 150 m are placed in the 200/300 class. UC-008 #3 (400 m table) is unchanged. Fixture: `internal/domain` `TestLaneGroupClassesTR204`. | closed | No — closed |
| OQ-035 *(TASK-018, 2026-07-12)* | **Two best-effort algorithms are shipped without a fully worked D-reference example to verify against, only synthetic fixture tests this iteration wrote itself:** (1) **Club-separation swap heuristic (D2.2):** the spec states the principle ("athletes from the same Member/team... should be placed in different heats whenever possible... exchanges... between athletes seeded in the same group of lanes") but gives no worked numeric example; `internal/domain/heatseeding.go`'s `separateClubs` resolves a same-club pass-boundary collision by exchanging with the next-ranked candidate in a different heat, proven only against a synthetic 6-athlete/2-heat case (`TestClubSeparationSYS026UC008_2`) — the general case (larger fields, multiple simultaneous club collisions) is not proven optimal, only "best effort, D2.2's own qualifier." (2) **Manual-override + TR20.4 group fidelity:** when a heat already contains a manually-overridden entry (SYS-028), `GenerateHeats` falls back to a by-lot draw for the remaining free lanes in that heat instead of re-deriving a partial TR20.4 group assignment around the fixed entry (`internal/app/seeding.go`'s `OverrideAssignment`/`GenerateHeats` comments) — collision-free and tested (`TestGenerateHeatsManualOverrideSurvivesRegenerationSYS028UC008_5`), but not literally TR20.4-grouped for that heat's free lanes. Confirm both are acceptable simplifications, or specify the exact expected behaviour with a worked example. | Founder / tech lead (algorithm refinement) | No — both are internal algorithm choices behind a stable interface (`domain.SeedHeats`/`DrawLanesGrouped`); refining either is additive, not a schema or API change |
| OQ-036 *(TASK-019, 2026-07-12; RESOLVED 2026-08-05, DEC-024/TASK-040)* | **Result corrections are scoped to the settled-result level, not the attempt level; the office-only correction UI now covers every family.** `app.ResultsService.CorrectResult` amends the settled `results` row directly for track, horizontal-field, and (as of TASK-040) vertical-field units once a unit is announced (UC-015 #2/#3), but does not rewrite the underlying attempt/trial series a field mark was derived from — a later `SaveFieldAttempt`/`SaveVerticalTrial` call (itself blocked once announced, `ErrCorrectionRequired`) would recompute the settled result from attempts and silently undo a settled-level field correction, so field/vertical corrections remain a one-time settled-level override, not a resumable capture flow (**attempt-level resumable corrections stay deferred, per DEC-024** — the open half of the original question). The settled-result-level wind API (`SetUnitWind`) stays restricted to track units unchanged (`TestSetUnitWindRejectsFieldFamilySYS040`). **Closed: the capture page's correction form is no longer track-only.** `fieldGrid` (horizontal) and `verticalGrid` (vertical, `internal/web/verticaljump.templ`) both grow the same reason/escalation/mark/status correction row once a unit is announced, and the vertical-jump page gained the protest banner/announce action it was missing (the announce route was already family-agnostic). Building this UI surfaced and fixed two pre-existing bugs it made newly reachable/visible: the capture page's own live unit standings (`fieldStandings`) ranked and displayed purely from the raw, never-rewritten attempt series, so a correction's mark/status change never reached placings (the meet-wide `CurrentStandings` used by public pages/exports/PDFs already read `results` directly and was unaffected); and `CorrectResult` scored/record-evaluated an empty mark unconditionally, so a status-only DNS/DNF/DQ/NM correction failed outright on any scored meet, for every family (unreachable before — no correction UI ever exposed a status field). See OQ-101/OQ-102 for two narrower follow-on questions this fix raised. | Founder / tech lead (follow-up to TASK-021/022's field-event work) | No — closed; the remaining attempt-level resumable-correction question is deliberately deferred per DEC-024, not blocking |
| OQ-037 *(TASK-019, 2026-07-12)* | **The protest-clock countdown is computed at render time, not live-ticking in the browser.** `domain.ComputeProtestState` derives the remaining minutes from the server clock on each page load/SSE-triggered standings refresh (UC-015 #1: "the protest window... countdown state is visible"); there is no client-side JavaScript timer decrementing the displayed minutes between requests, so an operator watching an idle tab sees a stale countdown until the next fetch. The existing SSE "results" event already triggers a standings refresh on any capture activity in the meet, which incidentally refreshes the banner too, but a genuinely idle unit's countdown will not visibly tick down. Confirm whether a client-side ticking countdown (a small additive `capture.js` enhancement) is required for the MVP, or whether render-time accuracy (correct on every page interaction) is sufficient. | Founder / tech lead | No — purely a display refinement; the underlying state (`ComputeProtestState`) is always correct when read, only the idle-tab display lags |
| OQ-038 *(TASK-019, 2026-07-12)* | **Reaction times (SYS-040: "reaction times where provided by the timing system") have no manual capture field.** `TrackResultInput`/`domain.Result` carry no reaction-time column; UC-010's executable acceptance criteria (#1–#5) never exercise reaction time, only SYS-040's requirement text mentions it, and a hand-operated capture form is not a plausible source of record for it (reaction time comes from a start-block transducer via the timing system, not manual entry). Shipped assumption: reaction time lands with the FinishLynx-class timing exchange (TASK-020, UC-014), which ingests it directly from the `.lif` result file per unit/athlete — no schema change anticipated there beyond a column, since `results` already carries per-athlete granularity. Confirm this sequencing (TASK-020 is the sole reaction-time source) is acceptable, or whether a manual reaction-time field is wanted for the FAT-but-no-FinishLynx case. | Founder / tech lead (revisit at TASK-020) | No — self-contained: adding a nullable reaction-time column and TASK-020's ingestion path is additive to the `results` schema, no code here assumes its absence |
| OQ-039 *(TASK-019, 2026-07-12)* | **The escalation reference required for a post-window correction (SYS-047, D8.3's implicit "official results need a higher bar to touch") is a free-text field, not a structured jury/referee decision record.** `app.CorrectionInput.Escalation` is an operator-typed string (e.g. "Jury of Appeal decision #3") stored verbatim in the audit entry's `after_json`, not a reference to a dedicated jury-decision entity with its own actor/timestamp/ruling — TASK-019's scope note ("corrections after window require explicit escalation/flag per spec") is satisfied literally (a non-empty flag is mandatory, `ErrEscalationRequired`), but there is no structured jury/protest-case workflow (who filed the protest, who ruled, the ruling text) behind it. Confirm whether a first-class jury/protest-case entity (SyRS §2 gap) is in scope for a later milestone, or whether the free-text reference is the intended MVP shape. | Founder / tech lead | No — `Escalation` is a plain string column path (embedded in the audit JSON, no schema of its own); introducing a structured entity later is additive and does not require migrating the free-text values already recorded |
| OQ-044 *(TASK-021, 2026-07-13; RESOLVED 2026-07-15, rule-data verification pass)* | **Cross-height elimination — confirmed against the primary WA CR&TR 2026 TR 26.2:** "Three consecutive failures, regardless of the height at which any of such failures occur, disqualify from further jumping/vaulting except in the case of a tie for first place. The effect of this Rule is that an athlete may pass their second or third trial at a particular height (after failing first or second time) and still jump/vault at a subsequent height." Shipped rule (`domain.VerticalSeries.Eliminated()`: reset on O, no-op on pass/–, increment on X) and the `TestVerticalEliminationSYS043UC012_2` fixture are exactly this — no change needed. Bonus context: the TR26 interpretation notes suggest children's/school meets may allow re-taking passed trials, matching this system's youth-first posture if ever requested. | closed | No — closed, shipped behaviour confirmed correct |
| OQ-045 *(TASK-021, 2026-07-13; RESOLVED 2026-07-15, rule-data verification pass)* | **WA combined-events tie-break — the secondary source described the PRE-2020 rule; the current primary rule abolishes tie-breaking entirely.** WA CR&TR 2026, TR 39 (Combined Events Competitions), final placing provision: "If two or more athletes achieve an equal number of points for any place in the competition, **it shall be determined as a tie**." The 3+-way carve-out worry is therefore moot — no majority/highest-event comparison applies at all. Shipped fix: `CombinedScoringTable` gained a `tieBreak` policy field (data-driven, `wa-combined-events-2001.json` declares `ties-stand`; empty defaults to ties-stand since TR 39 governs every WA-formula table), `domain.RankCombinedWithTieBreak` implements it, and `ResultsService.Standings` applies the meet's combined table's policy — UKC meets keep the Reglement §3 majority rule (`RankCombined` default) unchanged. Fixtures: `internal/domain` `TestRankCombinedTiesStandTR39`/`TestCombinedScoringTableTieBreakPolicy`, `internal/app` `TestCombinedEventsEqualTotalsTieStandsTR39`. | closed | No — closed |
| OQ-046 *(TASK-021, 2026-07-13)* | **Vertical jump capture (`SaveVerticalTrial`) is online-only for M2 — the offline capture island (TASK-009, UC-034/SYS-085-087) was not extended to it.** UC-034's acceptance criteria are generic ("attempt/result entry continues uninterrupted... captures submit automatically... idempotent replay") and name no specific discipline family; the offline island's client code (`capture-offline.js`, the service worker, the local durable queue) is wired specifically to the horizontal-attempt grid (`v.IsField` in `internal/web/capture.templ`) and predates vertical jump's existence. Extending it to the height-progression grid would mean teaching the offline queue a second cell-addressing scheme (height+seq instead of seq alone) and is a materially larger client-side change than the server-side capture path this task adds. Vertical jump capture (`/meets/{id}/capture/{unit}/vertical-trial`, `vertical-heights`) requires connectivity; a field official capturing high jump/pole vault offline sees an ordinary network error, not a graceful offline queue. Confirm whether offline tolerance for vertical jump is required before M2 ships, or whether it is acceptable to defer (vertical jump officials are typically stationed near the throws/jumps area, often within hub Wi-Fi range, unlike a roaming 60 m official). | Founder / tech lead | No — the offline island's scope is additive per discipline family; extending it to vertical jump later does not require reworking the horizontal-grid path already shipped |
| OQ-047 *(TASK-021, 2026-07-13)* | **SYS-031's "ordered discipline sequences over one or two days, with participants divisible into parallel groups" is satisfied by reusing TASK-018's existing `Event`/`Round`/`Unit`/`Session` foundation, with no new combined-events-specific entities and no dedicated multi-day/multi-group fixture test written in this task.** Each decathlon/heptathlon discipline is its own `Event` (in template-declared order via `data/templates/wa-decathlon.json`/`wa-heptathlon.json`), scheduled onto a `Session` (which already carries a competition day) with the ordinary per-unit heat/flight mechanism standing in for "parallel groups" — the same primitives TASK-018 built and tested for track/field seeding generally, not a combined-events-specific addition. This is judged sufficient because SYS-031's own acceptance evidence (UC-013 #1) only requires the *discipline sequence and scoring* to work, not a specific multi-day/multi-group scheduling UI; `TestCreateCombinedEventsMeetFromTemplateSYS031SYS044UC013` proves the sequence and template mechanism, not day/group splitting (which would be exercised via `MeetService.AddEvent`'s existing `Rounds`/session-scheduling paths, already covered by TASK-018's own tests, applied to combined-events disciplines rather than repeated here). Confirm this reuse is sufficient, or whether combined events need a first-class "this group of events is one competition" grouping entity (e.g. to prevent a public results page from listing a decathlon's ten disciplines as unrelated individual events once other TASK-016+ event types share a meet). | Founder / tech lead | No — no new entities were introduced; a future grouping entity would be additive and would not require migrating the `Event`-per-discipline shape already shipped |
| OQ-040 *(TASK-023, 2026-07-13)* | **SYS-103 "minor" age threshold:** `domain.MinorAgeThreshold` = 18 (Swiss age of majority, ZGB Art. 14) is used to flag athletes for the office privacy worklist's "minor" badge. nFADP itself sets no fixed digital-consent age the way GDPR Art. 8 lets member states set 13–16 for information-society services; confirm 18 is the right threshold for *publication*-consent specifically (as opposed to general legal capacity), not a stricter/looser sport-specific convention Swiss Athletics or a club might already use. | Founder / privacy counsel (feeds the Opus TASK-023 review gate) | No — `domain.IsMinor` is a single named constant; changing it is a one-line, fully-traced change |
| OQ-041 *(TASK-023, 2026-07-13)* | **Scope of `ResultsPublicationWithdrawn` suppression:** SYS-103's own wording emphasizes minors ("esp. for minors") without saying withdrawal is *restricted* to minors. This system's shipped behaviour applies the withdrawal flag uniformly to any athlete of any age (simpler single code path, more protective default), rather than restricting suppression to minors and treating an adult's individual objection as legally overridable by the "official competition record" interest. Confirm this reading, or narrow the enforcement to minors only if adult results must remain public regardless of an individual objection. | Founder / privacy counsel | No — `PublicDisplayNameFor`/`PublicDisplayClubFor` (`internal/domain/privacy.go`) are the single choke point; adding an `IsMinor` gate there is a small, localized change |
| OQ-042 *(TASK-023, 2026-07-13)* | **SYS-102 default retention period (90 days)** is UC-024 #3's own acceptance-criteria example, adopted as `app.DefaultRetentionDays`, not sourced from a specific legal or federation deadline. Separately, `PhotoConsentGiven`/`ExtendedDataConsentGiven` (`domain.PublicationConsent`) are recorded at entry time but not yet enforced anywhere — no photo-publication surface exists in this system yet. Confirm 90 days against any Swiss Athletics results-challenge/protest window once known, and revisit the two reserved consent flags' enforcement when a photo-publication feature is scoped. *(Post-merge status, 2026-07-13: also records two adjacent TASK-016/017 privacy determinations made under this task's SYS-100 audit — (a) the fee-summary CSV (`/meets/{id}/fees/export`, organizer-gated) contains only club-level data (club name, entry counts, CHF totals), no natural person's data at all, so SYS-100 minimization does not apply to it; (b) the CSV import path creates athletes with the not-withdrawn consent baseline and NO RecordedAt/RecordedBy — an import file carries no consent artifact and fabricating one would be worse than recording none — and never touches a matched existing athlete's consent, so a withdrawn athlete cannot be silently re-published by re-import, `TestImportPreservesConsentSYS103UC023`.)* | Founder / next Swiss Athletics contact | No — `--retention-days` is already an operator-configurable flag (`cmd/bahnfrei serve`); a corrected default is a one-line change, and the reserved flags are additive, no schema change needed to start enforcing them |
| OQ-043 *(TASK-023, 2026-07-13)* | **Public-suppression "legal mode":** UC-023 #2 anticipates a "configured legal mode" for suppressing a withdrawn athlete's public entry. This system ships one mode only — "neutralize" (keep the row: bib/rank/marks/total; blank name and club to `domain.PublicSuppressedMarker`) — deliberately chosen over fully omitting the row, since omission changes a division's visible row count and lets a determined viewer infer identity by elimination, which is often a *worse* privacy outcome in a small division. It also does not currently strip BirthYear or Sex/Nationality/ClubIDs from an already-erased (SYS-101 anonymized) athlete's row, accepting a residual small-sample re-identification risk in small clubs/categories. Confirm the "neutralize" choice satisfies every jurisdiction this system might be deployed into, or whether a "suppress the row entirely" mode must also ship. | Founder / Opus privacy reviewer | No — the mode is centralized in `internal/domain/privacy.go`; adding a second mode is additive, not a rearchitecture |
| OQ-048 *(TASK-020, 2026-07-13)* | **FinishLynx status codes FS (false start) and SC (scratched) have no CR 25 result-status equivalent, and an imported DQ carries no rule reference.** The vendor's documented Lynx status vocabulary is DNS/FS/DNF/DQ/SC/ADV (Database Files, help.finishlynx.com); this system's operator-settable CR 25 vocabulary (SYS-045, `domain.ValidateCaptureStatus`) is DNS/DNF/DQ/NM. DNS/DNF/DQ map directly (`exchange.MapLynxStatus`); FS/SC/ADV do not — a false start is arguably DQ-class, a scratch is an *Entry*-level not *Result*-level concept, and ADV is a round-progression code TASK-018 already owns — so an imported row carrying one of these is queued as an `unmapped_status` conflict for the operator to translate by hand (`ResolveTimingConflict`'s `OverrideStatus`), never guessed. Separately, a DQ imported cleanly (no conflict) carries an empty `StatusDetail` — FinishLynx has no rule-reference field — so `applyTimingRow` deliberately does not enforce SYS-045's "a DQ requires a rule reference" the way operator capture does; the office fills it in later via the ordinary correction flow (`CorrectResult`, which *does* enforce it). Confirm the FS→DQ-class judgment call (or an FS-specific status if the CR 25 vocabulary should grow one) with the founder/a timing-crew contact. | Founder / tech lead | No — the conflict path is safe by construction (nothing is silently misclassified); resolving the mapping question only changes which codes route through `unmapped_status` |
| OQ-049 *(TASK-020, 2026-07-13)* | **Scope limits taken in the timing-exchange slice, all additive to relax later:** (1) **Track only** — `ExportTimingFiles`/`ExportGenericCSV` only include `domain.FamilyTrack` units; FinishLynx is a photo-finish (timed-race) integration with no natural counterpart for horizontal/vertical field events, so field units are simply omitted from the start list rather than force-fit into a "time" field. (2) **Individual entries only** — a `unit_entries` row whose `entries.RelayTeamID` is set (a relay leg) is skipped when building the export roster; relay timing (one result per team, not per athlete-bib) needs its own ID/roster convention this slice does not define. (3) **`unresolved_unit` conflicts only support "discard"** — when a `.lif`/CSV file's event/round/heat numbers match no unit at all (e.g. a stale export re-imported after the meet's programme changed), `ResolveTimingConflict` refuses `replace`/`merge` (`ErrTimingConflictUnitUnresolved`); manually re-pointing an entire file's rows at a different unit is not implemented. Confirm whether relay-timing support and/or unresolved-unit reassignment are wanted for a follow-up task. | Founder / tech lead | No — each limit fails safely (an omission or an explicit refusal, never a silent misapplication); lifting any of them is additive to `internal/app/exchange.go` |
| OQ-050 *(TASK-020, 2026-07-13)* | **The timing agent has no TLS trust-distribution mechanism for a venue's self-signed local hub certificate.** `serve --role venue` defaults to `TLSModeLocal` (a self-signed certificate, SYS-093 — "no internet-dependent certificate validation"); `cmd/bahnfrei timing-agent`'s HTTP client therefore cannot validate that certificate against a public CA. The shipped stopgap is an explicit `--insecure-skip-verify` flag (documented as venue-LAN-only, never for a hub reached over the open internet) rather than a real trust mechanism (e.g. distributing the venue's CA/leaf fingerprint to the agent out of band, or pinning). This mirrors the exact open question UC-019 #4 already names for LAN clients generally ("mechanism is a Phase B ADR") — the timing agent is one more client hitting the same unresolved question, not a new one. Route the actual trust-distribution design through an ADR when a venue/offline-node deployment is prioritized (UC-019 is currently evidence-gated, DEC-013). | Founder / tech lead (ADR, when UC-019 is prioritized) | No — `--insecure-skip-verify` is opt-in and documented; the hub role (ACME-issued certs) needs no such flag |
| OQ-051 *(TASK-020, 2026-07-13)* | **`.lif`/`.evt` header field-layout fixture-vs-reality drift risk, per ADR-006's own named risk.** The authoritative field order transcribed into `internal/exchange/finishlynx.go` is help.finishlynx.com's own "Database Files" page (13-field event header, 19-field competitor/result row incl. Place). A real third-party `.lif` sample (meettrax docs, itself citing the same vendor page) corroborates the competitor-row layout field-for-field, but its header row pads two extra empty fields before its trailing timestamp — inconsistent with the vendor's own 13-field spec (see the `TestParseLIFRealWorldSampleSYS061UC014_3` code comment). This system's encoder/decoder implements the authoritative vendor layout and does not special-case that sample's apparent header quirk. No real FinishLynx-generated file from an actual timing PC has been obtained to confirm which is correct in practice — exactly the risk ADR-006 §"Consequences" names explicitly ("Without real venue samples the risk is fixture-vs-reality drift; mitigated by ... making the ingest pipeline forgiving"). The ingest pipeline already degrades safely (an unparseable/mismatched header surfaces as an `unresolved_unit` conflict, never silently misapplied) — but a real venue `.lif`/`.evt` sample (per OQ-013's club-contact ask) should be diffed against this fixture set once available. | Founder / OQ-013's club contact (or any Swiss club running FinishLynx) | No — the codec already degrades to an explicit conflict rather than misparsing; obtaining a real sample only tightens fixture fidelity |
| OQ-052 *(TASK-022, 2026-07-13)* | **Record/best flags are not yet surfaced in `internal/exchange`'s omx export/timing-exchange formats.** `domain.Result.RecordFlags` and the SYS-049 evaluator are wired into the two manual-capture entry points (`ResultsService.SaveTrackResult`/`SaveFieldAttempt`) and the correction flow (`CorrectResult`), and render in the operator capture view, the public results page, and the printed result-list PDF (all read the same `store.ResultRecord.RecordFlags`/`domain.CombinedPerformance.RecordFlags`). *(Post-merge status, 2026-07-13: the omx half is closed — TASK-025's `omx/v1` landed with a `recordFlags` field wired through the shared `results.record_flags` column, and `TestOMXRoundTripCarriesComputedRecordFlagsSYS049SYS073` (`internal/app`) proves a real evaluator-computed `MR` flag survives export → schema validation → fresh-system re-import verbatim. Still open: results written by the FinishLynx timing-import ingest pipeline (`internal/app/exchange.go`, `store.SaveResultWithSource`) do not run through the evaluator — an imported mark earns no record/PB/SB flag until an operator re-saves or corrects it.)* | Tech lead / founder (timing-import evaluator wiring as a follow-up) | No — the remaining gap is an omission (an imported mark carries no flag), never a wrong flag; omx exports are complete per SYS-049 |
| OQ-053 *(TASK-022, 2026-07-13)* | **Combined-events wind-average record flagging (SYS-050's "and combined-events records — average across wind-relevant disciplines per D5.3") is not wired into the evaluator.** TASK-021 already computes the legality figure itself (`domain.AverageWind`, D5.3/D5.5) for combined-events *scoring* legality; TASK-022's `evaluateRecord` is only called from the plain track/field capture paths (`SaveTrackResult`/`SaveFieldAttempt`/`CorrectResult`), not from the combined-events per-discipline save path (`ResultsService.SaveResult` in `internal/app/results.go`, used by decathlon/heptathlon-template meets) — so a combined-events meet's own per-discipline marks never earn a record/PB/SB flag today, and no combined-events *record type* (e.g. a decathlon national record) is evaluated at all. All of UC-016's five acceptance criteria are demonstrated with single-discipline track/field examples, matching this scope trim. | Tech lead / founder (prioritize a follow-up task if combined-events records are wanted) | No — the gap is an omission (no flag), not an incorrect one; combined-events standings/scoring themselves are unaffected |
| OQ-054 *(TASK-022, 2026-07-13)* | **No organizer-facing UI to select/upload a meet's record lists.** `ResultsService.SetMeetRecordLists`/`RecordListOptions`/`MeetRecordListIDs` (backed by the `meet_record_lists` association table, migration 0019) is a complete, tested service-layer mechanism, mirroring the `SetMeetCombinedScoringTable` precedent — but no meet-settings web page exposes it, and there is no organizer file-upload flow for a custom record-list JSON file (the pattern other loadable data — category schemes, scoring tables — also lacks today; ADR-005 documents the loader/parser side as the reusable mechanism, not a self-serve upload UI). Until a UI lands, wiring a meet to a record list requires either a built-in `domain.RecordList` (`SetRecordLists`) or direct service-level configuration. The one shipped built-in list (`domain.RecordListExample`) is explicitly a labeled non-authoritative fixture (real record marks were not fabricated — see its `source` field), not usable as-is for a real meet. | Founder / tech lead (prioritize a meet-settings UI + list-upload flow) | No — the gap only affects self-serve configurability, not correctness of flagging once a list is wired |
| OQ-055 *(TASK-022, 2026-07-13)* | **`SetUnitWind` (TASK-019, retroactive per-race wind correction applied to every already-settled result of a unit) does not re-evaluate record/best flags for results whose wind reading changes after they were saved.** `store.UpdateResultsWind` updates the stored `wind` column on every settled result of the unit but was pre-existing behaviour this task did not touch; every UC-016 test in this task instead sets the unit's wind *before* capturing marks (the realistic operating order — a wind gun reads live, then times are typed in), so the acceptance criteria as written never exercise the retroactive-correction case. A result saved with a legal-at-the-time wind reading whose reading is later corrected to illegal (or vice versa) keeps its original `RecordFlags` until the next capture/correction touches that same (unit, athlete) row. | Tech lead / founder (decide whether `SetUnitWind` should re-run `evaluateRecord` for every affected result) | Possibly — a rare retroactive-wind-correction scenario could leave a stale record flag; flag if a real meet operates this way in practice |
| OQ-056 *(TASK-025, 2026-07-13)* | **`omx/v1` carries only the current settled state of each result, never correction/audit history.** UC-027 #1's acceptance text ("official results with statuses, wind, categories, record flags, and announcement timestamps") describes a snapshot, not a change log; SYS-046's append-only audit log already owns "who changed what, when, why" durably, and duplicating that into the interchange format would be a second, driftable source of truth for the same fact. `exchange.Document`'s `results[]` therefore always reflects the row `store.GetResult` would return right now — a correction overwrites in place, exactly like the live system's own `results` table (`SaveResultWithSource`'s "capture flows re-submit as marks are corrected" upsert semantics), and the round-trip property test (`TestUC027_2_OMXRoundTripProperty`) deliberately exercises this by applying an in-session correction before exporting and asserting only the corrected value survives. Confirm no federation/regulatory delivery requirement (D10.2's ≤24h submission window) actually needs the correction *history* riding along in the same file — if one emerges, an additive `corrections[]` array is a natural, non-breaking `omx/v1` extension (point 1 of the versioning policy, `docs/schemas/omx-v1.md`), not a v2. | Founder / tech lead | No — the settled-state-only shape is what every existing capture/correction code path already produces; adding a history array later is additive, not a rearchitecture |
| OQ-057 *(TASK-025, 2026-07-13)* | **`omx/v1` results cover individual entries only — relay-team race results have no capture path anywhere in this system yet.** This mirrors OQ-049's TASK-020 finding for the FinishLynx export exactly, but is now confirmed system-wide, not just for that one integration: `ResultsService.SaveTrackResult`/`SaveFieldAttempt`/`CorrectResult` all key off `athleteId`, and no code path anywhere writes a `results` row for a `RelayTeamID`-only entry. `omx/v1` therefore round-trips relay **rosters** (relay teams, their club/leg composition, their event entries — `TestOMXRelayTeamRoundTrip`) but simply has no relay result rows to carry, because none exist to export. This is a pre-existing system gap TASK-025 documents rather than one it introduces or is scoped to close (out of scope per the task brief's UC-027/UC-021 focus). Confirm whether relay-result capture is wanted for a follow-up task; when it ships, extending `ResultDoc`/the schema with an optional `relayTeamId` alongside `athleteId` is additive, not a v2. | Founder / tech lead | No — the gap fails safely (relay results are simply absent from every export, never fabricated or misattributed to an individual); adding relay-result capture and its schema field later is additive |
| OQ-058 *(TASK-025, 2026-07-13)* | **`omx/v1`'s `results[].recordFlags` field exists from the start but ships empty — no flag computation runs yet.** Per the parallel-work coordination between this task and TASK-022 (records & mixed-category fields, building concurrently): TASK-022 owns computing `MR`/`PB`/`SB`/wind-assisted-exclusion flags onto `domain.Result.RecordFlags`; this task only guarantees the *wire shape* — the array is always present (`[]` not omitted, never `null`), so TASK-022's flag computation landing later needs no `omx/v1` schema change, no version bump, and no consumer-side compatibility shim. The round-trip property generator (`internal/app/omx_roundtrip_test.go`) exercises the field with synthetic `PB`/`SB` string values (proving the *plumbing* carries arbitrary flag strings losslessly) without asserting anything about which flags a real meet's rules would compute — that correctness is TASK-022's to prove against its own fixtures. *(Post-merge status, 2026-07-13: reconciled — both tasks landed on the same pre-existing `results.record_flags` column (0004), so export/import carried computed flags with no code change needed; `TestOMXRoundTripCarriesComputedRecordFlagsSYS049SYS073` (`internal/app`) pins real evaluator-computed flags end-to-end through export → schema validation → fresh-system re-import, closing this question.)* | Closed (tech lead, merge reconciliation) | No — the field is additive-safe by construction; TASK-022 only needs to populate it, never change its shape |
| OQ-059 *(TASK-029, 2026-07-13)* | **Relay-entry audit rows and a relay leg's identity are not part of this task's SYS-101 erasure-redaction sweep.** `AthletePersonalDataEntityIDs` (`internal/store/privacy.go`) reaches `participants`/`results`/`entries` by `athlete_id` — but a relay-team entry's `entries` row carries `relay_team_id`, not `athlete_id` (0009_online_entries.sql: the two are mutually exclusive), and `entry.submit_relay`'s audit payload (`auditEntrySubmit`, `internal/app/entry.go`) records the CLUB name as `subject`, never a leg athlete's name, so no audit row leaks a relay leg's identity today — this gap is about `relay_teams.composition_json`/`reserves_json`, which store the erased athlete's ID (not name) as a leg member, and about `RelayTeamDetail`'s display names, which are resolved live from `athletes` at render time and therefore already show the post-erasure pseudonym automatically (no separate leak). Net: erasing a relay leg's athlete is not cosmetically defeated the way findings #1/#2 were, but it was also never verified by a test the way the individual-entry path now is. Confirm whether a relay-scoped erasure/redaction test is wanted as a small follow-up, or whether "no audit row ever carried the name, and the live-resolved display already reflects erasure" is a sufficient closed-form argument. | Founder / tech lead | No — `relay_teams` composition is unchanged by this task; adding a relay-specific test later is additive, not a rework |
| OQ-060 *(TASK-029, 2026-07-13)* | **The public-path crawler (`internal/web/privacy_crawler_test.go`, finding #4) discovers pages by following rendered `<a href="/m/...">` links plus a small hardcoded `mustVisit` list for routes that are never plain-linked (the SSE `results/live` fragment).** This is a meaningfully stronger regression than the pre-existing hand-enumerated path list (`TestPublicPageMinimizationSYS100UC023_1`) — a newly linked public page is swept automatically — but it still cannot discover a future public route that is (a) reachable only via unlinked/JS-constructed URLs (this system has none today, but a future one might), or (b) served outside the `/m/{id}/...` prefix the crawler restricts itself to (a deliberate scope guard, so the crawler never tries to walk into `/meets/...` office/authenticated routes with an anonymous client and misreport a 403 as "nothing to leak"). Confirm whether a stronger mechanism is wanted before more public surfaces ship — e.g. a compile-time route registry tag (`routes.go` already lists every public route in one five-line block, so a manual add is low-friction today) — or whether link-following + the small explicit list remains sufficient as the public surface grows. | Founder / tech lead | No — the crawler is additive test infrastructure; a stronger discovery mechanism later would not require rewriting the existing pages it already covers |
| OQ-061 *(UX requirements round, 2026-07-13)* | **Visual identity is undefined.** STR-045/SYS-116 mandate a design system; TASK-032 will consolidate the current styles into a documented, neutral default token set (palette, typography). No brand identity (logo, brand colors, wordmark usage) has been decided for Bahnfrei. Does the founder want a brand identity ratified before release 0.1, or is the neutral default acceptable for 0.1 with branding as a later token-level change? | Founder | No — tokens make rebranding a data-level change; the neutral default ships either way |
| OQ-062 *(TASK-033, 2026-07-13)* | **Timing-conflict "merge" resolution can never attach an imported mark to a status-only result, contradicting its own doc comment.** `internal/app/exchange.go` (merge branch ~line 770 + `applyTimingRow` ~line 588): when merging an imported row into an existing result that carries only a status (e.g. DNS, no mark), the merge fills `row.Mark` from the import but then unconditionally restores the existing non-`StatusNone` status — and `applyTimingRow` only writes `Mark` when `row.Status == StatusNone`, so the freshly merged mark is silently discarded. The doc comment promises "fill only the existing result's blank fields from the imported data". `TestResolveTimingConflictMergeFillsBlanksOnly` (added by TASK-033) pins the actual status-wins/mark-dropped behavior rather than the documented one. Is the intended semantics (a) status wins, mark dropped — then fix the doc comment; or (b) blank fields genuinely filled — then fix the merge logic (a behavior change to UC-014 conflict resolution)? | Tech lead / founder | No — either resolution is a small localized change; the pinning test makes the current behavior explicit until decided |
| OQ-063 *(TASK-033, 2026-07-13; closed TASK-034, 2026-07-14)* | **`ExportGenericCSV` (internal/app/exchange.go ~line 257) never verifies the meet exists, unlike its siblings `ExportTimingFiles`/`ImportTimingFile`/`CreateTimingAgentToken` — so `GET /meets/{unknown}/timing/export/csv` returns 200 with a header-only CSV while the `.ppl`/`.sch`/`.evt` export routes correctly 404 for the same unknown meet ID.** Harmless today (office-gated route, empty file), but the inconsistency is a latent trap: a timing operator scripting against the CSV endpoint gets no signal that a meet ID is wrong. **Closed (TASK-034):** `ExportGenericCSV` now calls `store.GetMeet` before building the export, matching its siblings — an unknown meet id 404s on all four timing-export routes uniformly. `TestTimingUnknownIDsRedirectOrNotFoundWeb` (`internal/web`) updated to assert the 404, folding the CSV route into the same table it already used for ppl/sch/evt. | Tech lead | No — a one-line change plus test update, as anticipated |
| OQ-066 *(TASK-027, 2026-07-13; RESOLVED 2026-08-03, DEC-015/TASK-035 read-path fix)* | **SYS-122's 2,000-concurrent-viewer budget (p95 render ≤3 s, errors ≤0.1%) is UNMEETABLE on the current read architecture — measured, not estimated.** Two compounding causes: (1) `store.Open` sets `SetMaxOpenConns(1)` (ADR-004 §2's single-writer model applied to reads too; its own code comment anticipated this finding: "WAL readers can be pooled separately once profiling demands it"), so every public results request convoys through one SQLite connection and p95 becomes queueing delay; (2) `ResultsService.Standings` recomputes the whole meet per request with no caching, allocating an athletes×disciplines performance matrix each time — **~64 MiB of in-flight heap per concurrent viewer** at the SYS-120 reference scale (1,500 athletes/250 event-units). Measured on the confined harness (`internal/web` `TestSYS122ViewerScalingProfile`, 16-core/31 GiB host, 12 GiB scope cap, GOMEMLIMIT=10GiB): 100 viewers → p95 **6.77 s**, peak heap 6.4 GiB, 0 errors; 250 viewers → p95 **17.2 s**, peak heap 9.8 GiB, 0 errors (≈69 ms of queueing per queued request — cleanly linear); 500 viewers → **OOM-killed at the 12 GiB cap**; an earlier unconfined 2,000-viewer run reached ~27 GB RSS and was killed by the host kernel (the 2026-07-13 OOM incident — see `docs/delivery/perf-and-recovery-task-027.md`). Linear extrapolation to 2,000 viewers: p95 ≈ **2.3 minutes** (~45× over budget) and ≈**125 GiB** of in-flight memory. The SSE live-update half of the surface is NOT the problem: delivery p95 was 27–49 ms at profiled scales and 1.9 s with 100 subscribers under full 2,000-viewer pressure — well inside SYS-071's 10 s. The failure mode is latency/memory, never 5xx errors (0 errors at every scale that completed). The full-scale assertion (`TestSYS122TwoThousandConcurrentViewers`) is deliberately left asserting the literal spec budget — per the task rule against silently weakening an unmeetable budget — and flips green when this decision lands. **Decision needed (ADR-004-adjacent, read path only — the ratified single-WRITER invariant is untouched):** (a) a separate pooled read connection set (WAL supports concurrent readers) — necessary but NOT sufficient (even 16 parallel readers ≈ 9 s p95 at 2,000 simultaneous renders, and per-request memory is unchanged); (b) per-meet caching of the rendered public-results page/fragment, invalidated by the existing results-changed bus event — this is the fix that actually meets the budget (one render per result change instead of one per viewer, memory O(1) in viewers); (c) alternatively/additionally, define SYS-122's "documented reference hosting size" as including a caching reverse proxy in front of the binary. **Resolved:** DEC-015 chose (a)+(b) together — TASK-035 implemented a pooled WAL read-connection set (`store.Store.ReadDB()`, pool size 16) plus a per-meet, per-locale render cache for the results page/fragment invalidated by the results-changed bus event (ADR-004 §9). Re-measured on the same confined harness: 100 viewers → p95 **185.8 ms**, peak heap 85.0 MiB; 250 → p95 **210.9 ms**, 91.1 MiB; 500 → p95 **251.0 ms**, 103.9 MiB; the literal 2,000-viewer assertion (`TestSYS122TwoThousandConcurrentViewers`) → p95 **343–377 ms** (two consecutive runs), peak heap 179–184 MiB, 0 errors — **PASS**, ~8–9× inside the 3 s budget, memory now flat rather than linear in viewer count. Full numbers: `docs/delivery/perf-and-recovery-task-027.md`. | Founder / tech lead (architecture decision; likely a small ADR amendment + follow-up TASK) | No — closed |
| OQ-067 *(TASK-027, 2026-07-13; RESOLVED 2026-08-05, DEC-021/TASK-038)* | **SYS-120 names "entry search" as a budgeted operator-visible interaction, but no search/filter feature exists anywhere in the system.** Confirmed by repo-wide sweep: no `Search`-named method in `internal/app` or `internal/web`, no query/filter parameter on any list endpoint — the operator's only way to find an entry among 4,000 is the full roster/entries list page. The SYS-120 benchmark (`internal/app` `TestSYS120ReferenceScaleOperatorBudgets/ParticipantRosterSearch`) therefore benchmarks the full participant-roster load as the closest existing proxy (p95 14 ms at reference scale — comfortably inside the 2 s budget, and browser-side Ctrl-F over one rendered page is workable at this scale), with an in-test note to add a dedicated budget test when a real search ships. Decide: is a server-side entry search a release-0.1 feature (competition-office usability at 1,500-athlete scale suggests yes — SYS-114's expert-use requirement pulls the same direction), or is the full-list-plus-browser-find workflow acceptable for 0.1? **Shipped (DEC-021, TASK-038):** a server-side `q` query-param filter (case-insensitive substring match on name/bib/club, DE/FR-labeled, a plain keyboard-reachable GET form — no JS, no migration) ships on both operator participant lists DEC-021 names: the roster page (`/meets/{id}/roster`) and the bib-assignment page (`/meets/{id}/bibs`). Both handlers (`standings.go`'s `rosterView`, `entries.go`'s `bibsView`) and the perf suite share one predicate, `app.MatchesParticipantSearch`, over the existing `Participants`/`ClubNamesFor` pooled-read-connection pipeline (ADR-004 §9) — the bulk-assign club dropdown on the bibs page stays unfiltered by design (it needs every club regardless of the search). The reserved SYS-120 sub-test lands alongside (not replacing) the existing full-list-load proxy: `TestSYS120ReferenceScaleOperatorBudgets/ParticipantSearch` narrows the reference-scale corpus to one of its 24 synthetic clubs (~62 of 1,500 athletes) — p95 **44 ms**, ~46× inside the 2 s budget. Fixture-verified: `internal/app` `TestMatchesParticipantSearchDEC021`; `internal/web` `TestRosterSearchFilterDEC021SYS120SYS114`/`TestBibsSearchFilterDEC021SYS120SYS114`. | Founder / tech lead | No — a search endpoint is additive; the benchmark gains one more sub-test when it lands |
| OQ-070 *(TASK-030, 2026-07-13)* | **SYS-114's "bulk operations for multi-athlete actions" has exactly one implemented instance today: check-in's "close check-in" action (marks every still-unconfirmed entry DNS in one keystroke, UC-007 #1).** No other bulk multi-athlete control exists anywhere in the operator surfaces this task's flows touch — track result entry (UC-010, `capture.templ`'s `trackForm`) is strictly per-athlete-per-row, one form and one save action each; there is no "select several rows, apply one status/time to all" mechanism. The keyboard-only E2E suite (`e2e/tests/keyboard-operator-SYS114.spec.ts`) therefore proves the one bulk operation that exists rather than fabricating test-only coverage for a bulk result-entry action the product does not have. Confirm whether this single instance satisfies SYS-114's bulk-operation clause for release 0.1, or whether a bulk result-entry action (e.g., "mark all remaining as DNS" on a still-open unit) should be scoped as a follow-up task. | Founder / tech lead | No — the gap is an omission (no bulk result-entry control), not an incorrect one; adding one later is additive to `capture.templ`/`internal/web/capture.go` |
| OQ-074 *(TASK-032, 2026-07-13; closed TASK-034, 2026-07-14)* | **Critical usability-audit finding (`usability-audit-checklist.md` H3/F5): no destructive/irreversible action has an explicit confirmation step.** Verified live against the running server: athlete data erasure (`POST /meets/{id}/privacy/{athlete}/erase`, `privacy.templ`), account disable (`POST /admin/accounts/{id}/disable`, `accounts.templ`), meet archive (`POST /meets/{id}/archive`, `meets.templ`) and the retention-purge trigger are all single-click submit buttons with no "are you sure?" step. This is not a simple oversight to patch inline: the shell's CSP (`default-src 'self'`, `securityHeaders` in `middleware.go`) has no `script-src` exception, so the usual `onclick="return confirm(...)"` pattern would not execute — a real fix needs either a genuine confirm sub-page (GET-then-POST) or a small reusable non-inline confirmation island. **Closed (TASK-034):** every one of the four actions now sits behind a shared GET confirm sub-page (`internal/web/confirm.go`/`.templ`, routed as `.../confirm` in front of each POST) — a real navigation, CSP-compatible, with correct back-button/bookmark semantics. Athlete erasure and retention purge (the two the ASVS review flagged as irreversible *and* unrecoverable) additionally require typing a confirmation token (the athlete's bib, or the literal "PURGE") before the POST is honored — a documented defense-in-depth choice over re-authentication (see `confirm.go`'s package doc for the rationale). Tests: `TestMeetArchiveConfirmFlowOQ074`, `TestAccountDisableConfirmFlowOQ074`, `TestPrivacyEraseConfirmFlowOQ074`, `TestRetentionPurgeConfirmFlowOQ074`, `TestDestructiveActionConfirmRoutesRequireRoleOQ074` (`internal/web`), plus an e2e proving a destructive action requires the confirm step and Cancel really cancels. | Founder / tech lead (implementation likely TASK-028 or a small follow-up TASK) | **Closed** — no longer blocks the 0.1 usability-audit re-run (UC-038 #3) |
| OQ-075 *(TASK-032, 2026-07-13; closed TASK-034, 2026-07-14)* | **Critical usability-audit finding (`usability-audit-checklist.md` H9/F3): validation errors render as a single page-level flash message, never inline at the offending field.** Verified live: submitting `/meets` with `end_date` before `start_date` returns 422 with `<p class="error" role="alert">Die Eingabe konnte nicht gespeichert werden.</p>` at the top of `<main>` (`layout.templ`'s `p.FlashError` mechanism, used by every form in the app) — it does not name which field is wrong or what to fix. The one part of SYS-117's F3/F4 pair that *does* already hold: the submitted values are preserved and re-rendered. **Closed (TASK-034):** a reusable `FieldErrors` mechanism (`internal/web/fielderrors.go`: a `map[string]string` of field name → localized message, plus `fieldError`/`fieldErrorID` templ helpers pairing `.field-error` with `aria-invalid`/`aria-describedby`) is now wired from handler validation into templ on UC-038 #4's three named representative forms: meet setup (`parseMeetForm`/`meets.templ` — the audit's own end-before-start repro case now names the `end_date` field), online entry (`handleEntryIndividualSubmit`/`entries.templ`, re-rendering instead of redirecting so input survives), and result correction (`handleCaptureCorrect`/`capture.templ`, row-scoped via `rowFieldKey` since the capture grid has one row per athlete). The page-level flash remains as a summary; the field-level render is what the acceptance criterion requires. Tests: `TestMeetCreateFieldErrorsOQ075UC038_4`, `TestOnlineEntryIndividualFieldErrorsOQ075UC038_4` (`internal/web`), and the strengthened no-reason case in `TestCaptureAnnounceAndCorrectionFlowSYS046SYS047UC015Web`. | Founder / tech lead (a follow-up TASK, or folded into TASK-031's forms work if it touches the same handlers) | **Closed** — no longer blocks the 0.1 usability-audit re-run (UC-038 #3) |
| OQ-076 *(TASK-032, 2026-07-13)* | **Major usability-audit finding (`usability-audit-checklist.md` H6/F2): the SYS-117 field-level hint-text convention is not adopted on existing forms.** A `.hint` CSS class already existed pre-TASK-032, but every current use (`seeding.templ`, `timing.templ`, `officials.templ`, plain `<p>` text in `privacy.templ`) is a page-level explanatory blurb, not a per-field format/unit/bound constraint next to its input — e.g. the meet-creation form (`/meets/new`, verified live) has zero hint text anywhere: no format hint on either date field, no explanation of `homologation_ref`. This task documents and token-backs the field-level convention (`.hint` styling in `base.css`, demonstrated on the gallery page's text/number inputs) but does not retrofit it onto existing forms, which is a per-form content/copy pass (what hint text says for each of ~40+ fields across the app), not a design-system change. *(Related F1 finding, fixed in this task rather than deferred: a `placeholder=` sweep across every `.templ` file found 13 inputs — `entries.templ` relay-edit rows, `timing.templ`'s conflict-resolution form, `import.templ`'s eligibility-override reason, `meets.templ`'s unit-schedule row — with a placeholder and no label/accessible-name at all; each now carries an `aria-label` (or, for the one `<select>`, a real `<label>`) reusing its existing translation key. This closes "no accessible name," but these dense inline/table-row fields still don't carry SYS-117's stricter permanently-*visible* label — folded into this same open question rather than filed separately, since fixing it is the same per-form layout/copy pass.)* **Update (TASK-031, 2026-07-13): substantially closed.** The hint-text pass adopted the field-level convention wherever a hard format/unit/bound constraint exists on a standard form: setup password (min length), meet end date (ordering), entry standard + entry limit (format / 0 = unlimited), online-entry seed performance + birth year, roster birth year, fee amounts (CHF), seeding heat size / track lanes / advancement standard, race wind (m/s, sign), and vertical bar-height progression (metres, comma-separated) — every hint DE/FR-localized and `aria-describedby`-associated with its input (`internal/web/help_test.go` `TestHardConstraintHintsVisibleSYS117UC037_5`); the same pass added the SYS-115 help-icon registry (`internal/web/help.go`) for non-obvious domain fields. **Still open (narrowed):** the residual visible-label upgrade for the 13 dense inline/table-row fields noted above, and hint copy for any remaining fields without hard constraints where a hint would still help — both remain the same per-form layout/copy pass; the per-release SYS-117 audit is the sweep that catches stragglers. | Founder / tech lead | No — the residual visible-label upgrade is additive copy/layout; adding it later does not require restyling or re-architecting the forms |
| OQ-078 *(TASK-026, 2026-07-14; RESOLVED 2026-08-03, testing-stack hardening pass)* | **SYS-092's "static analysis in CI" clause is satisfied for dependencies (`govulncheck`) but has no dedicated own-code security SAST.** CI runs `golangci-lint` (depguard + misspell + standard) and `govulncheck`; neither is a security-focused taint/rule scanner. Adding **gosec** as a CI job would strengthen the own-code half of SYS-092. Not enabled in TASK-026 because gosec is not installed in this sandbox and an unverified, possibly-failing CI job should not be added blind (the ASVS pass instead audits the code by inspection + tests). **Resolved:** gosec v2.28.0 (pinned) now runs as the CI `sast` job and in `scripts/check-gate.sh`; all 40 baseline findings triaged — real fixes shipped (data/cert-cache/backup dirs 0755→0750, backup/lynx-export files 0644→0600, logout cookie gains `Secure`, bounds-checked `uint32` conversions in `password.go`, and three check-in `Referer` redirects routed through `sameOriginRedirectTarget` — the OQ-079 class, previously missed there), everything else carries an inline justified `#nosec` (29 tags, no rule-wide excludes). | closed | No — closed |
| OQ-079 *(TASK-026, 2026-07-14; closed TASK-034, 2026-07-14)* | **Low-severity open-redirect in `handleLocaleSwitch` (`internal/web/routes.go`):** after setting the locale cookie the handler redirects to the raw `Referer` header, which may be an absolute off-origin URL. Assessed **not a practical phishing primitive** — the destination comes from the victim's own browser-set `Referer`, not an attacker-supplied query parameter, so it can only bounce the user back to the page they navigated from (typically same-origin), never to an attacker-chosen site embedded in a crafted link. Left unfixed in TASK-026's security-only slice to avoid touching the locale-switch UX. **Closed (TASK-034):** `sameOriginRedirectTarget` (`internal/web/routes.go`) accepts the `Referer` only when it is root-relative (a single leading `/`, not the protocol-relative `//`) or its host matches the current request's host; anything else falls back to `/`. Tests: `TestSameOriginRedirectTargetOQ079` (table-driven unit test of the guard), `TestLocaleSwitchRejectsOffOriginRefererOQ079` (end-to-end through the real handler) — both `internal/web`. | Tech lead | No — a bounded guard plus test; no architectural impact |
| OQ-077 *(TASK-032, 2026-07-13)* | **Minor usability-audit finding (`usability-audit-checklist.md` F6): no operation shows explicit progress/in-flight feedback beyond the browser's native page-load indicator.** Every mutating action is a synchronous POST-redirect-GET (or a direct file download for exports/backup/PDFs); there is no disabled-while-submitting button state, spinner, or "processing…" text anywhere. At current MVP scale this is low-risk (SYS-120 targets ≤2s p95 for interactive operations, and a full page navigation's native loading affordance is not "silence"), but it is worth a deliberate look once import/export operations on larger meets are measured (TASK-027 performance work) rather than assuming it stays fine. | Founder / tech lead | No — purely additive UX polish; does not change any handler's contract |
| OQ-086 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-017)* | **Container image publication target is undecided.** `docs/ops/release-process.md` §5 documents that the `Dockerfile` builds a verified working image (`docker build .` succeeds; `docker run` starts the server; `--version` reports the ldflags-stamped build-arg version) and that `scripts/build-release.sh` builds it locally, but SYS-146's "container publication" needs a registry to publish *to* — no GHCR/Docker Hub/self-hosted-registry account exists yet, and TASK-028's scope explicitly excludes running `docker push`. Until resolved, the documented path for anyone wanting the image is `docker build -t bahnfrei .` from the tagged source. **Closed (TASK-037):** images publish to **GHCR** (`ghcr.io/kriegalex/bahnfrei`, DEC-017), tagged with the release version and `latest`, pushed by `.github/workflows/release.yml` on every `vX.Y.Z` tag push. `docs/ops/release-process.md` §5 and `docs/ops/quickstart.md` §1 Option B updated accordingly. | Closed (founder decision DEC-017, implemented TASK-037) | No — closed |
| OQ-087 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-019)* | **Release artifacts are unsigned.** `scripts/build-release.sh` ships a SHA-256 `checksums.txt` per release (verified: built, cross-compiled for all five targets, and round-tripped through `sha256sum -c` in this task) but no code-signing certificate for macOS notarization or Windows Authenticode, no GPG-signed checksums file, and no cosign/sigstore signature on the container image. Practical effect: unsigned macOS/Windows binaries trigger OS Gatekeeper/SmartScreen warnings, adding friction to the SYS-131 ≤30-minute quickstart for non-technical operators on those platforms; unsigned artifacts generally are a weaker supply-chain posture for anyone auditing a download before running it on real athlete data. Documented as the current stance in `docs/ops/release-process.md` §4 rather than left implicit. **Closed (TASK-037):** the container image and `checksums.txt` are now signed **keyless with sigstore/cosign in CI** (`.github/workflows/release.yml`, GitHub Actions OIDC — DEC-019), with `cosign verify`/`cosign verify-blob` instructions in `docs/ops/release-process.md` §4. macOS notarization and Windows Authenticode remain deliberately deferred (cost, no adopter demand yet); the Gatekeeper/SmartScreen workaround for the still-unsigned binaries is documented in the same section. | Closed (founder decision DEC-019, implemented TASK-037) | No — closed |
| OQ-088 *(TASK-028, 2026-07-14; closed TASK-037, 2026-08-05, DEC-018)* | **Support window / backport policy across release lines is undecided.** SYS-146's release process needs a documented support window; no founder decision exists yet on whether older MINOR lines get backported security/critical fixes or only the latest release is supported. `docs/ops/release-process.md` §6 documents an interim default — latest release only, best-effort — so operators have something concrete to plan upgrades against rather than an unstated policy, but this should be confirmed (or replaced with a real N-1 backport commitment) once the project has more than one release line in the wild. **Closed (TASK-037):** the interim default is confirmed as the real 0.x policy (DEC-018) — latest release only, best-effort, no multi-version backport/LTS commitment; `docs/ops/release-process.md` §6 reworded from "interim" to the standing policy. Revisit if/when a club runs an older line in production. | Closed (founder decision DEC-018, implemented TASK-037) | No — closed |
| OQ-089 *(TASK-028, 2026-07-14; decided 2026-08-03, DEC-025 — "my assignments" dashboard pulled into scope)* | **Logged-in non-organizer accounts land on a content-free hub home page.** Replacing the TASK-005 landing-page placeholder (`internal/web/pages.templ`'s `homePage`) surfaced that only `meet_organizer`+ sessions have an instance-wide workspace (`/meets`) to be pointed at from `/`; `competition_office`, `field_official`, and `entry_submitter` sessions have no personal landing surface — by design today, they work from meet-scoped links (e.g. `/meets/{id}/checkin`) the organizer shares out of band, so the shipped home page for those roles is deliberately just "you're logged in; use the link your organizer sent you" (`home.intro_loggedin`, DE/FR). Confirm whether a personal "my assignments" dashboard (e.g. listing the meets/units a field official is scoped to) is wanted for a later release, or whether out-of-band links remain the intended pattern at this club scale. | Founder / tech lead | No — today's copy is accurate to the shipped capability; a future dashboard is additive and does not require reworking the role model |
| OQ-090 *(TASK-036, 2026-08-04)* | **French-locale TAF3 unranked-standings marker text is unverified.** The LV Langenthal Gesamtrangliste evidence (DEC-016/OQ-020) is German-language TAF3 output ("aufg."/"n.a."); no French-locale UBS Kids Cup individual Dreikampf Rangliste could be located to confirm the equivalent abbreviations. The shipped `standings.unranked_missing`/`standings.unranked_out_of_competition` FR strings ("ab."/"h.c.") are a reasoned assumption — standard French sporting abbreviations for "abandon" and "hors concours" respectively, the latter corroborated by a UBS Kids Cup team-relay result list's own legend ("A = Hors concurrence", https://www.ubs-kidscup.ch/fileadmin/ubskidscup/ubs_kids_cup_team/Ranglisten/Rangliste_Yverdon.pdf) — not a literal citation from the individual-Dreikampf French output. Obtain a French-locale TAF3 UBS Kids Cup Gesamtrangliste and diff the exact abbreviations. | Founder / tech lead | No — a values-only i18n string correction (`internal/web/i18n/locales/fr.json`) if the real abbreviations differ |
| OQ-091 *(TASK-036, 2026-08-04)* | **Out-of-competition flag has no operator-facing UI toggle.** The DEC-016/OQ-020 evidence's Thome Lauriane row (every discipline mark present, still unranked "n.a.") suggested an ausser-Konkurrenz/hors-concours participant status; `store.Participant.OutOfCompetition` (migration 0020) and `ResultsService.SetOutOfCompetition` (office-role capability, audited as `participant.out_of_competition.update`) are a complete, tested mechanism — standings correctly exclude a flagged participant from ranking in both provisional and final modes while keeping their marks visible (`TestSetOutOfCompetition`) — but no roster/entries page exposes a control to set it; today it is reachable only via a direct service call. Add a roster-page toggle (mirroring the existing bib-edit control) when an operator surface for it is prioritized. | Founder / tech lead | No — the standings-side mechanism is complete and tested; a UI toggle is additive |
| OQ-094 *(TASK-035, 2026-08-03)* | **The new public-results render cache (ADR-004 §9, DEC-015/TASK-035) invalidates on the results-changed bus event, but two other writes that affect the same public pages are not (yet) wired to that event: `MeetService.UpdateMeet` (name/venue/dates/results-positioning edits) and `PrivacyService.EraseAthlete`/`PurgeExpired`/`PurgeExpiredAtStartup` (SYS-101/SYS-102 erasure and retention purge).** `ResultsService.SetConsent` (SYS-103 consent withdrawal) *is* wired — it now calls the same `notifyChanged` hook every capture write uses, restoring the "next public read already reflects this write" promise UC-023 #3 documents. The two unwired paths instead rely on the cache's 5s TTL as a bounded safety net (`publicResultsCacheTTL`, `internal/web/publiccache.go`) — comfortably inside SYS-071's 10s live-update budget, and no regression was found in testing (`TestPublicPathCrawlerNeverLeaksWithdrawnOrErasedNameSYS100SYS101UC024` passes because it never re-fetches a page that was rendered and cached *before* the erasure in the same test run). Whether a bounded TTL is an acceptable permanent answer for the erasure/purge case, or whether SYS-101's erasure guarantee warrants extending the invalidation hook to `PrivacyService` too (it would need to resolve which meets an athlete participated in, since erasure is athlete-scoped, not meet-scoped) is a design call worth a deliberate decision rather than defaulting to "the TTL was good enough." | Founder / tech lead | No — erasure/purge/meet-edits are rare, deliberate operator actions with no measured incident, and the TTL bounds the exposure window; a full fix is a small, well-understood follow-up if wanted |
| OQ-101 *(TASK-040, 2026-08-05)* | **The `fieldStandings` fix for OQ-036/DEC-024 (a settled-level mark correction now reflects in the capture page's live standings, not just the "Resultat" column) substitutes the corrected mark as the athlete's sole ranking input, losing D5.6's full next-best-mark tie-break vector against any *other, uncorrected* athlete they end up tied with.** E.g. if Anna's corrected 3.60 exactly ties Bea's raw-attempt 3.60 (Bea's next-best is 3.40, a real second-round mark), the tie-break would compare Anna's synthetic single-attempt series (no second mark) against Bea's real one — Anna loses the tie-break by default (fewer marks = worse) even if Anna's own un-corrected second attempt would have beaten Bea's 3.40. This is a narrower gap than OQ-036's original "not a resumable capture flow" framing: it only bites when a correction produces an exact tie with another athlete's raw best, a rare double coincidence. Confirm whether this residual edge case needs a real fix (the correction form would need an explicit ordered mark sequence, not just a single "corrected mark" field) or is an acceptable simplification given corrections are already a rare, office-reviewed action. | Founder / tech lead | No — the common case (a correction moves an athlete's rank unambiguously) is correct; the narrow tie-break gap is a documented simplification, not silently wrong data |
| OQ-102 *(TASK-040, 2026-08-05)* | **Vertical-jump corrections can now earn a record/best flag (`CorrectResult` runs `evaluateRecord` for every family once `in.Status == StatusNone`, TASK-040 widened the family gate), but normal vertical capture (`SaveVerticalTrial`) never evaluates records at all (OQ-046's TASK-021 scope note: vertical jump has no record-flagging support yet).** This is an inconsistency a real meet could hit: an athlete's height is captured and settled normally (no flag, by design gap), then a later settled-level correction to the same or a different height *does* get record-evaluated and could surface an "MR"/"PB" flag a normal capture of the identical height never would have shown. Confirm whether `CorrectResult` should skip record evaluation for vertical family too (matching today's capture-time behavior exactly, deferring record support to whenever vertical jump gets it properly) or whether this is the first slice of vertical record support and should stay as shipped. | Founder / tech lead | No — either answer is a small, self-contained change (an added family check in `CorrectResult`, or documenting the asymmetry as intentional); nothing downstream assumes one behavior over the other |

## 8. Founder answers (received 2026-08-03) and resulting decisions

Guided walkthrough of the remaining category-1 (founder/tech-lead) items from
`docs/delivery/oq-triage-2026-07-15.md`; each answer converted into a decision **DEC-###**
(numbering continues from DEC-014). Category-2a/2b items (external contacts required) remain open.

| OQ | Founder decision (2026-08-03) | Decision |
|----|-------------------------------|----------|
| OQ-066 | Read-path fix = per-meet render cache + pooled readers | **DEC-015:** Public-results read path = **per-meet cached render of the public results page/fragment, invalidated by the existing results-changed bus event, plus a pooled WAL read connection set**. Single-WRITER invariant (ADR-004) untouched. Record as an ADR-004 amendment; `TestSYS122TwoThousandConcurrentViewers` flips green when the follow-up task lands. Scheduled as **TASK-035**. |
| OQ-020 | Match the official UKC convention, including the 1-point floor | **DEC-016:** **Final** standings follow the official TAF3 convention — athletes without all series disciplines are listed **unranked at the bottom** ("aufg."/"n.a."-style), and a present-but-no-valid-attempt discipline scores the **1-point floor**; **live** standings keep rank-by-partial-total, labeled **provisional**. Requires a final-vs-provisional distinction in `Standings` and a UC-033 #3 wording amendment. Scheduled as **TASK-036**. |
| OQ-086 | GHCR | **DEC-017:** Container images publish to **GHCR** (`ghcr.io`), pushed from CI on release tags. Values-only edits to `docs/ops/release-process.md` §5 and `docs/ops/quickstart.md` §1 Option B, plus a push step in the release workflow. Scheduled as **TASK-037**. |
| OQ-088 | Latest-only, best-effort | **DEC-018:** Support window = **latest release only, best-effort** (the interim default in `docs/ops/release-process.md` §6 is confirmed as the real 0.x policy). Revisit if/when a club runs an older line in production. Scheduled as **TASK-037**. |
| OQ-087 | Cosign keyless in CI; defer OS-level signing | **DEC-019:** Sign the **container image and checksums file with sigstore/cosign keyless** in CI. macOS notarization and Windows Authenticode are **deferred** (cost/no adopters); document the Gatekeeper/SmartScreen workaround in the quickstart. Scheduled as **TASK-037**. |
| OQ-027, OQ-032, OQ-049, OQ-057 | Relays stay roster-only in 0.1 | **DEC-020:** Release 0.1 ships relays as **entries/roster-only** (teams entered, composed, bib-assigned; no result capture, timing import, per-leg eligibility, or entry standards). Post-0.1: **one bundled relay task** covering manual capture, FinishLynx roster convention, per-leg eligibility, entry standards, and the OQ-059 relay-scoped erasure test — instead of five scattered gaps. All extensions were verified additive. Logged in the work-breakdown *Later* table; TASK-scheduled when triggered. |
| OQ-067 | Ship entry search in 0.1 | **DEC-021:** A **server-side entry/roster search (query-param filter)** is a release-0.1 feature; add the reserved SYS-120 benchmark sub-test when it lands. Scheduled as **TASK-038**. |
| OQ-061 | Neutral default for 0.1 | **DEC-022:** 0.1 ships on the **neutral design-token set**; brand identity (logo, colors, wordmark) is decided later — a token-level data change, timed for when there is a PoC demo audience. |
| OQ-033 (+ OQ-025, OQ-026) | Licence field yes; sign-up and finer caps deferred | **DEC-023:** The online-entry form gains an **optional licence-number field** (closes the A/B-tier operator round-trip). Self-service submitter sign-up (OQ-025) and per-club/per-athlete caps (OQ-026) stay **deferred** — organizers provision accounts out of band; the per-event cap remains the shipped granularity. Licence field scheduled as **TASK-039**. |
| OQ-036 (+ OQ-037, OQ-038, OQ-039) | Field-correction UI yes; the rest deferred/closed | **DEC-024:** The capture page gains a **field-event correction UI** (settled-level, matching the track form); attempt-level resumable corrections stay later. Ticking protest clock (OQ-037) and structured jury/protest entity (OQ-039) **deferred**. OQ-038 **closed**: the FinishLynx `.lif` import is the sole reaction-time source; no manual field. Correction UI shipped by **TASK-040** (both horizontal and vertical field rows; see OQ-036's resolution note and OQ-101/OQ-102). |
| OQ-070, OQ-089 (+ OQ-046, OQ-077) | Bulk DNS and assignments dashboard yes; the rest deferred | **DEC-025:** In scope: a **"mark all remaining as DNS" bulk action** on a still-open unit (second real SYS-114 bulk operation), and a **"my assignments" landing dashboard** for non-organizer roles (field official / competition office / entry submitter see the meets/units they are scoped to). Deferred: offline vertical-jump capture (OQ-046 — ask the pilot club whether HJ/PV officials ever roam beyond hub Wi-Fi before building) and in-flight submit feedback (OQ-077). Scheduled as **TASK-041** (bulk DNS) and **TASK-042** (dashboard). |
| OQ-041, OQ-043 | Confirm both shipped defaults | **DEC-026:** Privacy defaults **confirmed as intended**: publication-withdrawal suppression applies at **any age** (not minors only), and the sole suppression mode is **"neutralize"** (row kept, name/club blanked) — the more protective reading in small divisions. Both closed; escalate to counsel only if challenged. OQ-040 (18 threshold) and OQ-042 (90-day retention) remain open pending legal/federation input (category 2b). |
